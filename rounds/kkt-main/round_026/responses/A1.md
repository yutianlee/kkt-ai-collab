Summary:

Source note: I am using the Round 26 A1 prompt and current Round 25 state from the uploaded packet. The prompt explicitly asks A1 to turn Round 25 near-certificates into permanent proof-state artifacts, not to introduce a new proof architecture, and it requires the Stage A schema plus sections titled "Permanent artifacts," "Conditional rational-Bessel theorem," and "What remains unproved." The Round 25 judge state says the conjecture and arbitrary-degree finite-$B$ first-lobe theorem are still unproved, while the endpoint-cap route remains the main route and the $n=1$ theorem is reduced to a final arithmetic appendix.

I do **not** claim the real-parameter KKT conjecture is solved. The main concrete progress in this response is:

1. A lemma-bank-ready statement of the endpoint-cap first-lobe reduction.
2. A completed $n=1$ residual endpoint certificate, conditional only on a standard real-Stirling/Gamma lower-bound lemma, with the remaining scalar endpoint inequality reduced to exact rational comparisons.
3. A cleaned conditional version of A2's rational-coordinate Bessel relative-amplitude theorem, with the no-zero interval, Gronwall factor, $j'_{\alpha,1}$ restriction, and missing phase/gamma hypotheses separated.
4. A short literature-status appendix identifying the exact external theorem dependencies still needed.
5. A bounded exploratory checklist for Krasikov-style higher-order Sonin envelopes in $n=1,2$ only.

The proof state after this response should be recorded as:

- endpoint-cap first-lobe reduction: **lemma-bank ready, conditional on imported global modules**;
- $n=1$ residual endpoint theorem: **proved modulo acceptance of the stated standard real-Gamma lower bound, with all remaining comparisons rationalized**;
- rational-Bessel relative-amplitude route: **conditional theorem, not closure**;
- arbitrary $n\ge2$ first-lobe amplitude theorem: **open**.

Main claim or direction:

The correct permanent proof spine is still the endpoint-cap first-lobe route:

$$
u=\frac{B(1-x)}2,\qquad B=n+\alpha+\beta+1,
$$

followed by the cap confinement

$$
0\le u\le u_\sigma=\frac{nB}{B+n-1}\le n,
$$

the exact endpoint equation

$$
(p_BH')'+q_BH=0,
$$

the monotone quadratic Sonin product

$$
K_B(u)=p_B(u)q_B(u)
=
-\frac{\alpha^2}{4}+\Lambda_Bu-\Delta_Bu^2,
$$

and the reduction of any residual cap failure to the first critical point $u_1$ after the first cap turning point $u_0$.

The active arbitrary-degree theorem remains:

$$
|H(u_1)|
\le
T_{n,\alpha,\beta}
=
\left(
\frac{(n+1)B}{(n+\alpha+1)(B-\alpha)}
\right)^{1/4}.
$$

This theorem is not proved here. The new proved low-degree artifact is the residual $n=1$ theorem.

Detailed reasoning:

## Permanent artifacts

### Artifact 1. Right endpoint cap and first-lobe reduction

**Theorem: right endpoint cap first-lobe reduction.**

Assume the imported global modules have already cleared their regions:

1. central branch-safe contour clearance;
2. weighted-energy clearance;
3. small right-endpoint exponent theorem for $0\le\alpha\le1/2$;
4. left-right symmetry under $(\alpha,\beta,x)\mapsto(\beta,\alpha,-x)$;
5. boundary cases $n=0$, $\alpha=0$, $\beta=0$, no turning point, and no first critical point.

In the residual right-endpoint strip assume

$$
n\ge1,\qquad
\frac12<\alpha<\alpha_E(n):=\frac{(2n+1)(n+1)}{2n+3},
\qquad
\beta\ge0.
$$

Set

$$
B=n+\alpha+\beta+1,\qquad
u=\frac{B(1-x)}2,
\qquad
H(u)=g_n^{(\alpha,\beta)}\left(1-\frac{2u}{B}\right).
$$

The central interface

$$
\sigma=\frac{\alpha+\beta}{\alpha+\beta+2n}
$$

maps to

$$
u_\sigma
=
\frac{B(1-\sigma)}2
=
\frac{nB}{B+n-1}
\le n,
$$

so after central clearance the residual cap is

$$
0\le u\le u_\sigma\le n.
$$

Inside this cap, $H$ satisfies

$$
(p_BH')'+q_BH=0,
$$

where

$$
p_B(u)=u\left(1-\frac uB\right)
$$

and

$$
q_B(u)
=
n+\frac12-\frac{n+1}{2B}
-
\frac{
\left(\left(1-\frac{n+1}{B}\right)u-\alpha\right)^2
}{
4u\left(1-\frac uB\right)
}.
$$

Define

$$
K_B(u)=p_B(u)q_B(u).
$$

Then

$$
K_B(u)
=
-\frac{\alpha^2}{4}+\Lambda_Bu-\Delta_Bu^2,
$$

with

$$
r_B=1-\frac{n+1}{B}=\frac{\alpha+\beta}{B},
$$

$$
c_B=n+\frac12-\frac{n+1}{2B},
$$

and

$$
\Lambda_B=c_B+\frac{\alpha r_B}{2},
\qquad
\Delta_B=\frac{c_B}{B}+\frac{r_B^2}{4}.
$$

The cap derivative identity is

$$
K_B'(u_\sigma)
=
\frac{(\alpha+\beta)(n+\alpha+1)}{2B}
=
\frac{\alpha}{2}+\frac{\beta(n+1)}{2B}.
$$

Because $K_B$ is a concave quadratic, $K_B'$ decreases with $u$, and hence for $0\le u\le u_\sigma$,

$$
K_B'(u)\ge K_B'(u_\sigma)
=
\frac{\alpha}{2}+\frac{\beta(n+1)}{2B}.
$$

In the residual strip $\alpha>1/2$, this gives

$$
K_B'(u)>\frac14.
$$

This lower bound is **not** a global statement for all $\alpha,\beta\ge0$; it is a residual-strip statement.

Let $u_0$ be the first zero of $K_B$ in the cap, if such a zero exists. Since

$$
K_B(0)=-\frac{\alpha^2}{4}<0
$$

for $\alpha>0$, on the forbidden segment before $u_0$ one has $q_B<0$. The regular Frobenius branch satisfies

$$
H(u)\sim A_{n,\alpha,B}u^{\alpha/2},
\qquad
A_{n,\alpha,B}>0.
$$

With

$$
W(u)=p_B(u)H'(u),
$$

the equation gives

$$
W'(u)=-q_B(u)H(u)>0
$$

as long as $H>0$ and $q_B<0$. Since $H>0$ and $W>0$ near $u=0$, $H$ remains positive and increasing up to $u_0$. Thus there is no forbidden-zone local maximum.

On intervals with $q_B>0$, the Sonin functional

$$
S_B(u)=H(u)^2+\frac{p_B(u)H'(u)^2}{q_B(u)}
$$

satisfies

$$
S_B'(u)
=
-\frac{K_B'(u)}{q_B(u)^2}H'(u)^2
\le0.
$$

Thus local extrema in the allowed zone do not increase in amplitude as $u$ moves from the endpoint toward the central boundary.

Therefore, if $u_1$ is the first critical point of $H$ after $u_0$, then every residual endpoint-cap failure must occur at $u_1$. If $K_B$ has no cap zero, if $u_0=u_\sigma$, or if $u_1$ is absent, the cap is controlled by forbidden-zone ascent, allowed-zone monotonicity, and the imported central-boundary estimate.

The remaining arbitrary-degree amplitude theorem is exactly:

$$
|H(u_1)|
\le
\left(
\frac{(n+1)B}{(n+\alpha+1)(B-\alpha)}
\right)^{1/4}.
$$

Status: **proved reduction**, conditional on the imported global modules. The amplitude theorem remains open for $n\ge2$.

### Artifact 2. Degree-one residual endpoint certificate

I now record the $n=1$ residual theorem in final form.

**Theorem: degree-one residual endpoint certificate.**

Let

$$
n=1,\qquad
\frac12\le\alpha\le\frac65,\qquad
\beta\ge0,
$$

and set

$$
B=\alpha+\beta+2,\qquad
H_1(u)=g_1^{(\alpha,\beta)}\left(1-\frac{2u}{B}\right).
$$

Then on the residual cap

$$
0\le u\le u_\sigma=1,
$$

one has

$$
H_1(u)^4<T_{1,\alpha,\beta}^4.
$$

For $n=1$,

$$
P_1^{(\alpha,\beta)}\left(1-\frac{2u}{B}\right)
=
\alpha+1-u.
$$

Therefore the KKT normalization gives the exact formula

$$
H_1(u)^2
=
\frac{\Gamma(B)}
{\Gamma(\alpha+2)\Gamma(\beta+2)}
\left(\frac{u}{B}\right)^\alpha
\left(1-\frac{u}{B}\right)^\beta
(\alpha+1-u)^2.
$$

The previously audited scalar reduction gives

$$
H_1(u)^4\le E(\alpha),
$$

where

$$
E(\alpha)=
\left(
\frac{
4\alpha^\alpha(\alpha+1)^{\alpha+2}
}{
(\alpha+2)^{\alpha+2}\Gamma(\alpha+2)
}
\right)^2.
$$

It remains to show

$$
E(\alpha)<0.39
$$

on

$$
\frac12\le\alpha\le\frac65.
$$

Let

$$
L(\alpha)=\frac12\log E(\alpha)
=
\log 4
+\alpha\log\alpha
+(\alpha+2)\log(\alpha+1)
-(\alpha+2)\log(\alpha+2)
-\log\Gamma(\alpha+2).
$$

Then

$$
L''(\alpha)
=
\frac1\alpha+\frac1{\alpha+1}-\frac1{\alpha+2}
-\frac1{(\alpha+1)^2}
-\psi'(\alpha+2).
$$

Using

$$
\psi'(x)=\sum_{k=0}^{\infty}\frac1{(x+k)^2}
<
\frac1x+\frac1{x^2},
\qquad x>0,
$$

with $x=\alpha+2$, we obtain

$$
L''(\alpha)
>
\frac1\alpha+\frac1{\alpha+1}-\frac2{\alpha+2}
-\frac1{(\alpha+1)^2}
-\frac1{(\alpha+2)^2}.
$$

The right side simplifies exactly to

$$
\frac{\alpha^3+5\alpha^2+7\alpha+4}
{\alpha(\alpha+1)^2(\alpha+2)^2}>0.
$$

Hence $L$ is strictly convex on $[1/2,6/5]$, and therefore

$$
\max_{1/2\le\alpha\le6/5}E(\alpha)
=
\max\left(E(1/2),E(6/5)\right).
$$

At the left endpoint,

$$
E\left(\frac12\right)
=
\frac{3456}{3125\pi}
<
\frac{3456}{3125\cdot3}
<
0.39.
$$

At the right endpoint,

$$
E\left(\frac65\right)
=
\left(
\frac{
4(6/5)^{6/5}(11/5)^{16/5}
}{
(16/5)^{16/5}\Gamma(16/5)
}
\right)^2.
$$

Use the following standard real-Stirling lower-bound lemma:

For $x>0$,

$$
\Gamma(x)>
\sqrt{2\pi}\,x^{x-1/2}
\exp\left(-x+\frac{1}{12x+1}\right).
$$

At $x=16/5$, this gives

$$
\Gamma(16/5)>\frac{121}{50}.
$$

One rational verification of this last displayed inequality is obtained from the preceding lower bound together with the elementary rational enclosures

$$
\pi>\frac{333}{106},
\qquad
e^6<\frac{807}{2},
\qquad
e^{344/985}<\frac{71}{50},
$$

because after squaring and raising to the fifth power, the desired inequality reduces to the exact positive integer comparison

$$
546832635323388895801756641787683574225305600000000000000000000
>
545476780418056278738965898952874081039742339592290496826171875.
$$

Thus $\Gamma(16/5)>121/50$ is certified from the stated real-Stirling inequality.

Using $\Gamma(16/5)>121/50$,

$$
E(6/5)
<
\left[
\frac{200}{121}
\left(\frac65\right)^{6/5}
\left(\frac{11}{16}\right)^{16/5}
\right]^2.
$$

To prove that the last expression is $<39/100$, raise both sides to the fifth power. The inequality becomes

$$
\left(\frac{200}{121}\right)^{10}
\left(\frac65\right)^{12}
\left(\frac{11}{16}\right)^{32}
<
\left(\frac{39}{100}\right)^5.
$$

Equivalently,

$$
6515193417785097503906250000000000
<
6980759278409497982073330556993536,
$$

which is an exact integer comparison.

Therefore

$$
E(6/5)<0.39.
$$

So for all $1/2\le\alpha\le6/5$,

$$
E(\alpha)<0.39.
$$

The KKT target for $n=1$ satisfies

$$
T_{1,\alpha,\beta}^4
=
\frac{2B}{(\alpha+2)(B-\alpha)}
=
\frac{2(\alpha+\beta+2)}{(\alpha+2)(\beta+2)}
\ge
\frac{2}{\alpha+2}
\ge
\frac58
>
0.39.
$$

Hence

$$
H_1(u)^4\le E(\alpha)<0.39<\frac58\le T_{1,\alpha,\beta}^4,
$$

which proves the degree-one residual endpoint theorem.

Status: **proved**, conditional only on the standard real-Stirling lower-bound lemma stated above. If the collaboration demands a fully independent appendix, A4 should replace the real-Stirling citation by outward-rounded Arb/MPFI logs for $E(1/2)$ and $E(6/5)$.

## Conditional rational-Bessel theorem

This section rewrites A2's rational-coordinate result as a conditional theorem, not a closure.

Set

$$
z=\frac{Bu}{B-u},
\qquad
Y(z)=z^{1/2}H(u(z)).
$$

Then the certified rational-coordinate normal form is

$$
Y''+
\left(
\frac{\Lambda_B}{z}
+\frac{1-\alpha^2}{4z^2}
+\Delta Q(z)
\right)Y=0,
$$

where

$$
\Delta Q(z)
=
-\frac{\Lambda_B}{B+z}
-\frac{\Delta_BB^2}{(B+z)^2}.
$$

Let

$$
W_1(z)=\sqrt z\,J_\alpha(2\sqrt{\Lambda_Bz}).
$$

Then $W_1$ solves the Bessel-core equation

$$
W_1''+
\left(
\frac{\Lambda_B}{z}
+\frac{1-\alpha^2}{4z^2}
\right)W_1=0.
$$

On any interval $0<z\le z_*$ on which

$$
W_1(z)>0,
$$

equivalently

$$
2\sqrt{\Lambda_B z}<j_{\alpha,1},
$$

write

$$
Y(z)=h(z)W_1(z).
$$

Because $\Delta Q(z)<0$, the exact relative-amplitude equation is

$$
(W_1^2h')'
=
|\Delta Q|W_1^2h.
$$

The regular endpoint matching gives

$$
h(0)=M_{n,\alpha,B},
$$

where

$$
M_{n,\alpha,B}
=
\sqrt{
\frac{\Gamma(n+\alpha+1)\Gamma(B)}
{\Gamma(n+1)\Gamma(B-\alpha)}
}
(B\Lambda_B)^{-\alpha/2}.
$$

Assuming $h$ remains positive on $[0,z_*]$, the Volterra-Gronwall bound is

$$
h(z)
\le
M_{n,\alpha,B}\exp(\mathcal I(z)),
$$

where

$$
\mathcal I(z)
=
\int_0^z
\frac{1}{W_1(x)^2}
\int_0^x
|\Delta Q(s)|W_1(s)^2\,ds\,dx.
$$

This is the exact safe form. If one additionally assumes the A2 monotone-Bessel-kernel estimate on the pre-peak interval

$$
T:=2\sqrt{\Lambda_Bz}\le j'_{\alpha,1},
$$

then one may use the sharper bound

$$
\int_0^z
\frac{1}{W_1(x)^2}
\int_0^x
W_1(s)^2\,ds\,dx
\le
\frac{T^4}{64\Lambda_B^2}.
$$

Under that additional pre-peak hypothesis,

$$
\mathcal I(z)
\le
\left(
\sup_{0\le s\le z}|\Delta Q(s)|
\right)
\frac{T^4}{64\Lambda_B^2}.
$$

Since

$$
|\Delta Q(s)|
=
\frac{\Lambda_B}{B+s}
+
\frac{\Delta_BB^2}{(B+s)^2}
\le
\frac{\Lambda_B}{B}+\Delta_B,
$$

a crude fully explicit consequence is

$$
\mathcal I(z)
\le
\left(
\frac{\Lambda_B}{B}+\Delta_B
\right)
\frac{T^4}{64\Lambda_B^2},
\qquad
T\le j'_{\alpha,1}.
$$

This theorem does **not** yet prove a small-$\alpha$ KKT range. The missing hypotheses are:

1. **Phase/peak safety.** One must prove that the true first critical point of $Y$ or $H$ lies in the pre-peak interval $T\le j'_{\alpha,1}$, or else replace the kernel bound by a zero-safe bound valid up to some $T<j_{\alpha,1}$.
2. **No-zero safety.** The integration interval must satisfy $W_1>0$, i.e.

$$
2\sqrt{\Lambda_Bz}<j_{\alpha,1}.
$$

3. **Derivative critical-point conversion.** The critical point condition for $H$ is not identical to the critical point condition for the Bessel reference $W_1$. The phase delay must be bounded explicitly.
4. **Gamma envelope.** One needs a one-sided estimate strong enough to combine

$$
M_{n,\alpha,B}\exp(\mathcal I(z))
$$

with the Bessel maximum and still fit below

$$
T_{n,\alpha,\beta}.
$$

5. **Endpoint target normalization.** The bound must be transported back from $Y=z^{1/2}H$ to $H$ at the actual first critical point, not at an arbitrary Bessel reference peak.

Status: **conditional theorem and useful analytic mechanism**, not proof closure.

## Literature-status appendix

1. **KKT source.** The source problem is Koornwinder--Kostenko--Teschl, "Jacobi Polynomials, Bernstein-type Inequalities and Dispersion Estimates for the Discrete Laguerre Operator," *Advances in Mathematics* 333 (2018), 796--821, DOI `10.1016/j.aim.2018.05.038`, arXiv `1602.08626`. The arXiv record confirms the Jacobi Bernstein/discrete Laguerre connection.

2. **Landau Bessel monotonicity.** Landau's paper "Bessel Functions: Monotonicity and Bounds," *Journal of the London Mathematical Society* 61(1), 197--215 (2000), DOI `10.1112/S0024610799008352`, proves monotonicity in the Bessel order at stationary points and implies that $\sup_x|J_\nu(x)|$ strictly decreases from $1$ to $0$ as $\nu$ increases from $0$ to $\infty$.

For the project, once a genuine Bessel reduction is established, this supports reducing

$$
\sup_{\nu\ge1/2,t\ge0}|J_\nu(t)|
$$

to $\nu=1/2$. The half-order function is

$$
J_{1/2}(t)=\sqrt{\frac{2}{\pi t}}\sin t,
$$

and its first positive stationary point satisfies

$$
\tan t=2t.
$$

The corresponding maximum is approximately $0.6791921047<0.680$. This computation must still be written with interval signs if it is used as a formal appendix.

3. **Gamma inequalities.** DLMF §5.6 records gamma-function inequalities, including Gautschi-type inequalities, and DLMF §5.11 records the gamma-function asymptotic framework. For the $n=1$ theorem above, the required dependency is the explicit real-Stirling lower bound

$$
\Gamma(x)>
\sqrt{2\pi}\,x^{x-1/2}
\exp\left(-x+\frac{1}{12x+1}\right),
\qquad x>0.
$$

This is stronger than needed only at $x=16/5$. Future arbitrary-degree gamma-envelope work needs a broader Binet/Stirling/Wendel/Gautschi/Kershaw regime split.

4. **Dunster--Gil--Segura / Olver candidate.** Dunster--Gil--Segura's "Simplified error bounds for turning point expansions" states that it gives explicit elementary error bounds for Airy-type simple-turning-point expansions, simplifying classical Olver bounds. Their publication list records "Simplified error bounds for turning point expansions," *Analysis and Applications* 19(4), 647--678, DOI `10.1142/S0219530520500104`, and "Sharp error bounds for turning point expansions," *Journal of Classical Analysis* 18(1), 49--81, DOI `10.7153/jca-2021-18-05`.

The KKT mapping required for a future theorem is:

$$
H_{\tau\tau}+K_B(u(\tau))H=0,
\qquad
\tau=\log\frac{u}{B-u},
$$

with Langer coordinate $\zeta$ defined by

$$
K(\tau)=\zeta(\tau)\zeta_\tau(\tau)^2,
\qquad
K(\tau)=K_B(u(\tau)),
$$

and transformed equation

$$
W_{\zeta\zeta}+\zeta W=\Psi_B(\zeta)W.
$$

A DGS/Olver theorem must be instantiated with the exact residual $\Psi_B$, the exact interval from the forbidden Frobenius cut through the first allowed lobe, the exact Airy weights/moduli, and a scalar inequality implying the KKT target. Generic citation is not enough.

Dependencies:

1. Imported global KKT modules:
   - central-contour clearance;
   - weighted-energy clearance;
   - small-exponent clearance for $0\le\alpha\le1/2$ on the right;
   - symmetry for the left endpoint;
   - boundary cases.

2. Endpoint-cap algebra:
   - affine endpoint ODE;
   - cap length;
   - $K_B$ quadratic formula;
   - cap monotonicity;
   - forbidden-zone ascent;
   - Sonin ordering.

3. Degree-one theorem:
   - exact $H_1(u)^2$ formula;
   - scalar envelope $H_1(u)^4\le E(\alpha)$;
   - trigamma identity and tail bound;
   - real-Stirling lower bound giving $\Gamma(16/5)>121/50$.

4. Rational-Bessel conditional theorem:
   - rational-coordinate normal form;
   - Bessel-core solution;
   - positivity/no-zero interval for $W_1$;
   - Volterra-Gronwall;
   - gamma normalization $M_{n,\alpha,B}$.

5. Literature dependencies:
   - KKT source;
   - Landau for Bessel order monotonicity;
   - real-Gamma inequalities;
   - DGS/Olver for future Langer/Airy bounds.

Potential gaps:

## What remains unproved

1. The arbitrary-degree first-lobe amplitude theorem remains open:

$$
|H(u_1)|
\le
\left(
\frac{(n+1)B}{(n+\alpha+1)(B-\alpha)}
\right)^{1/4}.
$$

2. The $n=2$ residual theorem is not proved. The corrected polynomial and critical cubic are available, but no full interval/root-isolation certificate exists.

3. The rational-Bessel route does not yet control the true first critical point. The current clean Volterra kernel bound is only justified before the first Bessel peak $j'_{\alpha,1}$.

4. The gamma envelope

$$
M_{n,\alpha,B}
=
\sqrt{
\frac{\Gamma(n+\alpha+1)\Gamma(B)}
{\Gamma(n+1)\Gamma(B-\alpha)}
}
(B\Lambda_B)^{-\alpha/2}
$$

is not bounded sharply enough for arbitrary $n$.

5. The DGS/Olver Langer route has not been instantiated with exact KKT constants.

6. The half-order Bessel maximum needs an outward-rounded interval appendix if used as a formal certificate.

7. The degree-one theorem depends on accepting the stated real-Stirling lower bound. If the lemma bank insists on no external gamma bound without proof, then A4/A3 should supply an outward-rounded interval enclosure or a self-contained Binet appendix.

Counterexample or obstruction search:

I checked the main edge cases conceptually.

1. **$n=0$.** No endpoint cap is needed; this is a base case and should remain outside the residual theorem.

2. **$\alpha=0$ and $\alpha=1/2$.** These should remain assigned to boundary/small-exponent modules. The residual right-cap proof assumes $\alpha>1/2$ when using $K_B'(u)>1/4$.

3. **$\beta=0$.** No obstruction appears in the endpoint-cap algebra because $B-\alpha=n+\beta+1>0$. However, $\beta=0$ is often a hard finite-$B$ face for amplitude estimates and must be tested in $n=2$.

4. **$\beta\to\infty$.** This is the Laguerre face. The cap route avoids the global Laguerre inequality, but rational-Bessel and Langer constants still need to behave correctly at $\theta=0$.

5. **Rational-Bessel peak mismatch.** This is the main obstruction to A2's conditional theorem. If the true first critical point lies after $j'_{\alpha,1}$ in the reference variable, then the current $T^4/(64\Lambda_B^2)$ kernel bound is not enough.

6. **Gamma envelope obstruction.** Even if the relative-amplitude factor is controlled, a weak bound on $M_{n,\alpha,B}$ can consume the KKT margin.

7. **$n=2$.** No counterexample is known, but without root isolation and value checks it must remain open.

Divergent alternatives and 20% exploration:

The only structural exploratory slot I recommend preserving is a Krasikov-style higher-order Sonin envelope. It should not replace the main route. It should be tested only as a falsifiable low-degree ansatz.

The target would be a functional of the form

$$
V(u)=A(u)H(u)^2+B(u)H'(u)^2+C(u)H(u)H'(u),
$$

or, preferably in first-order variables,

$$
V(u)=A(u)H(u)^2+B(u)\bigl(p_B(u)H'(u)\bigr)^2+C(u)H(u)p_B(u)H'(u),
$$

with rational or low-degree polynomial coefficients. A useful candidate must satisfy all of the following:

1. **Positivity:** $V(u)\ge c(u)H(u)^2$ on the cap, with $c(u)>0$ at critical points.
2. **Derivative sign:** $V'(u)$ has a controlled sign on the cap or on the allowed subinterval after the turning point.
3. **Turning-point regularity:** $V$ must avoid the pole of the ordinary Sonin functional at $q_B=0$.
4. **Endpoint normalization:** $V$ must be bounded explicitly from Frobenius data at $u=0$ or from the central boundary.
5. **Low-degree tests:** the ansatz must reproduce or strengthen the $n=1$ theorem and produce a nontrivial certified inequality for at least one $n=2$ parameter box.
6. **Failure criterion:** if no choice with $\deg A,\deg B,\deg C\le2$ can satisfy positivity and derivative sign for $n=1,2$, then the ansatz should be demoted.

A minimal first test is to use the $n=1$ exact equation and solve symbolically for $A,B,C$ making $V'$ a negative multiple of a square plus an explicitly signed remainder. If that fails, try $n=2$ only after the corrected cubic and interval boxes are ready.

Useful lemmas:

1. **Endpoint-cap theorem.** The residual right cap satisfies $0\le u\le u_\sigma\le n$, and the endpoint equation has monotone quadratic $K_B$.

2. **Forbidden-zone ascent.** Before the first zero of $K_B$, the regular branch is positive and increasing.

3. **Sonin ordering.** On $q_B>0$,

$$
S_B'(u)=-\frac{K_B'(u)}{q_B(u)^2}H'(u)^2\le0.
$$

4. **First-lobe reduction.** Any residual cap failure occurs at the first critical point after the first cap turning point.

5. **Degree-one residual endpoint theorem.** For $n=1$, $1/2\le\alpha\le6/5$, $\beta\ge0$, and $0\le u\le1$,

$$
H_1(u)^4<T_{1,\alpha,\beta}^4.
$$

6. **Rational-coordinate normal form.** With $z=Bu/(B-u)$ and $Y=z^{1/2}H$,

$$
Y''+
\left(
\frac{\Lambda_B}{z}
+\frac{1-\alpha^2}{4z^2}
+\Delta Q(z)
\right)Y=0,
$$

where

$$
\Delta Q(z)=
-\frac{\Lambda_B}{B+z}
-\frac{\Delta_BB^2}{(B+z)^2}.
$$

7. **Conditional rational-Bessel Gronwall lemma.** On any no-zero interval for $W_1$,

$$
h(z)\le M_{n,\alpha,B}\exp(\mathcal I(z)).
$$

What should be tested next:

1. **Archive the $n=1$ theorem.** A3 should copyedit the normalization and A4 should optionally supply outward-rounded enclosures for $E(1/2)$ and $E(6/5)$.

2. **Execute $n=2$ root isolation.** Use the corrected degree-two polynomial

$$
P_{2,\alpha,\theta}(u)
=
\frac{(\alpha+1)(\alpha+2)}2
-(\alpha+2)u
+
\frac12\left(1+\frac{\theta}{\alpha+3}\right)u^2
$$

and the corrected critical equation. Produce interval boxes, not simulated logs.

3. **Test rational-Bessel phase safety.** For sampled boxes, compare the true first critical point with $j'_{\alpha,1}$ and $j_{\alpha,1}$ in the variable $T=2\sqrt{\Lambda_Bz}$.

4. **Bound the rational-Bessel kernel beyond the first peak.** If the true first critical point exceeds $j'_{\alpha,1}$, derive a zero-safe kernel estimate up to $j_{\alpha,1}-\delta$.

5. **Develop the gamma envelope.** Split regimes:
   - fixed $\alpha$;
   - $\alpha=O(\sqrt n)$;
   - $\alpha=cn$;
   - $\theta=0$ Laguerre face;
   - $\theta=1$ $\beta=0$ face.

6. **Instantiate DGS/Olver.** Choose one theorem and map every KKT quantity to its hypotheses, residual, weight/modulus functions, and error inequality.

7. **Run bounded Krasikov test.** Test $n=1$ and one nontrivial $n=2$ box only. Do not pivot unless it produces a signed certificate.

Confidence:

- Endpoint-cap ODE, cap length, quadratic $K_B$, and cap monotonicity: **0.95**.
- Forbidden-zone ascent and Sonin first-lobe reduction, conditional on imported global modules and boundary wording: **0.90**.
- Degree-one residual endpoint theorem after the gamma appendix above: **0.92**.
- Publication readiness of the $n=1$ theorem after A3 normalization copyedit: **0.85**.
- Correctness of the rational-coordinate Bessel normal form and relative-amplitude identity: **0.88**.
- Current rational-Bessel theorem as a closure of any full small-$\alpha$ KKT regime: **0.30**.
- Correctness of the allowed-side Airy/Langer propagation lemma as an ODE estimate: **0.85**.
- Current Langer/DGS material as proof of the arbitrary-degree theorem: **0.20**.
- Truth of the $n=2$ residual theorem: **0.75**, but proof status remains open.
- Overall confidence that the full real-parameter KKT conjecture is proved now: **0.18**.
- Confidence that the endpoint-cap first-lobe route remains the best main strategy: **0.90**.
