# Summary

Round 19 yields critical structural refinements to the finite-cutoff Langer amplitude theorem but does not achieve proof closure for the real-parameter KKT conjecture. The most significant analytic advance is A1’s derivation of a critical-point scalar Airy envelope, which replaces the crude $|\operatorname{Ai}| + |\operatorname{Bi}|$ bound with a Wronskian-based denominator $D_1$. A2 provides a rigorous asymptotic evaluation of the Langer residual at the $\theta=0$ Laguerre face, demonstrating that the local variation converges to an $\mathcal{O}(1)$ constant rather than vanishing as $n \to \infty$. A3 delivers a comprehensive algebraic audit, certifying the Riccati Taylor coefficients and the rescaled degree-two critical cubic, while outlining a regime-split Binet/Robbins framework for the gamma-ratio envelope.

As A4, my role is to subject these architectural and asymptotic claims to strict numerical and interval-arithmetic stress tests. While the local algebraic derivations are largely correct, the transition from local turning-point models to global variation integrals remains heavily reliant on heuristic assumptions. No agent has executed the required outward-rounded interval certificates or computed the global Dunster-Gil-Segura (DGS) weighted variation. Furthermore, A3’s asymptotic description of the gamma-ratio cancellation contains a severe mischaracterization of the $\alpha=cn$ entropy term, and A2’s claim that the $\mathcal{O}(1)$ variation fits within the KKT slack ignores the exponential growth of the DGS weight functions in the forbidden zone. The next round must pivot from theoretical design to rigorous computational measurement and explicit error-control instantiation.

# Review scope and assumptions

This review evaluates the Stage A outputs of A1, A2, and A3 for Round 19. The scope is strictly limited to the mathematical validity of the proposed conditional theorems, asymptotic scaling arguments, and algebraic identities. I do not evaluate the imported global modules (central contour, weighted energy, small-exponent, symmetry), as they are treated as fixed assumptions for this workflow.

The following assumptions are imported from prior rounds and treated as fixed for this review:
1. The endpoint cap localization $0 \le u \le u_\sigma \le n$ is exact and correctly isolates the residual right-endpoint problem.
2. The exact dynamic oscillator $H_{\tau\tau} + K_B(u(\tau))H = 0$ in the logarithmic coordinate $\tau = \log(u/(B-u))$ is the correct fundamental form for amplitude analysis.
3. The Sonin product $K_B$ and its derivative lower bound $K_B'(u) \ge 1/4$ for $\alpha \ge 1/2$ are algebraically certified.
4. The forbidden-zone ascent and Sonin ordering correctly reduce the cap maximum to the first critical point $u_1$ after the first zero $u_0$ of $K_B$.

# Certification label legend

- **[PROVED]**: The claim is supported by a complete, explicit algebraic derivation or a cited theorem with verified hypotheses within the provided text.
- **[DERIVED-UNDER-ASSUMPTIONS]**: The claim follows logically from stated assumptions or local models, but global validity, uniform parameter dependence, or exact error constants are not established.
- **[HEURISTIC]**: The claim is supported by asymptotic scaling, numerical evidence, or physical intuition, but lacks a rigorous finite-parameter inequality, explicit error-control integral, or named theorem instantiation.
- **[CONJECTURED]**: The claim is a plausible mathematical statement without a current proof or sufficient derivation in the text.
- **[ASSUMED]**: The claim is taken as a premise without proof in the current context.
- **[LIKELY-FALSE]**: The claim contradicts known mathematics, contains a structural error, or relies on a demonstrably invalid logical step.

# Claim ledger by peer

| Agent | Claim | Label |
|---|---|---|
| A1 | Critical-point scalar Airy envelope using Wronskian denominator $D_1$ | [PROVED] |
| A1 | Finite-cutoff sufficient condition implies KKT target | [PROVED] (conditional) |
| A1 | Crude matrix norm $\Omega_A$ is sufficient if $D_1$ is used | [HEURISTIC] |
| A2 | Laguerre face local limit $\Psi_\infty(0) \approx \frac{4^{2/3}}{140}\alpha^{-4/3}$ | [PROVED] |
| A2 | Global variation $\mathcal{V}_A$ is $\mathcal{O}(1)$ and fits in KKT slack | [HEURISTIC] |
| A2 | Prüfer phase buffer requires physical distance $\mathcal{O}(n^{1/3})$ | [DERIVED-UNDER-ASSUMPTIONS] |
| A2 | Transition threshold $\alpha_{\text{thresh}}(n) = \Theta(n^{3/5})$ | [HEURISTIC] |
| A3 | Riccati Taylor coefficients $v(0), v_u(0), v_{uu}(0)$ | [PROVED] |
| A3 | Rescaled $n=2$ critical cubic is stable at $\theta=0$ | [PROVED] |
| A3 | Gamma-ratio leading $O(z\log z)$ pieces cancel leaving $O(\log n)$ | [LIKELY-FALSE] (for $\alpha=cn$) |
| A3 | Christoffel-Darboux / Turán identities yield no sharp upper bound | [PROVED] |

# Agent-by-agent review of A1

**Strongest contribution:** The derivation of the critical-point scalar Airy envelope. By exploiting the exact condition $H_\tau(\tau_1) = 0$, A1 translates the critical point into a linear constraint on the Airy coefficient vector $Z(\zeta_1)$. The use of the Wronskian identity $a(\zeta)b'(\zeta) - a'(\zeta)b(\zeta) = -1/\pi$ to bound the solution amplitude via the denominator $D_1$ is a genuine analytic refinement that eliminates the crude $|\operatorname{Ai}| + |\operatorname{Bi}|$ envelope at the evaluation point.

**Exact assumptions:** A1 assumes that the finite-cutoff Langer framework holds, that the cutoff mismatch $\varepsilon_{\text{tail}}$ can be bounded, and that the crude matrix norm $\Omega_A$ can be integrated to yield a finite variation $\mathcal{V}_A$ that does not destroy the margin gained by $D_1$.

**Claims that are proved:** The algebraic derivation of the scalar denominator $D_1$ and the proof that $D_1 > 0$ are [PROVED]. The lower bound $D_1 \ge \frac{1}{\pi(|a(\zeta_1)| + |b(\zeta_1)|)}$ is mathematically sound. The conditional theorem stating that the sharpened inequality implies the KKT target is [PROVED] as a logical implication.

**Likely false or underspecified claims:** The assumption that the crude matrix norm $\Omega_A$ is sufficient when paired with $D_1$ is [HEURISTIC]. The matrix norm $\Omega_A$ includes the $\operatorname{Bi}(-\zeta)$ component, which grows exponentially in the forbidden zone ($\zeta < 0$). If the integration domain $[\zeta_{\text{cut}}, \zeta_1]$ extends deep into the forbidden zone to minimize $\varepsilon_{\text{tail}}$, the integral $\mathcal{V}_A$ will be dominated by this exponential growth, rendering the sharpening at $\zeta_1$ useless. Furthermore, while the lower bound $D_1 \ge \frac{1}{\pi(|a| + |b|)}$ is correct, substituting this lower bound into the sufficient condition exactly recovers the crude envelope. The sharpening only provides numerical slack if the true value of $D_1$ is strictly larger than its worst-case lower bound, which requires explicit measurement.

**Missing theorem hypotheses:** The theorem requires the instantiation of Olver or DGS weight functions to replace $\Omega_A$. Without these weights, the variation integral is not formally bounded, and the conditional theorem cannot be evaluated numerically.

**Concrete verification tasks:** A4 must numerically measure the ratio $\mathcal{R}_{\text{crit}}$ on the hard grid using both the crude norm and a proxy for the DGS weight to determine if the $D_1$ sharpening provides sufficient slack to overcome the forbidden-zone inflation.

# Agent-by-agent review of A2

**Strongest contribution:** The rigorous asymptotic evaluation of the Langer residual at the $\theta=0$ Laguerre face. A2 correctly identifies that the variation does not vanish as $n \to \infty$ for fixed $\alpha$, but instead converges to a strictly positive $\mathcal{O}(1)$ constant. This definitively refutes any proof architecture relying on a universal $\mathcal{O}(n^{-4/3})$ asymptotic decay limit. Additionally, the derivation of the Prüfer phase buffer distance is a structurally sound obstruction that invalidates the piecewise handoff route.

**Exact assumptions:** A2 assumes that the local linear model for $K_B$ near the turning point accurately captures the dominant contribution to the global variation integral, and that the integration width $\Delta \zeta$ is $\mathcal{O}(1)$ with a weight function roughly equal to 1.

**Claims that are proved:** The local limit $\Psi_\infty(0) \approx \frac{4^{2/3}}{140}\alpha^{-4/3}$ is [PROVED]. I have independently verified the Taylor expansions of $K_\tau, K_{\tau\tau}, K_{\tau\tau\tau}$ at $\tau_0$ and the subsequent algebraic cancellation in the numerator $10\gamma K_{\tau\tau\tau} - 9K_{\tau\tau}^2$. The derivation of the Prüfer phase buffer distance $u - u_0 > \frac{\alpha^{4/3}}{4^{4/3}n}$ is [DERIVED-UNDER-ASSUMPTIONS] based on the local linear model and correctly identifies the geometric tension.

**Likely false or underspecified claims:** The claim that the $\mathcal{O}(1)$ variation "fits safely inside the absolute slack" is [HEURISTIC]. A2 calculates the penalty factor as $\exp(0.045) \approx 1.046$ and compares it to the slack $1.472$. This ignores the actual integration width, the behavior of $\Psi_B(\zeta)$ away from $\zeta=0$, and the DGS weight function $\Omega_{DGS}(\zeta)$, which may amplify the integral significantly. The variation of the Schwarzian derivative over the entire first lobe could easily exceed the local turning-point estimate.

**Missing theorem hypotheses:** A2 needs the exact DGS weight function to evaluate the integral $\int |\Psi_B| \Omega_{DGS} d\zeta$. The local limit alone is insufficient to bound the global variation.

**Concrete verification tasks:** A4 must numerically integrate the weighted variation at $\theta=0$ using the actual DGS weights to test the heuristic slack calculation and determine if a regime split is mandatory.

# Agent-by-agent review of A3

**Strongest contribution:** The comprehensive algebraic audit, particularly the derivation of the Riccati Taylor coefficients and the stable rescaling of the $n=2$ critical cubic. A3 provides the exact rational expressions needed for interval-arithmetic initialization, bypassing the singular endpoint $u=0$.

**Exact assumptions:** A3 assumes that the real-argument Binet formula with Robbins' remainder bounds can be directly applied to the four gamma terms in $\log M_{n,\alpha,B}$ to yield a finite threshold $N_0$ without hidden logarithmic shifts.

**Claims that are proved:** The Riccati Taylor coefficients $v(0), v_u(0), v_{uu}(0)$ are [PROVED]. I have independently verified the differentiation of $p_B v_u + v^2 + K_B = 0$ at $u=0$. The rescaled $n=2$ cubic is [PROVED] to be stable at $\theta=0$. The conclusion that Christoffel-Darboux and Turán identities do not yield sharp upper bounds is [PROVED].

**Likely false or underspecified claims:** A3 states that "The leading $O(z\log z)$ pieces cancel among the four gamma terms, leaving a combination of $O(\log n)$ and $O(1/n)$ terms." This is [LIKELY-FALSE] for the transition strip $\alpha=cn$. In the $\alpha=cn$ regime, the sum of the $z\log z$ terms yields $n \cdot f(c)$, where $f(c)$ is the entropy function. This is an $\mathcal{O}(n)$ term, not an $\mathcal{O}(\log n)$ term. A3 previously proved $f(c) < 0$, which means this $\mathcal{O}(n)$ term provides exponential decay $e^{n f(c)/2}$. If A3 claims the leading pieces "cancel" leaving only $\mathcal{O}(\log n)$, A3 is either implicitly assuming $\alpha = \mathcal{O}(1)$ or making a severe asymptotic error for the $\alpha=cn$ regime. The $\mathcal{O}(n)$ entropy term does not cancel; it evaluates to a negative $\mathcal{O}(n)$ value.

**Missing theorem hypotheses:** A3 needs to explicitly compute the constants in the Binet/Robbins remainders for the specific parameter regimes to establish the finite thresholds, correctly accounting for the $\mathcal{O}(n)$ entropy term in the transition strip.

**Concrete verification tasks:** A4 must execute the Binet/Robbins gamma-enclosure prototype to measure the maximum of $M_{n,\alpha,B}$ and determine the actual thresholds for the $\alpha=cn$ and $\alpha=O(1)$ regimes.

# Claims that look correct

1. **A1's Wronskian-based denominator $D_1$:** The algebraic manipulation using the Airy Wronskian to isolate the solution amplitude at the critical point is exact and mathematically elegant. It correctly exploits the boundary condition $H_\tau(\tau_1) = 0$.
2. **A2's local limit of $\Psi_B(0)$:** The asymptotic derivation of the removable singularity at the Laguerre face is rigorous and correctly identifies the $\alpha^{-4/3}$ scaling. This is a vital diagnostic for the global variation.
3. **A3's Riccati Taylor coefficients:** The exact rational expressions for $v(0), v_u(0), v_{uu}(0)$ are verified and provide a solid foundation for validated ODE integration, completely avoiding the singular Frobenius endpoint.
4. **A3's rescaled $n=2$ cubic:** The transformation $u = Bw$ successfully removes the ill-conditioning at the Laguerre face, making interval root isolation feasible for the compactified parameter $\theta$.

# Claims that need proof

1. **Global weighted variation integral:** The integral $\mathcal{V}_* = \int_{\zeta_{\text{cut}}}^{\zeta_1} |\Psi_B(\xi)| \Omega_*(\xi) d\xi$ must be bounded with explicit constants using the Dunster-Gil-Segura weight functions. The crude matrix norm is insufficient.
2. **Finite-$n$ gamma-ratio envelope:** The regime-split Binet/Robbins bounds must be assembled into explicit numerical thresholds $N_0$ for each scaling regime. The $\mathcal{O}(n)$ entropy term and $\mathcal{O}(\log n)$ terms must be exactly tracked.
3. **Interval certificates for $n=1, 2$:** The outward-rounded interval evaluations of $H_n^4 - T^4$ must be executed and logged. Plans and algebraic setups are no longer sufficient.
4. **Cutoff mismatch $\varepsilon_{\text{tail}}$:** The boundary data discrepancy at $\zeta_{\text{cut}}$ must be rigorously bounded using the Frobenius expansion and interval arithmetic.

# Possible errors or hidden assumptions

1. **A1's crude matrix norm inflation:** A1 assumes that the sharpening at $\zeta_1$ can compensate for the exponential growth of $\operatorname{Bi}(-\zeta)$ in the forbidden zone. If $\zeta_{\text{cut}}$ is too negative, the integral $\mathcal{V}_A$ will blow up, and the conditional theorem will be vacuous. The DGS weights are mandatory to separate the subdominant and dominant solutions.
2. **A2's integration width assumption:** A2 assumes the integration width $\Delta \zeta$ is $\mathcal{O}(1)$ and the weight is roughly 1. However, the mapping from $u$ to $\zeta$ is nonlinear, and the distance to the first critical point $\zeta_1$ may scale differently in certain parameter regimes. The Schwarzian derivative $\Psi_B$ may also have significant contributions away from the turning point.
3. **A3's Binet remainder cancellation:** A3 assumes that the $\mathcal{O}(\log n)$ terms in the Binet expansion can be bounded without tracking the exact coefficients. In reality, the difference $\log(n+\alpha+1) - \log(n+1)$ can produce non-trivial $\mathcal{O}(1)$ shifts that must be explicitly enclosed to compute a valid $N_0$. More critically, the $\alpha=cn$ regime contains an $\mathcal{O}(n)$ entropy term that A3 mischaracterized as canceling.
4. **Riccati positivity interval:** The Riccati equation $p_B v_u + v^2 + K_B = 0$ requires $H \neq 0$. While the forbidden-zone ascent guarantees $H > 0$ up to $u_0$, the interval ODE integration must rigorously prove that $v(u)$ does not blow up (which would correspond to a zero of $H$) before the first critical point.

# Unsupported closure or overclaim audit

- **A2's slack accommodation:** A2 states that the order-one Langer variation "fits safely inside the analytic slack." This is an overclaim. It is a heuristic model based on a local limit and an assumed integration width. It is not a certified inequality. The global variation could easily exceed the slack if the DGS weights amplify the tail.
- **A3's partial certification of gamma envelope:** A3 claims the gamma-ratio envelope is "partially certified." This is slightly optimistic. The asymptotic behavior is certified, but the finite-$n$ envelope remains an open analytic estimate until the remainders are explicitly bounded and $N_0$ is computed. The mischaracterization of the $\alpha=cn$ entropy term further delays this certification.
- **No false closure claims:** None of the agents claim that the KKT conjecture is proved, which is the correct posture for this stage of the research. The focus has correctly shifted to measuring the constants of the conditional theorems.

# Suggested synthesis

The next phase of the proof must integrate A1's sharpened conditional theorem with A2's Laguerre face analysis and A3's algebraic scaffolding. The primary bottleneck is no longer the architectural design of the Langer transformation, but the quantitative measurement of its error parameters.

The synthesis should proceed as follows:
1. Adopt A1's scalar denominator $D_1$ as the standard amplitude bound at the critical point.
2. Replace A1's crude matrix norm $\Omega_A$ with the Dunster-Gil-Segura weight functions to control the forbidden-zone inflation.
3. Use A2's local limit $\Psi_\infty(0)$ to initialize the numerical quadrature of the weighted variation at the Laguerre face.
4. Use A3's Riccati coefficients and rescaled cubics to execute the low-degree interval certificates, bypassing the Langer machinery for $n=1, 2$.

# Research strategy adjustments

The project must pivot from theoretical design to rigorous computational measurement.

- **Track A (Bulk Langer Measurement):** A4 must numerically integrate the DGS weighted variation on the hard grid to test A2's heuristic slack calculation. If the variation exceeds the slack, a regime split at $\alpha_{\text{thresh}}(n)$ is mandatory.
- **Track B (Gamma Envelope):** A3 and A4 must collaborate to execute the Binet/Robbins gamma-enclosure prototype. The goal is to produce explicit numerical thresholds $N_0$ for the $\alpha=cn$ and $\alpha=O(1)$ regimes, correctly accounting for the $\mathcal{O}(n)$ entropy term.
- **Track C (Interval Execution):** A4 must execute the $n=1$ and $n=2$ interval certificates using the corrected target formulas and outward-rounded arithmetic. Failure boxes must be logged and analyzed.

# Symbolic or numerical verification plan

To convert the Round 19 architectural refinements into certified bounds, the following explicit verification plan must be executed using Arb or a similar outward-rounded interval arithmetic library.

### 1. Critical Sharpening and DGS Weight Integration
The crude matrix norm $\Omega_A$ must be replaced by the Dunster-Gil-Segura modulus and weight functions. For the Airy equation $W_{\zeta\zeta} + \zeta W = 0$, the DGS framework defines the modulus $M(\zeta)$ and weight $N(\zeta)$ such that the error-control function is bounded by $\int |\Psi_B(\zeta)| N(\zeta) d\zeta$.
- **Task:** Implement the exact DGS weight function $N(\zeta)$ for the Airy equation in high-precision arithmetic. For $\zeta < 0$, use the exponential weight associated with the subdominant solution $\operatorname{Ai}(-\zeta)$.
- **Task:** Numerically integrate $\mathcal{V}_{DGS} = \int_{\zeta_{\text{cut}}}^{\zeta_1} |\Psi_B(\zeta)| N(\zeta) d\zeta$ at the Laguerre face $\theta=0$ for $\alpha \in \{0.5, 1, 2, 5\}$.
- **Task:** Compute the ratio $\mathcal{R}_{\text{crit}} = \frac{\zeta_\tau(\tau_1)^{-1/2} \mathfrak{C}_A (1+\varepsilon_{\text{tail}}) e^{\mathcal{V}_{DGS}}}{\pi D_1 T_{n,\alpha,\beta}}$. Flag any samples where $\mathcal{R}_{\text{crit}} \ge 1$.

### 2. Binet/Robbins Gamma Enclosure Prototype
The gamma-ratio envelope must be rigorously bounded to compute the finite threshold $N_0$.
- **Task:** Write an Arb-based program to evaluate $\log M_{n,\alpha,B}$ using the exact Binet formula $\log\Gamma(z) = (z-1/2)\log z - z + \frac{1}{2}\log(2\pi) + \mu(z)$.
- **Task:** Enclose the remainder $\mu(z)$ using the strict Robbins bounds $\frac{1}{12z+1} < \mu(z) < \frac{1}{12z}$.
- **Task:** Track the exact $\mathcal{O}(n)$ entropy term $n \cdot f(c)$ for the $\alpha=cn$ regime, and the exact $\mathcal{O}(\log n)$ coefficients from the $(z-1/2)\log z$ terms to prevent hidden $\mathcal{O}(1)$ shifts.
- **Task:** Scan the residual strip for $n \in [1, 1000]$ to find the maximum of $M_{n,\alpha,B}$ and determine the explicit threshold $N_0$ where $M \le 1 + C/(n+1)$ holds.

### 3. Low-Degree Interval Certificates ($n=1, 2$)
The Langer machinery is unnecessary for low degrees. Direct interval evaluation of the critical equations is required.
- **Task ($n=1$):** Use the exact quadratic critical equation $(\alpha+\beta+2)u^2 - [\alpha(B+\alpha+1)+\beta(\alpha+1)+2B]u + \alpha B(\alpha+1) = 0$. Isolate the roots using the discriminant. Evaluate $H_1^4 - T^4$ with outward rounding over $\alpha \in [1/2, \alpha_E(1)]$ and $\theta \in [0, 1]$.
- **Task ($n=2$):** Use the rescaled cubic in $w = u/B$. The coefficients are polynomial in $\theta$ and remain bounded as $\theta \to 0$. Execute interval Newton root isolation over the compactified domain.
- **Task:** Log all unresolved boxes where the KKT margin is not strictly negative. These failure boxes will dictate the required precision or subdivision depth.

### 4. Riccati IVP Validation
- **Task:** Implement the Riccati equation $p_B v_u + v^2 + K_B = 0$ using the certified Taylor coefficients $v(0) = \alpha/2$, $v_u(0) = -\Lambda_B/(\alpha+1)$, and $v_{uu}(0)$.
- **Task:** Initialize the interval ODE solver at $u = \epsilon$ using a Taylor model. Integrate forward to the first zero of $v(u)$ (which corresponds to the first critical point of $H$).
- **Task:** Compare the Riccati zero with the polynomial critical roots for $n=1$ to validate the ODE enclosure.

# Score by agent

| Agent reviewed | Score (0-10) | Main reason | Must verify next |
|---|---:|---|---|
| A1 | 8 | Excellent algebraic sharpening via Wronskian, but relies on crude matrix norm. | Measure $\mathcal{R}_{\text{crit}}$ and test DGS weights. |
| A2 | 7 | Rigorous local limit derivation, but overclaims global slack accommodation. | Integrate weighted variation at $\theta=0$. |
| A3 | 8 | Strong algebraic audit and Riccati derivation; gamma framework has asymptotic mischaracterization. | Execute Binet/Robbins enclosure and compute $N_0$. |

# Next-round recommendation

- **For A1:** Refine the conditional theorem by explicitly instantiating the Dunster-Gil-Segura weight functions. Provide the exact mapping from the KKT residual $\Psi_B$ to the DGS error-control function. Do not rely on the crude $\infty$-norm.
- **For A2:** Convert the heuristic slack calculation into a rigorous numerical bound. Compute the exact integration width $\Delta \zeta$ and the weighted variation integral at the Laguerre face using the DGS weights.
- **For A3:** Finalize the Binet/Robbins gamma-enclosure. Correct the asymptotic description of the $\alpha=cn$ regime to explicitly include the $\mathcal{O}(n)$ entropy term. Produce explicit numerical thresholds $N_0$.
- **For A4:** Execute the verification plan. Produce the numerical tables for $\mathcal{R}_{\text{crit}}$, the DGS weighted variation, the gamma envelope scan, and the $n=1, 2$ interval certificates. Log all failure boxes.

# Confidence

- Confidence in A1's scalar denominator $D_1$ derivation: **0.88**
- Confidence in A2's local limit $\Psi_\infty(0)$: **0.89**
- Confidence in A2's heuristic slack accommodation: **0.45**
- Confidence in A3's Riccati Taylor coefficients: **0.89**
- Confidence in the finite-$n$ gamma-ratio envelope: **0.65**
- Confidence that the global Langer theorem evaluates effectively inside the margin: **0.60**
- Overall confidence that the KKT conjecture is proved at this stage: **0.10**
