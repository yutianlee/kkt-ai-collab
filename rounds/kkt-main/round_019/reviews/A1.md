Summary:

According to `A1_review_19.md`, created 2026-06-09, this is a Round 19 Stage B review of the Stage A outputs from A2, A3, and A4; the packet explicitly asks for a referee report, a score table, hidden assumptions, exact claims needing proof, concrete verification tasks, and research-strategy adjustment.

Round 19 is useful, but it does not close the KKT conjecture. The finite-$B$ endpoint-cap first-lobe route remains the selected architecture. The best new material is:

1. A2’s warning that a monolithic global Langer theorem may fail to have a uniformly vanishing variation bound on the $\theta=0$ Laguerre face.
2. A3’s algebra audit of the Langer residual, removable turning-point value, $\tau$-derivative identities, Liouville normal-form sign, compactified hypergeometric coefficient, low-degree critical equations, and Riccati Taylor data.
3. A4’s low-degree/Riccati interval-certificate scaffolding, especially the corrected $n=1$ boundary-face calculations and Riccati initialization.

The main unresolved theorem remains the finite-$B$ first-lobe amplitude estimate:

$$
|H(u_1)|
\le
T_{n,\alpha,\beta}
=
\left(
\frac{(n+1)B}{(n+\alpha+1)(B-\alpha)}
\right)^{1/4},
$$

where $u_1$ is the first critical point after the first endpoint turning point in the residual cap. No Round 19 agent proves this theorem. A3 is the most reliable mathematical source this round. A2 is valuable for obstruction analysis. A4 is useful for low-degree and interval-certificate planning, but its work is not yet certificate-level because the requested Arb or outward-rounded interval execution was not actually supplied.

Literature check:

- The KKT paper is correctly identified as Koornwinder--Kostenko--Teschl, *Jacobi Polynomials, Bernstein-type Inequalities and Dispersion Estimates for the Discrete Laguerre Operator*, Adv. Math. 333 (2018), 796--821. The arXiv record states that the paper connects Bernstein-type inequalities for Jacobi polynomials with dispersive estimates for generalized Laguerre operators.
- Haagerup--Schlichtkrull’s real-parameter Jacobi inequality is relevant background, but not a sharp KKT closure. Their arXiv abstract states a Bernstein-type inequality uniform in $n\ge0$, real $\alpha,\beta\ge0$, and $x\in[-1,1]$, and the journal reference is Ramanujan J. 33 (2014), 227--246.
- Landau’s Bessel theorem is a valid dependency only where the proof genuinely reduces to a Bessel maximum. The abstract states that $\sup_x |J_\nu(x)|$ strictly decreases from $1$ to $0$ as $\nu$ increases from $0$ to $\infty$; the bibliographic data are L. J. Landau, “Bessel Functions: Monotonicity and Bounds,” J. London Math. Soc. 61(1), 197--215 (2000), DOI 10.1112/S0024610799008352.
- Dunster--Gil--Segura are relevant for computable turning-point Airy errors: their 2020 paper derives computable sharp error bounds for asymptotic expansions of second-order linear ODEs with a simple turning point, involving Airy functions and slowly varying coefficient functions.
- Robbins’ 1955 note gives the strict Stirling remainder for integer factorials,
  $n!=\sqrt{2\pi}n^{n+1/2}e^{-n}e^{r_n}$ with $1/(12n+1)<r_n<1/(12n)$. For the current real-gamma ratios, this is suggestive but not automatically sufficient; the proof needs a real-argument Binet/Stirling remainder theorem or a carefully justified extension.
- Arb is an appropriate platform for interval certification. Johansson describes Arb as a C library for arbitrary-precision midpoint-radius, or ball, interval arithmetic; the Arb documentation cites the IEEE Trans. Computers 66(8), 1281--1292 (2017) paper and DOI.

Most valuable input from others:

### A2

A2’s most valuable input is the $\theta=0$ Laguerre-face obstruction analysis. A2 argues that for fixed $\alpha$ at the Laguerre face, the removable value of the Langer residual at the turning point does not vanish with $n$ but approaches a local scale of order $\alpha^{-4/3}$. The packet records A2’s claim that a proof relying on a universal $\mathcal O(n^{-4/3})$ Langer variation decay is invalid, while also noting that the variation constant may be small enough to fit within available Bessel/KKT slack after correct Airy weighting and interval measurement.

The useful calculation is the Laguerre-face model

$$
K_\infty(u)
=
-\frac{\alpha^2}{4}
+
\Lambda_\infty u
-
\frac{u^2}{4},
\qquad
\Lambda_\infty=n+\frac{\alpha+1}{2},
$$

with turning point

$$
u_0
=
2\Lambda_\infty
-
2\sqrt{\Lambda_\infty^2-\alpha^2/4}
\sim
\frac{\alpha^2}{4\Lambda_\infty}.
$$

At $u_0$, A2 obtains

$$
\gamma=K_\tau(\tau_0)=\frac{\alpha^2-u_0^2}{4},
$$

$$
K_{\tau\tau}(\tau_0)=\frac{\alpha^2-3u_0^2}{4},
$$

$$
K_{\tau\tau\tau}(\tau_0)=\frac{\alpha^2-7u_0^2}{4}.
$$

Substituting these into

$$
\Psi(0)
=
\frac{
10\gamma K_{\tau\tau\tau}(\tau_0)
-
9K_{\tau\tau}(\tau_0)^2
}{
140\gamma^{8/3}
}
$$

gives the fixed-$\alpha$, large-$n$ local limit

$$
\Psi_\infty(0)
\sim
\frac{4^{2/3}}{140}\alpha^{-4/3}.
$$

This is a serious warning against treating the Langer residual variation as automatically small in all regimes. It should be retained as a diagnostic, not yet as a no-go theorem.

A2 also correctly identifies the piecewise Airy-to-Prüfer handoff as structurally delicate: to suppress boundary terms one needs a buffer beyond the immediate linear Airy layer, but pushing the buffer too far degrades the local Airy approximation. This supports the current preference for a global Langer/Airy theorem with proper error weights over a local handoff plus raw Prüfer drift.

### A3

A3 provides the strongest and most reusable mathematics. The Stage A memo states that the algebraic infrastructure is lemma-bank ready while explicitly not proving the finite-$B$ first-lobe amplitude theorem.

The most valuable A3 fragments are:

1. The global Langer residual formula

$$
W_{\zeta\zeta}+\zeta W=\Psi_B(\zeta)W,
$$

with

$$
\Psi_B(\zeta)
=
\frac{\zeta K_{\tau\tau}}{4K^2}
-
\frac{5\zeta K_\tau^2}{16K^3}
+
\frac{5}{16\zeta^2},
\qquad
K(\tau)=K_B(u(\tau)).
$$

The packet also records the removable value

$$
\Psi_B(0)
=
\frac{
10\gamma K_{\tau\tau\tau}(\tau_0)
-
9K_{\tau\tau}(\tau_0)^2
}{
140\gamma^{8/3}
},
\qquad
\gamma=K_\tau(\tau_0)=p_B(u_0)K_B'(u_0).
$$

This is the current central residual formula for any Langer theorem.

2. The correct affine Liouville normal-form sign. With

$$
Y_u=p_B^{1/2}H,
$$

one has

$$
Y_u''
+
\frac{K_B(u)+1/4}{p_B(u)^2}Y_u
=
0,
$$

not a $K_B-1/4$ formula. Therefore the Liouville-normal turning point is $K_B=-1/4$, distinct from the original Sonin/Sturm turning point $K_B=0$.

3. The stable compactified hypergeometric coefficient

$$
\frac{(B)_k}{B^k}
=
\prod_{j=0}^{k-1}
\left(
1+\frac{j\theta}{n+\alpha+1}
\right),
\qquad
\theta=\frac{n+\alpha+1}{B}.
$$

This keeps the $\theta=0$ Laguerre face stable for interval evaluation.

4. Low-degree critical equations. For $n=1$,

$$
P_1^{(\alpha,\beta)}\left(1-\frac{2u}{B}\right)
=
\alpha+1-u,
$$

and the critical equation is

$$
(\alpha+\beta+2)u^2
-
\left[
\alpha(B+\alpha+1)+\beta(\alpha+1)+2B
\right]u
+
\alpha B(\alpha+1)
=
0.
$$

For $n=2$, A3 gives a cubic derived from

$$
(\alpha(B-u)-\beta u)P_2(u)+2u(B-u)P_2'(u)=0,
$$

with coefficients involving $A=(\alpha+1)(\alpha+2)/2$, $b_1=\alpha+2$, and $c_1=(B+1)/(2B)$. The packet flags this as useful for interval work, with rescaling near $\theta=0$ still required.

5. The gamma normalization starting point

$$
\log M_{n,\alpha,B}
=
\frac12[
\log\Gamma(n+\alpha+1)+\log\Gamma(B)
-\log\Gamma(n+1)-\log\Gamma(B-\alpha)]
-\frac{\alpha}{2}\log(B\Lambda_B).
$$

This is the correct object for any Binet/Robbins or interval gamma-envelope theorem.

6. Riccati Taylor data. If

$$
v(u)=p_B(u)\frac{H'(u)}{H(u)},
$$

then

$$
p_Bv_u+v^2+K_B=0.
$$

The packet records the Frobenius initialization

$$
v(0)=\frac{\alpha}{2},
\qquad
v_u(0)=-\frac{\Lambda_B}{\alpha+1},
$$

and A3 derives the next coefficient. This is directly useful for low-degree interval IVP certification.

### A4

A4’s useful contribution is implementation scaffolding and low-degree algebra, not a completed certificate. The packet explicitly says A4 offers scaffolding but no completed interval certificate, with Arb sweeps, gamma scans, Riccati enclosures, and Langer variation tables still unexecuted.

The useful A4 fragments are:

1. The $n=1$ boundary-face computations. A4 corrects earlier algebra for the Laguerre face $\theta=0$ and the finite face $\theta=1$ with $\beta=0$. The finite-face formula for $\beta=0$ and $B=\alpha+2$ is

$$
u_1=\frac{\alpha(\alpha+1)}{\alpha+2},
$$

and

$$
H_1(u_1)^4
=
\left(
\frac{\alpha(\alpha+1)}{(\alpha+2)^2}
\right)^{2\alpha}
\frac{16(\alpha+1)^4}{(\alpha+2)^4}.
$$

At $\alpha=1/2$, this gives $0.248832<T^4=1$. This is useful evidence and likely a small lemma for the $\theta=1$ boundary, but it is not a full $n=1$ certificate over $\theta\in[0,1]$.

2. The Riccati IVP route. A4 argues that validated integration of

$$
p_Bv_u+v^2+K_B=0
$$

from Frobenius data can bypass turning-point Airy complications for low degrees. This is a promising computational path for $n=1,2$, provided it is executed with outward rounding and root-isolation logs.

3. The correction that $\beta=0$ is not the Laguerre face. With

$$
\theta=\frac{n+\alpha+1}{B},
$$

the face $\theta=0$ is the Laguerre face $B\to\infty$, while $\theta=1$ is the finite face $\beta=0$. The packet specifically warns that this terminology must be corrected.

Claims that look correct:

1. The endpoint-cap first-lobe reduction remains the right skeleton. The residual cap is still

$$
0\le u\le u_\sigma=\frac{nB}{B+n-1}\le n,
$$

and the project should keep the first-lobe amplitude theorem as the single analytic target.

2. A2 is probably right that a proof based on a uniformly vanishing Langer variation over the whole residual strip is too optimistic. The local residual value at $\theta=0$ and fixed $\alpha$ does not vanish with $n$ in the naive local model. This does not kill the Langer route, but it forces a regime split and a measured, weighted variation theorem.

3. A3’s Langer residual formula, removable turning-point value, $\tau$-derivative chain, Liouville sign $K_B+1/4$, compactified hypergeometric coefficient, and low-degree critical equations all look mathematically coherent. The Langer residual and removable value still deserve a final CAS/Taylor audit before being promoted as certified.

4. A3’s warning that Christoffel--Darboux and Turán identities do not immediately produce a sharp first-critical-point upper bound looks correct. The available identities are not aligned with the exact weighted Jacobi normalization and moving first critical point.

5. A4’s corrected $n=1$ boundary computations appear numerically consistent. My check gives approximately $0.1702$ for the Laguerre-face fourth-power value at $\alpha=1/2$ and $0.248832$ for the $\beta=0$ finite face at $\alpha=1/2$. The exact values are far below their respective targets, so the margin is large. A4’s $0.1715$ is slightly inaccurate but harmless.

6. The semi-discrete contiguous-relation route should remain exploratory only. The moving critical point and normalization factors make a contractive induction in $\beta$ unlikely without new sign structure.

Claims that need proof:

1. **A2’s “order-one variation barrier” needs a global weighted variation theorem or countertheorem.** A2 derives a local removable value at $\zeta=0$, but the actual proof-relevant object is not simply $\Psi(0)$. It is a weighted Dunster--Gil--Segura/Olver variation integral over a finite interval, with the correct modulus and weight functions and with Frobenius/recessive initial data. A2 has not proved a lower bound for that weighted integral.

2. **The “Bessel slack absorbs the variation” claim needs all constants.** The rough slack

$$
1/0.67919\approx1.47
$$

ignores matching constants, gamma normalization, $\zeta_\tau(\tau_1)^{-1/2}$, Airy modulus factors, tail mismatch, and finite-$B$ corrections. This slack is real but cannot be used until all multiplicative factors are placed in one inequality.

3. **The Dunster--Gil--Segura theorem must be instantiated.** It is not enough to cite sharp Airy error bounds. The KKT residual $\Psi_B(\zeta)$ must be shown to satisfy the needed regularity and bounded-variation hypotheses on the chosen interval, including a removable singularity at $\zeta=0$, and the resulting constants must be explicitly computed.

4. **The Langer residual formula and removable value need one final independent symbolic check.** A3’s formula is highly plausible, but because all later analytic estimates depend on cancellations of singular terms, it should be verified by a CAS Taylor expansion from the defining relation $K=\zeta\zeta_\tau^2$.

5. **A3’s endpoint ODE derivation should be rewritten cleanly.** The final $p_B,q_B,K_B$ formulas are correct, but A3’s displayed derivation contains a chain-rule factor inconsistency: the transformation gives

$$
\frac{d}{dx}\left((1-x^2)g_x\right)
=
B(p_BH')',
$$

not a $B/2$ factor. The lemma-bank proof must use the correct chain-rule factor even if A3’s final $q_B$ is right.

6. **The degree-two cubic must be independently checked.** A3’s cubic is very likely correct, but before A4 uses it for interval certification it should be compared against direct differentiation of the compactified polynomial and tested at $\theta=0$ and $\theta=1$.

7. **The gamma-ratio envelope remains open.** A3’s leading entropy negativity for $\alpha=cn$ supports decay in the deep transition regime, but finite-$n$ Binet/Robbins remainder bounds have not been assembled. Robbins’ original statement is for $n!$; for real gamma arguments one needs a real-argument Binet or Stirling remainder inequality with carefully signed upper envelopes.

8. **A4’s $n=1$ boundary-face closure needs exact monotonicity or interval proof.** Sampling and endpoint numerics do not prove the maximum over $\alpha\in[1/2,6/5]$ unless the derivative in $\alpha$ or an interval enclosure is supplied. The large margins make this easy, but it still must be done.

9. **A4’s interval certificate has not been executed.** A4 gives pseudo-code and a plan, but no code version, no library version, no outward-rounded boxes, no root-isolation certificates, no accepted/unresolved box logs, and no raw interval margins. The Round 19 prompt explicitly asked A4 for execution rather than pseudo-code; that standard was not met.

10. **The Riccati IVP route needs positivity and crossing certification.** Integrating $v=p_BH'/H$ requires ensuring $H$ does not vanish before the first critical point, giving a rigorous Taylor remainder at the starting point, and proving that the first zero of $v$ corresponds exactly to $u_1$ with no missed critical points.

Possible errors or hidden assumptions:

### A2

- A2 upgrades a local turning-point residual asymptotic into a “variation barrier” too quickly. The local value $\Psi(0)$ is not the same as the weighted variation used in a sharp Airy error theorem.
- The heuristic threshold $\alpha_{\mathrm{thresh}}(n)=\Theta(n^{3/5})$ is not proved. It should be treated as a regime-design hypothesis, not as a theorem.
- The slack comparison to $J_{1/2}$ is incomplete until the Airy/Langer normalization is connected to the Bessel or Airy envelope used in the actual finite-cutoff theorem.
- The statement that the external theorem hypotheses were “explicitly checked” is overstrong. They were named and discussed, but not instantiated with a full KKT residual verification.

### A3

- The final ODE formulas are credible, but the displayed derivation’s chain-rule coefficient needs correction before lemma-bank insertion.
- The Langer residual formula is given in the $\tau$ convention. Any alternate $u$-form should be rejected unless it is derived by chain rule from the $\tau$ formula.
- The notation for Riccati Taylor data must distinguish coefficients from derivatives. A4 writes $v_2$ as the coefficient of $u^2$, while A3 writes $v_{uu}(0)$; these differ by a factor of $2$.
- The claim $M\le1+C/(n+1)$ for fixed $\alpha$ with a sample constant such as $C=2$ is not yet proved.
- The entropy proof $f(c)<0$ is valuable but does not control the mesoscopic regime $\alpha=C\sqrt n$ or small fixed $\alpha$.

### A4

- A4 labels some claims as [PROVED] that are only analytically reduced or numerically sampled. Boundary-face calculations may be exact pointwise formulas, but global $\alpha$-range closure still needs monotonicity or interval proof.
- A4’s $\theta=0$ and $\theta=1$ terminology is mostly corrected, but the text still calls the $n=1,\beta=0$ calculation “finite face”; this is correct, not Laguerre. This distinction must remain strict.
- The “Olver weights resolve the barrier” claim is plausible, but A4 has not produced the weighted variation integral or a bound proving it.
- The Langer diagnostic table is explicitly heuristic, not interval-enclosed. It must not be cited as numerical evidence in the proof state except as a planning diagnostic.
- The pseudo-code is not a certificate. A valid certificate requires exact reproducibility data and archived interval outputs.
- The contiguous relation quoted for $\beta$-shifts should not be used until the exact identity is verified; even if correct, sign-regularity at moving extrema is absent.

Suggested synthesis:

The next synthesis should preserve the endpoint-cap first-lobe architecture and split the remaining work into three disciplined tracks.

### Track 1: Algebra finalization

A3 should be treated as the algebra authority, but one more cleanup pass is required. The lemma bank should accept only formulas that survive a clean derivation or CAS verification:

- endpoint ODE with correct chain-rule factor;
- Langer residual formula;
- removable turning-point value;
- $\tau$-derivative identities;
- Liouville sign $K_B+1/4$;
- compactified hypergeometric coefficient;
- $n=1$ quadratic and $n=2$ cubic;
- Riccati Taylor initialization with coefficient/derivative notation fixed.

### Track 2: Regime-split analytic theorem

Do not attempt a monolithic Langer theorem over the full residual strip. The likely split is:

1. **Bulk Langer/Airy regime:** $\alpha\ge C\sqrt n$ or a similar threshold, to be chosen after measuring $\mathcal V_A$, $\mathfrak C_A/T$, and $M_{n,\alpha,B}$.
2. **Small-$\alpha$ or near-Laguerre-face regime:** use Frobenius/Bessel/Volterra or Riccati methods rather than relying on a decaying Langer variation.
3. **Low-degree regime:** execute interval certificates for $n=1,2$, then extend to $n<N_0$ after a large-$n$ theorem exists.

A2’s $\theta=0$ warning should force this split unless a weighted Dunster--Gil--Segura theorem proves that the apparent barrier is removed by proper Airy weights.

### Track 3: Certified computation

A4 should stop producing pseudo-code and produce actual artifacts. The minimum acceptable next deliverable is:

- exact source code or notebook;
- library and version;
- precision;
- interval representation;
- subdivision policy;
- root-isolation criterion;
- raw accepted boxes;
- unresolved boxes;
- interval margins for $H_n(u)^4-T^4$;
- boundary-face logs for $\theta=0$ and $\theta=1$.

Until those are supplied, A4’s work remains a plan.

Research strategy adjustments:

1. **Continue the main route, but split it.** Keep endpoint-cap first-lobe reduction as the proof skeleton. Treat global finite-cutoff Langer as the bulk route, not the universal route.

2. **Prioritize constants over architecture.** The project has enough normal forms. The next round should measure or bound $\mathcal V_A$, $\varepsilon_{\mathrm{tail}}$, $\mathfrak C_A/T$, $M_{n,\alpha,B}$, and $H_n(u_1)^4-T^4$ on low-degree boxes.

3. **Require theorem instantiation.** External references cannot be cited generically. For Landau, specify exactly where the proof reduces to a Bessel maximum. For Dunster--Gil--Segura, state the exact error theorem and map $\Psi_B$ to its hypotheses. For gamma ratios, use a real-gamma Binet theorem rather than only the integer Robbins factorial inequality.

4. **Do not promote computations without outward rounding.** High-precision floating tables are useful diagnostics only.

5. **Make A4 execute.** A4’s next deliverable should be rejected if it lacks actual interval output.

6. **Add a separate gamma-ratio track.** The analytic amplitude theorem cannot close without a one-sided envelope for $M_{n,\alpha,B}$ or the corresponding Langer/Airy normalization constant.

7. **Semi-discrete route remains secondary.** Because the application only needs $\beta\in\mathbb N_0$, keep testing it, but do not pivot there unless a real monotonicity or positivity mechanism is found.

Score by agent:

| Agent reviewed | Score (0-10) | Main reason | Must verify next |
|---|---:|---|---|
| A2 | 7.4 | Strong obstruction analysis on the $\theta=0$ Laguerre face and useful warning against uniform Langer decay; however, local residual scaling is not yet a proof of global weighted variation obstruction, and the slack argument omits several normalization constants. | Prove or refute, with Dunster--Gil--Segura/Olver weights, a rigorous upper or lower bound for the actual variation integral on the $\theta=0$ face; make the $\alpha_{\mathrm{thresh}}(n)$ split quantitative or withdraw it. |
| A3 | 8.6 | Best algebra source. Provides the residual formula, removable value, derivative identities, Liouville sign, compactified coefficient, low-degree critical equations, Riccati data, and gamma-ratio starting point; correctly does not claim an amplitude theorem. | Repair the endpoint ODE chain-rule line, CAS-verify the Langer removable-value cancellation, independently verify the $n=2$ cubic, and turn the gamma-ratio entropy into a finite-$n$ Binet theorem in at least one regime. |
| A4 | 6.4 | Useful low-degree formulas, corrected $n=1$ boundary-face calculations, Riccati IVP idea, and implementation blueprint; but the requested interval execution was not supplied, and several “proved” labels are too strong. | Execute the $n=1$ and $n=2$ interval prototypes with outward rounding, source/version/precision details, root-isolation logs, accepted boxes, unresolved boxes, and raw interval margins. |

Next-round recommendation:

### For A1

Write a Stage C synthesis that preserves only one proof skeleton:

$$
\text{endpoint cap}
\to
\text{first turning point}
\to
\text{first critical point}
\to
\text{finite-cutoff Langer/Riccati certificate}.
$$

State the remaining first-lobe theorem exactly. Define the constants

$$
\mathfrak C_A,\qquad
\varepsilon_{\mathrm{tail}},\qquad
\mathcal V_A,\qquad
M_{n,\alpha,B},
$$

and state a single sufficient inequality of the form

$$
\text{Airy/Langer envelope}
\times
\text{gamma or Frobenius normalization}
\times
\exp(\mathcal V_A)
\le
T_{n,\alpha,\beta}.
$$

Add a regime split explicitly: bulk Langer, small-$\alpha$ Frobenius/Bessel/Riccati, and low-degree intervals. Do not state that KKT is solved.

### For A2

Turn the $\theta=0$ warning into one of two outputs:

1. a rigorous lower bound showing that the properly weighted Langer variation cannot be small in a specified scaling; or
2. a corrected theorem showing that Olver/Dunster--Gil--Segura weights remove the apparent obstruction.

Use the exact residual

$$
\Psi_B(\zeta)
=
\frac{\zeta K_{\tau\tau}}{4K^2}
-
\frac{5\zeta K_\tau^2}{16K^3}
+
\frac{5}{16\zeta^2}.
$$

Your deliverable should be an inequality for an actual integral, not only a local $\Psi(0)$ asymptotic. Also state a falsification test: a parameter grid or limiting scaling where the proposed bound would fail.

### For A3

Produce lemma-bank-ready algebra, with no false-start chain-rule factors. Required items:

1. exact endpoint ODE with correct transformation;
2. exact Langer residual and removable value, CAS-verified;
3. exact $\tau$-derivative chain;
4. affine and rational Liouville normal forms with $K_B+1/4$;
5. compactified hypergeometric formula;
6. $n=1$ quadratic and $n=2$ cubic, independently verified;
7. Riccati Taylor data with coefficient/derivative notation standardized;
8. a real-gamma Binet theorem attempt for $\log M_{n,\alpha,B}$, including at least one fully explicit finite-$n$ subregime.

### For A4

Execute, do not plan. The next A4 output should contain:

1. a complete outward-rounded interval certificate for $n=1$ over $\alpha\in[1/2,6/5]$, $\theta\in[0,1]$, or a list of unresolved boxes;
2. an $n=2$ prototype using A3’s rescaled cubic, again with unresolved boxes if any;
3. source code or notebook, library versions, precision, interval type, root isolation method, subdivision policy, and raw interval margins;
4. a Riccati IVP prototype for $n=1$ initialized from A3’s Taylor data, compared with the quadratic critical root;
5. a clear separation between certified interval results and high-precision diagnostics.

Overall next-round decision:

Round 20 should be an execution-and-constant-measurement round. The panel should not add new proof architectures unless they replace one of the concrete missing constants. The first deliverable that would materially advance the proof is either:

1. a verified finite-cutoff Langer theorem with instantiated Dunster--Gil--Segura weights and gamma normalization bounds; or
2. a successful outward-rounded $n=1,2$ Riccati/critical-point interval certificate with reproducible logs.

Confidence:

- Confidence that Round 19 improves the obstruction map: 0.80.
- Confidence that A3’s algebra, after minor cleanup, should be promoted to the lemma bank: 0.86.
- Confidence that A2’s $\theta=0$ warning forces a genuine regime split: 0.70.
- Confidence that A4’s current output is already a certificate: 0.10.
- Confidence that the full real-parameter KKT conjecture is proved after Round 19: 0.08.
- Confidence that the endpoint-cap first-lobe route remains the best current strategy: 0.82.


