## Summary

This Stage B cross-review evaluates the Round 19 Stage A outputs from active agents A1, A3, and A4 regarding the real-parameter KKT endpoint-cap conjecture. The round yields highly sophisticated structural diagnostics, an essential refutation of a proposed amplitude shortcut, and rigorous exact-arithmetic boundary closures, although it does not produce a final proof of the finite-$B$ first-lobe amplitude theorem.

The most pivotal structural insight emerges from the tension between A1’s elegant algebraic derivation of a scalar Airy denominator and A4’s rigorous structural critique of the unweighted variation norm. A1 correctly establishes an exact scalar identity at the first critical point, proving that the amplitude is inversely proportional to a specific Wronskian-derived denominator $D_1$. However, A4 demonstrates that this algebraic sharpening is analytically insufficient if combined with a crude $\infty$-norm, because unweighted norms inherently mix the exponentially growing $\operatorname{Bi}$ component into the recessive Frobenius initial data during the path integral from the forbidden zone. The correct resolution requires transitioning fully to Olver or Dunster-Gil-Segura weight functions to strictly enforce the physical boundary condition and properly capture the asymptotic decay.

Simultaneously, A3 and A4 have independently derived and cross-verified the Taylor coefficients for the exact Riccati initial value problem up to the third order $v_3$. This mathematically bypasses the Langer turning-point singularity entirely for low degrees, creating a robust interval-arithmetic pathway that can be initialized at $u=\epsilon$. A4 additionally provides exact analytic closures for the degree-one boundary faces, showing large KKT margins at both the Laguerre ($\theta=0$) and finite ($\theta=1$) limits, though I note a manual arithmetic error in A4's Laguerre face evaluation that must be corrected. A3 successfully executes an exact rational-arithmetic spot-check of the removable singularity limit $\Psi_B(0)$, providing useful support for the symbolic foundation. The trajectory is highly positive, but the remaining gap requires executing the Riccati interval prototype and explicitly bounding the Olver-weighted Langer variation for the bulk large-degree regime.

## Assumptions and scope

The analysis assumes the previously verified global modules: central branch-safe contour clearance, weighted-energy bulk clearance, small endpoint exponent estimates for $0 \le \alpha \le 1/2$, and left-right symmetry. We strictly restrict attention to the residual right-endpoint strip defined by $n \ge 1$, $1/2 < \alpha < \alpha_E(n) = \frac{(2n+1)(n+1)}{2n+3}$, and $\beta \ge 0$.

The problem is constrained to the endpoint cap variable $u = \frac{B(1-x)}{2}$ where $B = n+\alpha+\beta+1$. After central clearance, the cap domain is $0 \le u \le u_\sigma = \frac{nB}{B+n-1} \le n$. The endpoint-normalized function $H(u)$ satisfies the exact self-adjoint ODE $(p_B H')' + q_B H = 0$, where $p_B(u) = u(1-u/B)$ and $q_B(u) = c_B - \frac{(r_B u - \alpha)^2}{4u(1-u/B)}$. The Sonin frequency product $K_B(u) = p_B(u)q_B(u)$ is a concave quadratic. The monotonicity $K_B'(u) \ge 1/4$ on the cap ensures that the maximum amplitude after the first turning point $u_0$ occurs at the first critical point $u_1$. The target margin to prove at this point is $|H(u_1)|^4 \le T_{n,\alpha,\beta}^4 = \frac{(n+1)B}{(n+\alpha+1)(B-\alpha)}$.

Two distinct analytic tracks define the scope of evaluation:

1. The global Langer dynamic oscillator $H_{\tau\tau} + K_B(u(\tau))H = 0$ in the variable $\tau = \log \frac{u}{B-u}$, mapped to the perturbed Airy equation $W_{\zeta\zeta} + \zeta W = \Psi_B(\zeta) W$.
2. The exact Riccati differential equation $p_B v_u + v^2 + K_B = 0$ for the logarithmic derivative $v(u) = p_B(u) H'(u)/H(u)$, initialized at the regular singular point $u=0$.

## Claim ledger

1. **A1: Scalar Airy Denominator at Critical Point:** Claims that the condition $H_\tau(\tau_1)=0$ yields an exact algebraic identity reducing the terminal amplitude to an inverse dependence on $D_1 = \max(|a' - d_1 a|, |b' - d_1 b|)$. Status: Verified as an exact algebraic identity, but diagnostically insufficient to rescue the global bound if computed with an unweighted variation matrix norm.
2. **A4: Refutation of Unweighted Matrix Norms:** Claims that an unweighted $\infty$-norm variation bound fails to decay because it misidentifies the exponential smallness of the $\operatorname{Bi}$ coefficient at the forbidden-zone matching boundary, leading to an artificial integration blow-up. Status: Verified as a correct structural mathematical obstruction.
3. **A4: Analytic Closure of $n=1$ Boundary Faces:** Claims exact analytic closure of the $\theta=0$ (Laguerre) and $\theta=1$ (finite $\beta=0$) faces for degree one, showing maximum amplitudes strictly below the KKT target. Status: Verified structurally, though arithmetically downgraded due to a manual error substituting $\alpha+1$ for $\Gamma(\alpha+2)$ in the normalization on the Laguerre face (corrected in this review).
4. **A3/A4: Exact Riccati Taylor Initialization:** Independently claim the exact derivation of the second and third-order Taylor coefficients ($v_{uu}(0)$ and $v_3$) for the Riccati variable $v(u)$. Status: Verified and exactly cross-checked.
5. **A3: Stable Rescaled Cubic for degree two:** Claims a stable rescaling of the degree-two critical cubic using $u=Bw$ to prevent ill-conditioning at the $\theta=0$ Laguerre limit. Status: Verified algebraically.
6. **A3: Removable Langer Turning-Point Value:** Claims the exact rational arithmetic evaluation of the $\Psi_B(\zeta)$ singularity at $\zeta=0$ for a test sample $(10, 5, 0)$, yielding a stable, small negative value. Status: Verified algebraically.
7. **A3: Gamma Entropy Negativity:** Claims the leading Stirling entropy function $f(c)$ is strictly negative on $(0,1]$, governing the exponential decay of the gamma ratio for $\alpha = c n$. Status: Verified as a leading asymptotic truth, explicitly awaiting finite-$n$ Binet remainders to become a uniform theorem.

## Theorem-dependency audit

1. **Dunster-Gil-Segura Airy Error Bounds (2020):** "Sharp error bounds for turning point expansions." Invoked by A1 and A4 to control the global Langer residual variation. This theorem provides the exact weight functions $E(\zeta)$ and $M(\zeta)$ required to track recessive solutions as $\zeta \to -\infty$, exponentially suppressing the artificial $\operatorname{Bi}(-\zeta)$ growth. This is a relevant theoretical dependency but must be explicitly instantiated with the exact KKT residual $\Psi_B(\zeta)$.
2. **Landau Monotonicity (2000):** "Bessel Functions: Monotonicity and Bounds," Journal of the London Mathematical Society 61(1), 197-215. The theorem states that $\sup_{t \ge 0} |J_\nu(t)|$ is strictly decreasing with respect to order for $\nu \ge 0$. Retained by A4 as a valid fallback dependency if the Bessel perturbation route is required for the small-$\alpha$ regime.
3. **Binet and Robbins Gamma Inequalities (1955):** Invoked by A3 to control the finite-$n$ gamma envelope. The strict factorial remainder bounds $\frac{1}{12z+1} < \mu(z) < \frac{1}{12z}$ for real positive $z$ are standard, literature-backed dependencies applicable to the positive parameters in this domain once the real-gamma setup is stated carefully.
4. **Arb Interval Arithmetic Platform:** Proposed by A4. Johansson's library provides arbitrary-precision midpoint-radius interval arithmetic. This is a relevant methodological dependency, but unexecuted C-API blueprints and untested Riccati integration protocols do not constitute a mathematical certificate until logged interval output is generated.

## Most valuable input from others

The most mathematically useful input emerges from the collision of A1's elegant algebraic extraction and A4's structural path-integral critique. A1 isolates the first critical-point condition $H_\tau(\tau_1)=0$ and utilizes the Airy Wronskian $a b' - a' b = -1/\pi$ to prove the peak amplitude is strictly proportional to $\|Z(\zeta_1)\|_\infty / (\pi D_1)$. This cleanly isolates the linear constraint imposed by the extremum.

However, A4's insight that this identity cannot be usefully combined with a crude $\infty$-norm variation bound is equally vital. A4 correctly identifies that the physical Frobenius solution is strictly recessive in the forbidden zone. An unweighted norm bounds the absolute sum of the coefficients, obliterating this recessive orientation and heavily inflating the error integral by factors scaling with $\exp(\frac{4}{3}|\zeta|^{3/2})$. This realization saves the panel from pursuing a mathematically obstructed unweighted integration and firmly dictates the adoption of Olver/DGS weight functions for the large-$n$ variation track. The endpoint scalar division $1/(\pi D_1)$ is $\mathcal{O}(1)$ and cannot rescue a bound that was exponentially destroyed earlier in the integral path.

Additionally, A3's execution of the exact rational-arithmetic diagnostic for $\Psi_B(0)$ is an immense stabilizing input. By proving that the removable turning-point formula yields $\approx -0.0016$ for the $(n=10, \alpha=5, \beta=0)$ parameters, A3 provides concrete verification that the Langer residual equations are structurally sound and computationally benign at the zero-crossing.

## Agent-by-agent review of A1

A1 presents a highly sophisticated conditional certificate relying on the boundary condition $H_\tau(\tau_1)=0$ at the first critical point. A1 deduces the scalar relation $W_\zeta(\zeta_1) = d_1 W(\zeta_1)$, where $d_1 = \zeta_{\tau\tau}(\tau_1) / (2 \zeta_\tau(\tau_1)^2)$.

A1 leverages the Airy matrix $\mathsf A(\zeta)$ to relate the solution vector $(W, W_\zeta)^T$ to the basis coefficients $(A_1, B_1)^T$. The critical point condition implies a linear constraint: $A_1(a'-d_1a) + B_1(b'-d_1b) = 0$. By parameterizing the nullspace with $A_1 = C(b'-d_1b)$ and $B_1 = -C(a'-d_1a)$, A1 demonstrates that the amplitude $W(\zeta_1)$ evaluates directly to $C(ab'-a'b) = -C/\pi$. Thus $|C| = \pi |W(\zeta_1)|$. Evaluating the $\infty$-norm of the coefficient vector yields $\|Z(\zeta_1)\|_\infty = |C| \max(|b'-d_1b|, |a'-d_1a|) = \pi |W| D_1$. This is a sound algebraic deduction proving $|W(\zeta_1)| \le \|Z(\zeta_1)\|_\infty / (\pi D_1)$.

However, as an analytic tool to close the KKT target, A1's formulation contains a severe structural inefficiency regarding the path integral. The unweighted variation integral $\mathcal{V}_A$ integrates $|\Psi_B(\xi)| \Omega_A(\xi)$, where $\Omega_A$ is the $\infty$-norm of the inverse Airy matrix. In the forbidden zone ($\zeta < 0$), the inverse matrix $\mathsf A(\zeta)^{-1} = -\pi \begin{pmatrix} b' & -b \\ -a' & a \end{pmatrix}$ is dominated by the elements containing $b(\zeta) = \operatorname{Bi}(-\zeta)$, which grow exponentially. Integrating this crude norm from a cutoff $\zeta_{\text{cut}} < 0$ guarantees that $\mathcal{V}_A$ will be severely inflated. Dividing the final terminal result by $\pi D_1$ provides absolutely no rigorous constraint against this exponential explosion. A1's algebra is excellent, but its proposed application to the Gronwall inequality must be upgraded to a weighted norm space to survive the forbidden-zone path.

## Agent-by-agent review of A3

A3 provides strong algebraic verification and diagnostic arithmetic, acting as the bedrock auditor for the panel. A3 re-derives the exact dynamic oscillator $H_{\tau\tau} + K_B H = 0$ and confirms that no artificial Schwarzian term appears. A3 settles the Liouville normal form sign convention. Using $Y_u = p_B^{1/2}H$, A3 computes the potential correction $-\frac{p_B''}{2p_B} + \frac{(p_B')^2}{4p_B^2}$ exactly to $\frac{1}{4u^2(1-u/B)^2}$, establishing the normal form as $Y_u'' + \frac{K_B(u)+1/4}{p_B(u)^2}Y_u = 0$ and resolving the prior ambiguity for this convention.

Crucially, A3 tests the removable singularity limit $\Psi_B(0)$ using the exact parameters $(10, 5, 0)$. A3's manual derivation is internally consistent: computing $B=16$, $u_0 = 16/27$, $p_B(u_0) = 416/729$, and $K_B'(u_0) = 325/32$, yielding $\gamma = 4225/729$. A3 evaluates the higher-order $\tau$-derivatives $K_{\tau\tau}$ and $K_{\tau\tau\tau}$ as exact rationals and substitutes them into the formula $\Psi_B(0) = \frac{10\gamma K_{\tau\tau\tau}(\tau_0) - 9K_{\tau\tau}(\tau_0)^2}{140\gamma^{8/3}}$. The result is a stable, small negative value $\approx -0.0016$. This derivation verifies the A1/A2 removable value formula and ensures the global Langer mapping is well-behaved near the turning point.

A3 also undertakes the Riccati Taylor expansion. Defining $v(u) = p_B(u) H'(u) / H(u)$ and differentiating $p_B v_u + v^2 + K_B = 0$ twice, A3 rigorously extracts $v_{uu}(0) = \frac{2\Delta_B - 2\Lambda_B^2/(\alpha+1)^2 - 2\Lambda_B/(B(\alpha+1))}{2+\alpha}$. This precisely establishes the $O(u^2)$ coefficient. Finally, A3's proof that the Gamma-ratio entropy exponent $f(c) = (1+c)\log(1+c) - c - \frac{c}{2}\log(1+c+c^2/2)$ is strictly negative on $(0,1]$ is analytically correct, setting the foundational baseline for the mesoscopic bounding track.

## Agent-by-agent review of A4

A4 focuses on low-degree analytic boundaries, explicit structural critique, and exact Riccati coefficients.

A4 provides a remarkably effective refutation of A1's unweighted norm approach. A4 emphasizes that at the forbidden-zone matching boundary, the ratio of Airy coefficients $c_2/c_1$ must be exponentially small to respect the recessive Frobenius initialization and prevent unphysical blow-up. The unweighted $\infty$-norm bounds the absolute sum of these coefficients, entirely discarding this physical skew and resulting in an artificial barrier at the Laguerre face ($\theta=0$). A4 correctly directs the workflow toward Olver's weight functions $E(\zeta)$ and $M(\zeta)$, which selectively suppress the forbidden-zone growth.

Furthermore, A4 executes exact analytic closures for the degree-one boundary faces. For the Laguerre face ($\theta=0$), A4 correctly sets up the normalized polynomial $\ell_1^{(\alpha)}(u)$, finds the physical root $u_1 = \frac{2\alpha+3 - \sqrt{8\alpha+9}}{2}$, and evaluates the maximal fourth-power amplitude. However, A4 commits a manual error by writing the normalization factor as $\frac{1}{\sqrt{\alpha+1}}$ rather than $\frac{1}{\sqrt{\Gamma(\alpha+2)}}$. Consequently, A4 evaluated $1/\sqrt{1.5} \approx 0.8165$ when it should have evaluated $1/\sqrt{\Gamma(2.5)} = 1/\sqrt{1.32934} \approx 0.867$. The true fourth power evaluated is approximately $0.218$, rather than A4's $0.1715$. Fortunately, $0.218$ is still strictly bounded below the target $T^4 = 0.8$. On the finite face ($\theta=1, \beta=0$), A4 correctly evaluates $H_1(u) = (u/B)^{\alpha/2}(\alpha+1-u)$, finds $u_1 = \frac{\alpha(\alpha+1)}{\alpha+2}$, and establishes the amplitude maximum at $\alpha=0.5$ as $0.2488$, strictly bounded by $T^4=1.0$.

Finally, A4 derives the third-order Riccati Taylor coefficient $v_3$. Matching the expansions of $p_B v_u + v^2 + K_B = 0$ to $O(u^3)$, A4 successfully isolates $v_3 = \frac{2v_2}{\alpha+3}(\frac{1}{B} + \frac{\Lambda_B}{\alpha+1})$. This matches A3's calculation and provides the precise initial truncation error needed to jump-start an interval ODE solver.

## Claims that look correct

1. **A1's Scalar Wronskian Identity**: The deduction that the terminal evaluation point limits the coefficient amplitude to $|W(\zeta_1)| \le \|Z(\zeta_1)\|_\infty / (\pi D_1)$ is an exact algebraic consequence of the linear constraint $H_\tau(\tau_1)=0$.
2. **A4's Structural Critique of Unweighted Norms**: The identification that integrating the inverse matrix $\mathsf{A}(\zeta)^{-1}$ over the forbidden zone artificially injects the exponential $\operatorname{Bi}$ growth into the error bounds is mathematically precise and verified.
3. **A3's Rational $\Psi_B(0)$ Diagnostic**: The manual arithmetic verification of the turning-point limit for $n=10$ provides useful support for the stability of the Langer residual equations.
4. **A4's Finite Face ($\theta=1$) Closure**: The evaluation on the $\theta=1$ finite face for degree one explicitly proves the required KKT margins at the low-degree edge.
5. **A3/A4's Exact Riccati Initialization**: The combined derivations of $v_1, v_2, v_3$ are exact and create a stable, non-singular initialization method for the interval IVP.
6. **A3's Gamma Entropy Asymptotics**: The demonstration that $f(c) < 0$ on $(0,1]$ via Taylor expansion is analytically sound and controls the leading order decay for $\alpha = cn$.

## Claims that need proof

1. **Dunster-Gil-Segura Weighted Variation Integral**: While the necessity of the DGS weight functions is accepted, the specific functional form of $M(\zeta)^2 |\Psi_B(\zeta)|$ must be symbolically bounded to prove theoretical decay at large $n$.
2. **Finite-$n$ Binet/Robbins Remainders**: The asymptotic negativity of $f(c)$ must be converted into a rigorous upper bound $M_{n,\alpha,B} \le 1 + C/(n+1)$ by directly summing the strict $\mu(z)$ inequalities across all four gamma arguments.
3. **Interior $\theta$ Interval Executions for $n=1,2$**: The analytic boundary closures are verified, but the C-API interval sweeps over $\theta \in (0,1)$ proposed by A4 remain unexecuted and lack explicit failure logs.

## Possible errors or hidden assumptions

1. **Manual Gamma Evaluation Error (A4):** As fully corrected in the review, A4 evaluated $\Gamma(2.5)$ as $1.5$ rather than the correct $1.32934$. While it did not change the binary truth of the inequality, manual floating-point arithmetic introduces severe risks when evaluating boundaries. This must be rigidly automated.
2. **Terminal Scalar Compensation Assumption (A1):** A1 implies that imposing a tight scalar relation at the end of the integral path can rescue a crude max-norm variation bound. This assumes the projection operator commutes with the accumulation of path error. It does not; the bound $\|Z(\zeta_{\text{cut}})\| \exp(\mathcal{V}_A)$ is fundamentally scaled by the $\operatorname{Bi}$ growth prior to reaching $\zeta_1$.
3. **Riccati Interval Blow-up Hazard:** The Riccati ODE $p_B v_u + v^2 + K_B = 0$ involves a $v^2$ term, which can induce rapid expansion of interval radii if the subdivision step is not sufficiently fine. Ensuring interval stability up to the critical point $v(u_1)=0$ requires tight outward-rounding control.
4. **Stable Rescaling Factorization:** A3's suggestion to rescale the $n=2$ cubic with $u=Bw$ is algebraically sound. However, evaluating interval polynomials near $\theta=0$ still demands precise factorization to prevent overlapping bounds from inflating the zero-crossings.

## Unsupported closure or overclaim audit

The panel operated with exemplary mathematical discipline. None of the active agents claimed the full KKT conjecture is proved, maintaining the strict calibration guidelines. A1 accurately labels its primary output as a "conditional certificate," leaving the variation integral as a hypothesis. A3 restricts its evaluations to "partially certified" when lacking finite-$n$ remainders. A4 claims its $n=1$ boundary cases are "PROVED," which is strictly accurate based on the explicit algebraic maximization (despite the arithmetic imprecision).

There were no occurrences of forbidden absolute closure rhetoric. All reported confidence metrics were safely bounded below the 0.89 maximum threshold. A1's slight overclaim regarding the analytical utility of the scalar sharpening was accurately diagnosed by A4 as a path-integral limitation, making it a strategic miscalculation rather than a rhetorical violation.

## Suggested synthesis

The KKT endpoint-cap architecture must now diverge decisively into a computational track for low degrees and a weighted asymptotic track for large degrees.

**Track 1: Low-Degree Riccati/Interval Certification ($n \le N_0$)**
The derivation of the exact Taylor coefficients $v_0, v_1, v_2, v_3$ for the Riccati variable $v(u) = p_B H'/H$ is highly significant. Because the Langer transformation generates complex removable singularities at $\theta=0$, the Riccati differential equation $p_B v_u + v^2 + K_B = 0$ provides a vastly superior alternative for direct interval evaluation. It is a pure rational-coefficient ODE. Initializing $v(u)$ at a small distance $u = \epsilon$ using the verified Taylor series, a validated interval ODE solver can integrate to the first zero of $v(u)$ (the critical point $u_1$) without encountering turning-point asymptotics. A4 must execute this script for degree one and $n=2$.

**Track 2: Large-Degree Olver-Weighted Langer Asymptotics ($n \ge N_0$)**
The synthesis fully adopts A4's structural critique: the crude matrix $\infty$-norm $\Omega_A$ is analytically inviable due to $\operatorname{Bi}$ contamination. All future variation tracking must be explicitly framed using Dunster-Gil-Segura (2020) weight functions $E(\zeta)$ and modulus functions $M(\zeta)$. The residual variation integral must take the strict form $\mathcal{V}_* = \int |\Psi_B(\xi)| M(\xi)^{-2} d\xi$ to rigorously enforce the recessive Frobenius boundary conditions and ensure variation decay on the $\theta=0$ face.

**Track 3: Binet Gamma Envelope**
The gamma-ratio $M_{n,\alpha,B}$ must be definitively bounded. A3's proof of leading entropy negativity is the essential stepping stone. The explicit Robbins remainders $\frac{1}{12z+1} < \mu(z) < \frac{1}{12z}$ must now be summed for the four log-gamma arguments, producing a concrete rational envelope to extract the numerical thresholds $N_0(c)$.

## Research strategy adjustments

The architectural formulations are complete. Round 20 must be an execution round focused strictly on bounding the defined objects.

1. **Retire Unweighted Langer Bounds**: The unweighted $\infty$-norm track is set aside unless a new weighted interpretation is supplied. A1 and A2 must structure all remaining large-$n$ variation estimates using explicit DGS weights.
2. **Execute Riccati IVP for Computation**: While interval Newton on the hypergeometric polynomials is valid, the Riccati integration directly targets the critical point via its zero-crossing. A4 should prioritize this methodology for computational execution.
3. **Execute Binet Remainder Algebra**: A3 must move beyond the asymptotic scaling $f(c)$ and derive the exact finite summation of the Robbins error terms to close the Gamma normalization.
4. **No New Normal Forms**: Do not introduce new coordinate transformations or alternative basis functions. Remain strictly within the Olver-weighted Langer formulation and the Riccati ODE.

## Verification plan

1. **Arb Riccati IVP Execution**: Implement the Taylor-initialized Riccati ODE $p_B v_u + v^2 + K_B = 0$ in Arb or FLINT. Verify the isolated zero $v(u_1)=0$ matches the exact analytic boundaries for degree one. Sweep the interior domain $\theta \in (0,1)$ for degree one and $n=2$, outputting the strict margins.
2. **Weighted Variation Integration**: Symbolically bound the explicit Dunster-Gil-Segura weighted variation integral $\mathcal{V}_*$ for the Langer residual $\Psi_B(\zeta)$ on the Laguerre face $\theta=0$. Prove that the weights successfully restore the asymptotic decay to $O(n^{-1})$ or $O(n^{-4/3})$.
3. **Finite-n Binet Summation**: Define the total remainder function from the four Robbins expansions for $\log M_{n,\alpha,B}$. Calculate the exact threshold index $N_0$ above which the negative leading entropy $-n |f(c)|$ permanently dominates the rational truncation errors.

## Score by agent

| Agent reviewed | Score (0-10) | Main reason | Must verify next |
| --- | --- | --- | --- |
| A1 | 7.5 | Brilliant algebraic derivation of the Wronskian scalar denominator, but failed to recognize the path-integral exponential blowup. | Formulate the Dunster-Gil-Segura weighted variation theorem. |
| A3 | 9.5 | Flawless algebraic audits of the $\Psi_B(0)$ singularity, Liouville sign, and exact Riccati Taylor coefficients. | Construct the explicit numeric thresholds using Binet/Robbins remainders. |
| A4 | 9.0 | Crucial structural refutation of unweighted norms and analytic boundary face closures, despite a manual evaluation error. | Execute the Arb interval sweep for the Riccati IVP track. |

## Next-round recommendation

The panel must proceed with strict segregation into execution tracks.

**For A1:**
Your scalar critical-point denominator $D_1$ is an exact algebraic identity, but A4 is correct that unweighted path integrals irreparably inflate the prior bounds. Your Round 20 task is to rewrite the global Langer variation theorem explicitly incorporating Dunster-Gil-Segura or Olver weight functions (e.g., $E(\zeta)$ and $M(\zeta)$) that mathematically enforce the recessive Frobenius boundary condition. Derive the exact weighted variation integral $\mathcal{V}_*$ that replaces $\mathcal{V}_A$.

**For A2:**
Focus on the quantitative evaluation of the Olver-weighted Langer residual. Using A1's weighted framework, target the $\theta=0$ Laguerre face in the mesoscopic regime $\alpha = C\sqrt{n}$. Prove explicitly how the Olver weight function suppresses the exponential $\operatorname{Bi}$ contamination and yields a theoretically bounded analytic error $E_{n,\alpha,\beta}$ that decays with $n$.

**For A3:**
Your $\Psi_B(0)$ and Riccati algebra is strongly supported by the current algebra. Your Round 20 task is to complete the Gamma-ratio envelope theorem. Take the Binet/Robbins expansion for $\log M_{n,\alpha,B}$ and explicitly sum the error bounds $\mu(z) \in (1/(12z+1), 1/(12z))$. Provide a concrete inequality demonstrating that the leading entropy $f(c)$ dominates the remainders, deriving an explicit numeric threshold $N_0$ for a test case (e.g., $c=0.5$).

**For A4:**
Your analytic boundary closures and $v_3$ derivations are certified. Your Round 20 task is to execute the actual interval arithmetic computations. Provide the literal output logs of an executed interval arithmetic sweep for degree one (and $n=2$ if feasible). Use the Riccati IVP formulation initiated from $v_{uu}(0)$ and $v_3$ as the primary mechanism, integrating to the first zero of $v(u)$ to verify the maximum amplitude. Report precise margins and any unresolved failure boxes. Do not blueprint; execute.

## Confidence

Confidence in the exact Riccati Taylor coefficients and ODE initialization: 0.88.
Confidence in A4's structural refutation of unweighted variation norms: 0.89.
Confidence in A3's exact rational evaluation of the removable turning point limit $\Psi_B(0)$: 0.88.
Confidence in the necessity of Dunster-Gil-Segura weights to resolve the path-integral exponential explosion: 0.87.
Confidence that interval Riccati IVP integration will successfully close the low-degree domains: 0.82.
Confidence that the real-parameter KKT conjecture is fully proven at this stage: 0.08.

## Pre-submit calibration check

* blocked rhetoric: none found.
* confidence values: strictly $\le 0.89$.
* unsupported proof-closure claim: none found.
* invented citation: none found.
* length and formatting: All 19 required labels present and correctly ordered. Word count targets satisfied via detailed mathematical derivations and refutations. Markdown formatting is pure and properly delimited.


