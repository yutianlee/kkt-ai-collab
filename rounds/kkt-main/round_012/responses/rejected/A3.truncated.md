# Model Reasoning Content

We need to produce Stage A independent reasoning for A3, the algebra checker and endpoint-reduction auditor. The task: rederive independently the transformation from Jacobi self-adjoint equation to endpoint ODE, verify formulas, check the cap bound, verify the monotonicity lemma, derive Frobenius coefficient and Bessel normalization, and check critical point equation. Mark each formula as verified, corrected, or rejected. Produce a symbolic/numeric check plan for the formulas.

We need to start from the Jacobi differential equation for the weighted normalized function, transform to endpoint variable, and confirm the claimed ODE and constants. Also verify the identity $u_\sigma = nB/(B+n-1)$, the inequality $K_B'(u)\ge 1/4$, etc. Must check for any algebraic mistakes.

The reading packet provides most formulas. Let's rederive step by step.

Define $g_n^{(\alpha,\beta)}(x)$ as in the problem. The Jacobi polynomial $P_n^{(\alpha,\beta)}(x)$ satisfies the Jacobi ODE:

\[
(1-x^2) y'' + [\beta-\alpha - (\alpha+\beta+2)x] y' + n(n+\alpha+\beta+1) y = 0.
\]

But we have the weighted normalized function:

\[
g(x) = C_{n,\alpha,\beta} \left(\frac{1-x}{2}\right)^{\alpha/2} \left(\frac{1+x}{2}\right)^{\beta/2} P_n^{(\alpha,\beta)}(x).
\]

with $C_{n,\alpha,\beta}^2 = \frac{\Gamma(n+1)\Gamma(n+s+1)}{\Gamma(n+\alpha+1)\Gamma(n+\beta+1)}$, $s=\alpha+\beta$.

The known transformed ODE derived in earlier rounds:

\[
(Ag')' + \frac{F(x)}{A} g = 0,
\]

where $A(x) = 1-x^2$, and

\[
F(x) = \kappa(1-x^2) - \frac14 (d - s x)^2,
\]

with $\kappa = n(n+s+1) + s/2$, $s=\alpha+\beta$, $d = \beta-\alpha$.

This is given in A1-11 and other files. I'll rederive quickly to confirm correctness, but more important is the endpoint ODE from the variable $u = B(1-x)/2$.

Set $B = n+s+1$ (note: some define $B = n+\alpha+\beta+1$, same). Define

\[
u = \frac{B(1-x)}{2}, \quad x = 1 - \frac{2u}{B}.
\]

Then $dx = -2 du / B$, and $1-x = 2u/B$, $1+x = 2 - 2u/B = 2(1 - u/B)$. So $A(x) = 1-x^2 = (1-x)(1+x) = (2u/B) \cdot 2(1 - u/B) = \frac{4u}{B} (1 - u/B)$. So $A(x) = \frac{4}{B} u(1 - u/B)$.

Also the weight factor: $\left(\frac{1-x}{2}\right)^{\alpha/2} \left(\frac{1+x}{2}\right)^{\beta/2} = (u/B)^{\alpha/2} (1 - u/B)^{\beta/2}$.

Now the function $H(u) = g(x)$ up to possible normalization factor? The definition in A1-Strategy: $H_{n,\alpha,\beta}(u) = G_n^{(\alpha,\beta)}(1 - \frac{2u}{B})$, where $G$ is the weighted normalized function. So $H(u) = g(1 - 2u/B)$. No extra factor.

Now we need the ODE for $H(u)$. Starting from $(Ag')' + \frac{F}{A} g = 0$. Transform $g'(x) = - \frac{B}{2} H'(u)$, $g''(x) = \frac{B^2}{4} H''(u)$. Also $A(x) = \frac{4}{B} u(1-u/B)$. So

\[
Ag' = \frac{4}{B} u(1-u/B) \cdot \left(-\frac{B}{2}\right) H'(u) = -2 u(1-u/B) H'(u).
\]

Then differentiate w.r.t $x$:

\[
\frac{d}{dx} (Ag') = \frac{d}{du} (-2 u(1-u/B) H'(u)) \cdot \frac{du}{dx} = \frac{d}{du} (-2 u(1-u/B) H'(u)) \cdot \left(-\frac{B}{2}\right).
\]

Since $du/dx = -B/2$. So

\[
(Ag')' = \frac{B}{2} \cdot \frac{d}{du} \left( 2 u(1-u/B) H'(u) \right) = B \frac{d}{du} \left( u(1-u/B) H'(u) \right).
\]

Now $F(x)/A(x)$: we need $F$ expressed in $u$. Since $F(x) = \kappa(1-x^2) - \frac14 (d - s x)^2$. Compute $1-x^2 = \frac{4}{B} u(1-u/B)$. Also compute $d - s x$. $d = \beta - \alpha$, $s = \alpha+\beta$, $x = 1 - \frac{2u}{B}$.

Then $d - s x = (\beta - \alpha) - (\alpha+\beta)(1 - \frac{2u}{B}) = (\beta - \alpha) - (\alpha+\beta) + \frac{2s u}{B} = -2\alpha + \frac{2s u}{B}$.

So $\frac14 (d - s x)^2 = \frac14 ( -2\alpha + \frac{2s u}{B} )^2 = \alpha^2 - \frac{2\alpha s u}{B} + \frac{s^2 u^2}{B^2}$.

Thus

\[
F(x) = \kappa \cdot \frac{4}{B} u(1-u/B) - \left( \alpha^2 - \frac{2\alpha s u}{B} + \frac{s^2 u^2}{B^2} \right).
\]

Simplify $\kappa = n(n+s+1) + s/2 = nB + s/2$ because $B = n+s+1$, so $n(n+s+1) = n(B-1)$? Wait $B = n+s+1$, so $nB = n(n+s+1) + n$. Actually $n(n+s+1) = n(B-1) = nB - n$. So $\kappa = nB - n + s/2$. But let's keep $\kappa$.

Now the ODE $(Ag')' + \frac{F}{A} g = 0$ becomes:

\[
B \frac{d}{du} \left( u(1-u/B) H' \right) + \frac{F}{A} H = 0,
\]

where $A = \frac{4}{B} u(1-u/B)$. So $\frac{F}{A} = \frac{F}{\frac{4}{B} u(1-u/B)}$. Insert $F$:

\[
\frac{F}{A} = \frac{ \kappa \frac{4}{B} u(1-u/B) - (\alpha^2 - \frac{2\alpha s u}{B} + \frac{s^2 u^2}{B^2}) }{ \frac{4}{B} u(1-u/B) } = \kappa - \frac{ \alpha^2 - \frac{2\alpha s u}{B} + \frac{s^2 u^2}{B^2} }{ \frac{4}{B} u(1-u/B) }.
\]

Simplify the fraction: multiply numerator and denominator:

\[
\frac{ \alpha^2 - \frac{2\alpha s u}{B} + \frac{s^2 u^2}{B^2} }{ \frac{4}{B} u(1-u/B) } = \frac{B}{4 u (1-u/B)} \left( \alpha^2 - \frac{2\alpha s u}{B} + \frac{s^2 u^2}{B^2} \right) = \frac{ \alpha^2 B }{4 u (1-u/B)} - \frac{ \alpha s }{2 (1-u/B)} + \frac{ s^2 u }{4 B (1-u/B) }.
\]

But that seems messy. The A1-Strategy gives a cleaner self-adjoint form:

\[
\left( p_B(u) H'(u) \right)' + q_B(u) H(u) = 0,
\]

with $p_B(u) = u(1-u/B)$ and

\[
q_B(u) = c_B - \frac{ (r_B u - \alpha)^2 }{4 u (1-u/B) },
\]

where $r_B = s/B = (B-n-1)/B = 1 - (n+1)/B$, and $c_B = n + \frac{s}{2B} = n + \frac{r_B}{2}$. Let's check if this matches.

Compute $(p_B H')' = \frac{d}{du}( u(1-u/B) H' )$. Our derived ODE had a prefactor $B$: $B (u(1-u/B) H')' + \frac{F}{A} H = 0$, i.e., $(u(1-u/B) H')' + \frac{F}{B A} H = 0$. So we need to check $q_B = \frac{F}{B A}$? Because the self-adjoint form has no extra $B$ factor; it's directly $(p_B H')' + q_B H = 0$. So we need to see if $q_B(u)$ as given equals $\frac{F}{B A}$. Compute:

\[
\frac{F}{B A} = \frac{F}{B \cdot \frac{4}{B} u(1-u/B)} = \frac{F}{4 u(1-u/B)}.
\]

But $F$ itself has term $\kappa \cdot \frac{4}{B} u(1-u/B)$ minus that square. So

\[
\frac{F}{4 u(1-u/B)} = \kappa \frac{1}{B} - \frac{ \alpha^2 - \frac{2\alpha s u}{B} + \frac{s^2 u^2}{B^2} }{4 u(1-u/B)}.
\]

Now $\kappa/B = \frac{ nB + s/2 }{B} = n + \frac{s}{2B} = n + \frac{r_B}{2} = c_B$. That's promising. So

\[
\frac{F}{B A} = c_B - \frac{ \alpha^2 - \frac{2\alpha s u}{B} + \frac{s^2 u^2}{B^2} }{4 u(1-u/B)}.
\]

Now notice $(r_B u - \alpha)^2 = ( \frac{s}{B} u - \alpha )^2 = \frac{s^2}{B^2} u^2 - 2\frac{s}{B} \alpha u + \alpha^2 = \alpha^2 - \frac{2\alpha s u}{B} + \frac{s^2 u^2}{B^2}$. So indeed the numerator matches. Thus

\[
\frac{F}{B A} = c_B - \frac{ (r_B u - \alpha)^2 }{4 u (1-u/B) } = q_B(u).
\]

Thus $(u(1-u/B) H')' + q_B(u) H = 0$. Perfect. So the endpoint ODE is verified.

Now the $K_B(u) = p_B(u) q_B(u) = u(1-u/B) q_B(u) = c_B u(1-u/B) - \frac{ (r_B u - \alpha)^2 }{4 }$.

Compute $c_B u(1-u/B) = c_B (u - u^2/B) = c_B u - \frac{c_B}{B} u^2$. So

\[
K_B(u) = c_B u - \frac{c_B}{B} u^2 - \frac{1}{4} ( r_B^2 u^2 - 2\alpha r_B u + \alpha^2 ) = -\frac{\alpha^2}{4} + (c_B + \frac{\alpha r_B}{2}) u - (\frac{c_B}{B} + \frac{r_B^2}{4}) u^2.
\]

Thus $\Lambda_B = c_B + \frac{\alpha r_B}{2}$, $\Delta_B = \frac{c_B}{B} + \frac{r_B^2}{4}$. Verified.

Now $c_B = n + \frac{r_B}{2}$. So $\Lambda_B = n + \frac{r_B}{2} + \frac{\alpha r_B}{2} = n + \frac{r_B (1+\alpha)}{2}?$ Wait $\alpha r_B /2$ plus $r_B/2$ gives $\frac{r_B (\alpha+1)}{2}$. So $\Lambda_B = n + \frac{r_B (\alpha+1)}{2}$. That's a simpler form? It doesn't matter. The A1-Strategy gives $\Lambda_B = c_B + \frac{\alpha r_B}{2}$. It's fine.

Now $K_B'(u) = \Lambda_B - 2 \Delta_B u$. The monotonicity lemma $K_B'(u) \ge 1/4$ on $0 \le u \le u_\sigma$ with $\alpha \ge 1/2$, $n\ge1$, $\beta\ge0$. Let's verify the algebraic certification in the bootstrap judge. The bootstrap judge derived:

\[
K_B'(u_\sigma) - \frac14 = \frac14 \left( 2\alpha + 2n + 1 - \frac{2(n+1)(n+\alpha+1)}{B} \right).
\]

Let's rederive that to ensure no mistake. Compute $K_B'(u_\sigma) = \Lambda_B - 2\Delta_B u_\sigma$. We need explicit expressions.

First, $r_B = s/B = (B-n-1)/B = 1 - (n+1)/B$.
$c_B = n + \frac{r_B}{2} = n + \frac12 - \frac{n+1}{2B}$.

$\Lambda_B = c_B + \frac{\alpha r_B}{2} = n + \frac12 - \frac{n+1}{2B} + \frac{\alpha}{2} (1 - \frac{n+1}{B}) = n + \frac12 + \frac{\alpha}{2} - \frac{n+1}{2B} - \frac{\alpha (n+1)}{2B} = n + \frac{\alpha+1}{2} - \frac{(n+1)(\alpha+1)}{2B}$.

$\Delta_B = \frac{c_B}{B} + \frac{r_B^2}{4} = \frac{n + \frac12 - \frac{n+1}{2B}}{B} + \frac{ (1 - (n+1)/B)^2 }{4} = \frac{n + \frac12}{B} - \frac{n+1}{2B^2} + \frac{1}{4} - \frac{n+1}{2B} + \frac{(n+1)^2}{4B^2}$.

Combine: $\Delta_B = \frac14 + \frac{n + \frac12 - \frac{n+1}{2}}{B} + \dots$? Let's compute carefully:

\[
\Delta_B = \frac{1}{4} + \frac{1}{B}\left( n + \frac12 - \frac{n+1}{2} \right) + \frac{1}{B^2}\left( - \frac{n+1}{2} + \frac{(n+1)^2}{4} \right).
\]
Simplify: $n + \frac12 - \frac{n+1}{2} = n + \frac12 - \frac{n}{2} - \frac12 = \frac{n}{2}$. So the $1/B$ term is $\frac{n}{2B}$.

The $1/B^2$ term: $- \frac{n+1}{2} + \frac{(n+1)^2}{4} = \frac{n+1}{4} ( -2 + (n+1) ) = \frac{(n+1)(n-1)}{4}$.

Thus $\Delta_B = \frac14 + \frac{n}{2B} + \frac{(n+1)(n-1)}{4B^2}$.

But maybe easier to directly evaluate $K_B'(u_\sigma)$. The bootstrap judge gave a simplification claiming $K_B'(u_\sigma) - 1/4 = \frac14 (2\alpha + 2n + 1 - \frac{2(n+1)(n+\alpha+1)}{B})$. Let's verify.

We have $u_\sigma = \frac{nB}{B+n-1}$. And $K_B'(u) = \Lambda_B - 2 \Delta_B u$.

Compute $2 \Delta_B u_\sigma$:

First $\Delta_B$ expression we can use $ \Delta_B = \frac{c_B}{B} + \frac{r_B^2}{4}$. Using $c_B = n + r_B/2$, and $r_B = 1 - (n+1)/B$. Possibly simpler to compute $K_B'(u_\sigma) = \Lambda_B - 2 \Delta_B u_\sigma$ using relationships.

Alternatively, note that $K_B(u) = p_B q_B$, and $K_B'(u) = p_B' q_B + p_B q_B'$. But maybe easier to derive the expression in the bootstrap judge: they claimed $K_B'(u_\sigma) - 1/4 = \frac14 (2\alpha + 2n + 1 - \frac{2(n+1)(n+\alpha+1)}{B})$. Let's test with some numeric values to see if this identity holds. If it does, then the inequality $K_B'(u_\sigma) \ge 1/4$ reduces to $2\alpha + 2n + 1 \ge \frac{2(n+1)(n+\alpha+1)}{B}$. Since $B \ge n+\alpha+1$, this holds. And indeed $\alpha\ge1/2$ ensures it. But we must verify the formula.

Let's derive $K_B'(u_\sigma)$ from first principles with algebraic manipulation.

We have $K_B(u) = -\alpha^2/4 + \Lambda_B u - \Delta_B u^2$. So $K_B'(u) = \Lambda_B - 2\Delta_B u$.

We need explicit $\Lambda_B$ and $\Delta_B$ in terms of $n,\alpha,\beta$ (or $B$).

Recall $B = n + s + 1$, $s = \alpha+\beta$, $\beta = s - \alpha = B - n - 1 - \alpha$. But we can treat $B$ and $\alpha$ and $n$ as independent, with $B \ge n+\alpha+1$ (since $\beta\ge0$). So $B$ varies.

Compute $r_B = \frac{s}{B} = \frac{B - n - 1}{B} = 1 - \frac{n+1}{B}$.

$c_B = n + \frac{r_B}{2} = n + \frac12 - \frac{n+1}{2B}$.

$\Lambda_B = c_B + \frac{\alpha r_B}{2} = n + \frac12 - \frac{n+1}{2B} + \frac{\alpha}{2}(1 - \frac{n+1}{B}) = n + \frac12 + \frac{\alpha}{2} - \frac{n+1}{2B}(1 + \alpha) = n + \frac{\alpha+1}{2} - \frac{(n+1)(\alpha+1)}{2B}$.

Now $\Delta_B$: as derived earlier: $\Delta_B = \frac{c_B}{B} + \frac{r_B^2}{4}$. Compute $\frac{c_B}{B} = \frac{n}{B} + \frac{1}{2B} - \frac{n+1}{2B^2}$.

$r_B^2 /4 = \frac14 (1 - \frac{n+1}{B})^2 = \frac14 - \frac{n+1}{2B} + \frac{(n+1)^2}{4B^2}$.

Thus $\Delta_B = \frac14 - \frac{n+1}{2B} + \frac{(n+1)^2}{4B^2} + \frac{n}{B} + \frac{1}{2B} - \frac{n+1}{2B^2}$.

Combine $1/B$ terms: $-\frac{n+1}{2} + n + \frac12 = n - \frac{n}{2} - \frac12 + \frac12 = \frac{n}{2}$. Good.

Combine $1/B^2$ terms: $\frac{(n+1)^2}{4} - \frac{n+1}{2} = \frac{n+1}{4}( (n+1) - 2 ) = \frac{(n+1)(n-1)}{4}$.

Thus $\Delta_B = \frac14 + \frac{n}{2B} + \frac{(n+1)(n-1)}{4B^2}$.

Now $K_B'(u) = \Lambda_B - 2 \Delta_B u = [n + \frac{\alpha+1}{2} - \frac{(n+1)(\alpha+1)}{2B}] - 2u [ \frac14 + \frac{n}{2B} + \frac{(n+1)(n-1)}{4B^2} ]$.

Simplify $2 \Delta_B u = \frac{u}{2} + \frac{n u}{B} + \frac{(n+1)(n-1) u}{2 B^2}$.

Now plug $u = u_\sigma = \frac{nB}{B+n-1}$.

Compute terms:

$2\Delta_B u_\sigma = \frac12 \frac{nB}{B+n-1} + \frac{n}{B} \frac{nB}{B+n-1} + \frac{(n+1)(n-1)}{2B^2} \frac{nB}{B+n-1} = \frac{nB}{2(B+n-1)} + \frac{n^2}{B+n-1} + \frac{n(n+1)(n-1)}{2B (B+n-1)}$.

That seems messy. The bootstrap judge simplified to $\frac14 (2\alpha+2n+1 - \dots )$, which suggests a cleaner algebraic identity. Let's verify if it's correct.

Bootstrap judge says:

\[
K_B'(u_\sigma) - \frac14 = \frac14 \left( 2\alpha + 2n + 1 - \frac{2(n+1)(n+\alpha+1)}{B} \right).
\]

Multiplying both sides by 4:

\[
4K_B'(u_\sigma) - 1 = 2\alpha + 2n + 1 - \frac{2(n+1)(n+\alpha+1)}{B}.
\]

Thus

\[
4K_B'(u_\sigma) = 2\alpha + 2n + 2 - \frac{2(n+1)(n+\alpha+1)}{B}.
\]

So $K_B'(u_\sigma) = \frac{ \alpha + n + 1 }{2} - \frac{(n+1)(n+\alpha+1)}{2B}$.

Now compare with our expression for $K_B'(u)$ in terms of $n,\alpha,B$? $K_B'(u_\sigma) = \Lambda_B - 2\Delta_B u_\sigma$. We need to check if $\Lambda_B - 2\Delta_B u_\sigma$ simplifies to that expression. Let's try to simplify $\Lambda_B - 2\Delta_B u_\sigma$ using the relationships without going through heavy rational simplification. We can try to compute $K_B'(u_\sigma)$ as the derivative of $K_B$ evaluated at $u_\sigma$, but maybe there is a more intrinsic relation. Recall $K_B(u) = p_B(u) q_B(u)$ and $u_\sigma$ is defined by $x=\sigma$ where $\sigma = s/(s+2n) = (B-n-1)/(B+n-1)$. This $u_\sigma$ is $u$ at central/endcap interface. Is there a simpler expression for $K_B'(u_\sigma)$ derived from the original $F$ and $A$? Possibly $K_B'(u_\sigma)$ connects to something like $F'(\sigma)$? Could derive from the transformation. But easier: let's just compute $K_B'(u)$ using explicit $\Lambda_B$ and $\Delta_B$, plug $u_\sigma$, and simplify symbolically. Since we're doing algebra audit, we can do that manually or reason.

We have $u_\sigma = \frac{nB}{B+n-1}$. Compute $K_B'(u_\sigma) = \Lambda_B - 2\Delta_B u_\sigma$.

From earlier:

$\Lambda_B = n + \frac{\alpha+1}{2} - \frac{(n+1)(\alpha+1)}{2B}$.

$2\Delta_B u_\sigma = 2 u_\sigma \left( \frac14 + \frac{n}{2B} + \frac{(n+1)(n-1)}{4B^2} \right) = \frac{u_\sigma}{2} + \frac{n u_\sigma}{B} + \frac{(n+1)(n-1) u_\sigma}{2 B^2}$.

Plug $u_\sigma = \frac{nB}{B+n-1}$:

- $\frac{u_\sigma}{2} = \frac{nB}{2(B+n-1)}$.
- $\frac{n u_\sigma}{B} = \frac{n^2 B}{B(B+n-1)} = \frac{n^2}{B+n-1}$.
- $\frac{(n+1)(n-1) u_\sigma}{2 B^2} = \frac{(n+1)(n-1)}{2 B^2} \cdot \frac{nB}{B+n-1} = \frac{n(n+1)(n-1)}{2B (B+n-1)}$.

So $2\Delta_B u_\sigma = \frac{nB}{2(B+n-1)} + \frac{n^2}{B+n-1} + \frac{n(n+1)(n-1)}{2B (B+n-1)}$.

Combine the first two terms over denominator $2(B+n-1)$: first term $\frac{nB}{2(B+n-1)}$, second term $\frac{2n^2}{2(B+n-1)}$. So sum of first two: $\frac{nB + 2n^2}{2(B+n-1)}$.

So $2\Delta_B u_\sigma = \frac{nB + 2n^2}{2(B+n-1)} + \frac{n(n+1)(n-1)}{2B (B+n-1)}$.

Now subtract from $\Lambda_B$: $K_B'(u_\sigma) = \Lambda_B - 2\Delta_B u_\sigma = n + \frac{\alpha+1}{2} - \frac{(n+1)(\alpha+1)}{2B} - \frac{nB + 2n^2}{2(B+n-1)} - \frac{n(n+1)(n-1)}{2B (B+n-1)}$.

That looks unlike the simple bootstrap judge expression. But perhaps it simplifies to that. Let's test numerically with some values to see if the bootstrap judge expression is correct and if ours matches. We can do mental arithmetic or symbolically. Choose easy numbers: $n=1$, $\alpha=1/2$, $\beta=0$ gives $s=1/2$, $B = n+s+1 = 1 + 0.5 + 1 = 2.5$. But $\beta$ integer? Not needed. Let's pick $\beta=0$, $n=1$, $\alpha=1$ (just for test). Then $s=1$, $B = 1+1+1=3$. $u_\sigma = nB/(B+n-1) = 1*3/(3+1-1)=3/3=1$. Then compute $\Lambda_B$: $r_B = s/B = 1/3$. $c_B = n + r_B/2 = 1 + 1/6 = 7/6$. $\Lambda_B = c_B + \alpha r_B/2 = 7/6 + 1*(1/6)=8/6=4/3$. $\Delta_B = c_B/B + r_B^2/4 = (7/6)/3 + (1/9)/4 = 7/18 + 1/36 = 14/36 + 1/36 = 15/36 = 5/12$. Then $K_B'(u_\sigma) = \Lambda_B - 2\Delta_B u_\sigma = 4/3 - 2*(5/12)*1 = 4/3 - 5/6 = 8/6 - 5/6 = 3/6 = 1/2$. Now bootstrap judge expression: $\frac14 (2\alpha+2n+1 - \frac{2(n+1)(n+\alpha+1)}{B}) = \frac14 (2*1 + 2*1 + 1 - \frac{2*2*(1+1+1)}{3}) = \frac14 (2+2+1 - \frac{2*2*3}{3}) = \frac14 (5 - 4) = 1/4$. Then $K_B'(u_\sigma) = 1/4 + 1/4 = 1/2$. Matches! So the identity holds for that test.

Another test: $n=2$, $\alpha=1$, $\beta=1$. Then $s=2$, $B = 2+2+1=5$. $u_\sigma = 2*5/(5+2-1)=10/6=5/3$. $r_B = 2/5$, $c_B = 2 + 1/5 = 11/5 = 2.2$. $\Lambda_B = 2.2 + 1*(2/5)/2 = 2.2 + 0.2 = 2.4$. $\Delta_B = (11/5)/5 + (4/25)/4 = 11/25 + 1/25 = 12/25 = 0.48$. $K_B'(u_\sigma) = 2.4 - 2*0.48*(5/3) = 2.4 - 0.96*1.666... = 2.4 - 1.6 = 0.8$. Bootstrap judge: $\frac14 (2*1 + 4 + 1 - \frac{2*3*(2+1+1)}{5}) = \frac14 (2+4+1 - \frac{2*3*4}{5}) = \frac14 (7 - 24/5) = \frac14 (7 - 4.8) = \frac14 * 2.2 = 0.55$. Then $K_B'(u_\sigma) = 0.55 + 0.25 = 0.8$. Matches. So the bootstrap judge identity is algebraically correct. We can trust it.

Thus the inequality $K_B'(u_\sigma) \ge 1/4$ reduces to $2\alpha + 2n + 1 \ge \frac{2(n+1)(n+\alpha+1)}{B}$, i.e., $B (2\alpha + 2n + 1) \ge 2(n+1)(n+\alpha+1)$. Since $B \ge n+\alpha+1$, the RHS denominator? Actually inequality: $2\alpha+2n+1 - \frac{2(n+1)(n+\alpha+1)}{B} \ge 0$. Since $B \ge n+\alpha+1$, $\frac{2(n+1)(n+\alpha+1)}{B} \le 2(n+1)$. So LHS $\ge 2\alpha+2n+1 - 2(n+1) = 2\alpha -1$. And $\alpha \ge 1/2$ gives $\ge 0$. So $K_B'(u_\sigma) \ge 1/4 + \frac{2\alpha-1}{4} \ge 1/4$. So the monotonicity lemma is verified. Good.

Now check the endpoint cap interface bound $u_\sigma \le n$. $u_\sigma = \frac{nB}{B+n-1} \le n$ is equivalent to $B \le B+n-1$, i.e., $n-1 \ge 0$? Actually $\frac{nB}{B+n-1} \le n$ implies $B \le B + n - 1$, i.e., $n-1 \ge 0$, which holds for $n\ge 1$. For $n=1$, equality $u_\sigma = B/(B) = 1$, so $u_\sigma = n$. For $n>1$, $u_\sigma < n$. So it's valid. And $u_\sigma \ge 0$ obviously. So $0 \le u \le u_\sigma \le n$ is a correct range.

Now Frobenius coefficient and Bessel normalization: near $u=0$, the endpoint ODE has a regular singular point. Since $p_B(u) = u(1-u/B) \approx u$, the leading behavior is $u H'' + H' + \dots$. Standard Frobenius: $H(u) \sim A u^{\alpha/2}$. We need to determine $A$ from matching with $g(x)$ as $x \to 1$. Actually as $x \to 1$, $u \to 0$, and $g(x) \approx C_{n,\alpha,\beta} ( (1-x)/2 )^{\alpha/2} \cdot \text{constant}$. Since $P_n^{(\alpha,\beta)}(1) = \binom{n+\alpha}{n}$ (or something). Indeed the Jacobi polynomial at $x=1$ is $\binom{n+\alpha}{n} = \frac{\Gamma(n+\alpha+1)}{\Gamma(\alpha+1)\Gamma(n+1)}$. The weight factor $(1-x)/2$ to the $\alpha/2$, times the normalization constant. So $H(u) \approx C_{n,\alpha,\beta} (u/B)^{\alpha/2} \cdot \frac{\Gamma(n+\alpha+1)}{\Gamma(\alpha+1)\Gamma(n+1)}$.

The constant $C_{n,\alpha,\beta}$ is the square root of the gamma ratio. So

\[
H(u) \sim C_{n,\alpha,\beta} \frac{\Gamma(n+\alpha+1)}{\Gamma(\alpha+1)\Gamma(n+1)} B^{-\alpha/2} u^{\alpha/2}.
\]

Thus the Frobenius coefficient $A_{n,\alpha,B}$ (denoted in A1-Strategy) is

\[
A_{n,\alpha,B} = C_{n,\alpha,\beta} \frac{\Gamma(n+\alpha+1)}{\Gamma(\alpha+1)\Gamma(n+1)} B^{-\alpha/2}.
\]

But A1-Strategy defines $A_{n,\alpha,B} = \left\{ \frac{\Gamma(n+1)\Gamma(B)} {\Gamma(n+\alpha+1)\Gamma(B-\alpha)} \right\}^{1/2} B^{-\alpha/2} \frac{\Gamma(n+\alpha+1)} {\Gamma(\alpha+1)\Gamma(n+1)}$. Let's check: $C_{n,\alpha,\beta}^2 = \frac{\Gamma(n+1)\Gamma(n+s+1)}{\Gamma(n+\alpha+1)\Gamma(n+\beta+1)} = \frac{\Gamma(n+1)\Gamma(B)}{\Gamma(n+\alpha+1)\Gamma(B-\alpha)}$ because $\beta = B - n - 1 - \alpha$? Actually $B = n+s+1 = n+\alpha+\beta+1$, so $\beta = B - n - 1 - \alpha$. So $\Gamma(n+\beta+1) = \Gamma(n + (B-n-1-\alpha) + 1) = \Gamma(B-\alpha)$. Yes. So $C_{n,\alpha,\beta} = \sqrt{ \frac{\Gamma(n+1)\Gamma(B)}{\Gamma(n+\alpha+1)\Gamma(B-\alpha)} }$. So

\[
A_{n,\alpha,B} = \sqrt{ \frac{\Gamma(n+1)\Gamma(B)}{\Gamma(n+\alpha+1)\Gamma(B-\alpha)} } \cdot \frac{\Gamma(n+\alpha+1)}{\Gamma(\alpha+1)\Gamma(n+1)} \cdot B^{-\alpha/2}.
\]

Simplify: $\frac{\Gamma(n+\alpha+1)}{\sqrt{\Gamma(n+\alpha+1)}} = \sqrt{\Gamma(n+\alpha+1)}$. So

\[
A_{n,\alpha,B} = \sqrt{ \frac{\Gamma(n+1)\Gamma(B)}{\Gamma(n+\alpha+1)\Gamma(B-\alpha)} \cdot \frac{\Gamma(n+\alpha+1)^2}{\Gamma(\alpha+1)^2 \Gamma(n+1)^2} } \cdot B^{-\alpha/2}
= \frac{ \sqrt{ \Gamma(B) \Gamma(n+\alpha+1) } }{ \Gamma(\alpha+1) \sqrt{ \Gamma(B-\alpha) \Gamma(n+1) } } B^{-\alpha/2}.
\]

But A1-Strategy writes $A_{n,\alpha,B} = \left\{ \frac{\Gamma(n+1)\Gamma(B)} {\Gamma(n+\alpha+1)\Gamma(B-\alpha)} \right\}^{1/2} B^{-\alpha/2} \frac{\Gamma(n+\alpha+1)} {\Gamma(\alpha+1)\Gamma(n+1)}$. That's the same as above. So we can keep that.

Now the Bessel model: they define $\mathcal B_{n,\alpha,B}(u) = M_{n,\alpha,B} J_\alpha (2 \sqrt{\Lambda_B u})$, with $M_{n,\alpha,B} = A_{n,\alpha,B} \Gamma(\alpha+1) \Lambda_B^{-\alpha/2}$. Indeed, because the Bessel function $J_\alpha(z) \sim \frac{1}{\Gamma(\alpha+1)} (z/2)^\alpha$ for $z\to0$. So $M_{n,\alpha,B} J_\alpha(2\sqrt{\Lambda_B u}) \sim M_{n,\alpha,B} \frac{1}{\Gamma(\alpha+1)} (\sqrt{\Lambda_B u})^\alpha = M_{n,\alpha,B} \frac{ \Lambda_B^{\alpha/2} }{ \Gamma(\alpha+1) } u^{\alpha/2}$. To match $A_{n,\alpha,B} u^{\alpha/2}$, we need $M_{n,\alpha,B} = A_{n,\alpha,B} \Gamma(\alpha+1) \Lambda_B^{-\alpha/2}$. So that's correct.

Plug in $A_{n,\alpha,B}$ to get $M_{n,\alpha,B} = \left\{ \frac{\Gamma(n+1)\Gamma(B)} {\Gamma(n+\alpha+1)\Gamma(B-\alpha)} \right\}^{1/2} B^{-\alpha/2} \frac{\Gamma(n+\alpha+1)} {\Gamma(\alpha+1)\Gamma(n+1)} \cdot \Gamma(\alpha+1) \Lambda_B^{-\alpha/2} = \left\{ \frac{\Gamma(B) \Gamma(n+\alpha+1)} {\Gamma(B-\alpha) \Gamma(n+1)} \right\}^{1/2} (B \Lambda_B)^{-\alpha/2}$.

Yes, that's exactly the A1-Strategy expression. So that's verified.

Now, the critical point equation: The points where $g'(x)=0$ or $H'(u)=0$. Could be derived from the ODE. But for finite verification, it's useful. However, for the first-lobe reduction, we rely on Sonin functional. That's plausible but we can note the need to confirm that $u_1$ is indeed a local maximum and that $K_B(u_1)>0$. Since $H$ starts at $0$ with positive derivative, it increases to a first maximum. Usually for the regular solution, $H(0)=0$, $H'(0)>0$, so $u_1$ exists. Then $H'(u_1)=0$, so $S_B(u_1) = H(u_1)^2$. The Sonin functional derivative $S_B' \le 0$ when $K_B>0$ ensures that $S_B$ is decreasing. Since $S_B(u) \ge H(u)^2$, we get $H(u)^2 \le S_B(u) \le S_B(u_1) = H(u_1)^2$. However, this requires that $K_B(u)>0$ on the interval $[u_1, u_\sigma]$ or at least that $S_B' \le 0$ there. The monotonicity lemma gives $K_B'(u) \ge 1/4 > 0$, so $K_B$ is increasing. At $u=0$, $K_B(0) = -\alpha^2/4 < 0$. So $K_B$ will cross zero at some $u_0$. For $u>u_0$, $K_B>0$. We need to ensure that $u_1 > u_0$ and that the Sonin functional is defined and decreasing on $[u_1, u_\sigma]$. This is a standard Sonin-Polya argument: $S_B$ is defined where $K_B>0$; $S_B'$ expression uses $K_B>0$. If $K_B(u_1)>0$, then $H(u_1)$ is a local max and $K_B(u_1) = - H''(u_1) / H(u_1)$? Actually from ODE: $(p_B H')' + q_B H = 0$; at a critical point, $H' = 0$, so $p_B H'' + q_B H = 0$. Since $p_B>0$ for $0<u<B$, $H''(u_1) = - q_B(u_1) H(u_1)$. And $q_B = K_B/p_B$. So $H''(u_1) = - (K_B(u_1)/p_B(u_1)) H(u_1)$. For a local max, $H''(u_1) < 0$, so $K_B(u_1)$ must be positive. So indeed $u_1$ is in region $K_B>0$. Good. So the Sonin argument should hold.

Thus the first-lobe reduction is plausible and standard, but we must check edge cases like $u_1$ beyond $u_\sigma$ or $u_1$ after $u_\sigma$? Since $u_\sigma \le n$ and the cap is small, it's possible that $u_1 > u_\sigma$, meaning the first maximum occurs after the cap, but then the cap is monotonic and we can bound by $H(u_\sigma)$ which is small because central control gives $|H(u_\sigma)| \le T$? Actually at $x=\sigma$, we already have the bound from central theorem. So the cap max is still bounded. The Sonin reduction claimed $\sup_{0\le u\le u_\sigma} |H(u)| = H(u_1)$ if $u_1 \le u_\sigma$, else it's bounded by the interface value. So that's fine.

Now the bootstrap judge's G1: Endpoint Sonin reduction lemma needs to be rigorously stated. We can note that as a gap.

Now the remaining main task: verify the first-lobe Bessel perturbation theorem. That's not purely algebraic; it involves analysis. But as algebra checker, I should check that the ODE transformation to Bessel-like form is correct. How would one get a perturbation of Bessel? The endpoint ODE:

\[
(p_B H')' + q_B H = 0.
\]

Set $t = 2 \sqrt{\Lambda_B u}$, or maybe use a transformation to put into Bessel form with a small perturbation. The plan is to compare with Bessel equation $J_\alpha'' + (1/t) J_\alpha' + (1 - \alpha^2/t^2) J_\alpha = 0$ or similar. But we need the transformed equation.

From A1-Strategy: "After the Liouville-Bessel change $t=2\sqrt{\Lambda_B u}$, rewrite the endpoint equation as a Bessel equation plus a perturbation." Let's derive the transformed ODE.

Define $t = 2\sqrt{\Lambda_B u}$, so $u = t^2/(4 \Lambda_B)$. Then $du = (t/(2\Lambda_B)) dt$.

Compute $H'(u) = \frac{dH}{dt} \frac{dt}{du} = H_t \cdot \frac{du}{dt}^{-1} = H_t \cdot \frac{2\Lambda_B}{t}$.

Then $p_B(u) H'(u) = u(1-u/B) \cdot \frac{2\Lambda_B}{t} H_t$. Write $u = t^2/(4\Lambda_B)$. So $p_B H' = \frac{t^2}{4\Lambda_B} (1 - \frac{t^2}{4B\Lambda_B}) \cdot \frac{2\Lambda_B}{t} H_t = \frac{t}{2} (1 - \frac{t^2}{4B\Lambda_B}) H_t$.

Then the term $(p_B H')'$: differentiate with respect to $u$: $\frac{d}{du}(p_B H') = \frac{d}{dt}(p_B H') \cdot \frac{dt}{du} = \frac{d}{dt}\left( \frac{t}{2} (1 - \frac{t^2}{4B\Lambda_B}) H_t \right) \cdot \frac{2\Lambda_B}{t}$. This yields a second-order operator. The resulting ODE will have a leading term $H_{tt} + \frac{1}{t} H_t + \dots$ leading to Bessel. The perturbation will include terms with $1/B$ and $u/B$ small. Since $u \le n$, $u/B \le n/B$, which is small when $B$ large. But we need explicit bounds.

As an algebra checker, I should verify that the constant $\Lambda_B$ is chosen such that the coefficient of $H$ matches the Bessel potential. In the Bessel equation $y'' + \frac{1}{t} y' + (1 - \frac{\alpha^2}{t^2}) y = 0$, the standard form is $(t y')' + (t - \frac{\alpha^2}{t}) y = 0$. Our ODE will be similar.

Let's transform the endpoint ODE to $t$ variable. Starting from $(p_B H')' + q_B H = 0$. Write $p_B(u) = u(1 - \epsilon u)$ with $\epsilon = 1/B$. Then compute $H'(u) = H_t \cdot \frac{dt}{du} = H_t \cdot \frac{2\Lambda_B}{t}$.

Then $(p_B H')' = \frac{d}{du}\left( u(1-\epsilon u) H'(u) \right) = \frac{d}{dt}\left( \frac{t^2}{4\Lambda_B} (1 - \frac{\epsilon t^2}{4\Lambda_B}) \cdot \frac{2\Lambda_B}{t} H_t \right) \cdot \frac{2\Lambda_B}{t}$.

Simplify the inner: $\frac{t^2}{4\Lambda_B} \cdot \frac{2\Lambda_B}{t} = \frac{t}{2}$. So inner product is $\frac{t}{2} (1 - \frac{\epsilon t^2}{4\Lambda_B}) H_t$.

Now differentiate w.r.t $t$:

$\frac{d}{dt} [ \frac{t}{2} (1 - \frac{\epsilon t^2}{4\Lambda_B}) H_t ] = \frac{1}{2} (1 - \frac{\epsilon t^2}{4\Lambda_B}) H_t + \frac{t}{2} ( - \frac{2 \epsilon t}{4\Lambda_B} ) H_t + \frac{t}{2} (1 - \frac{\epsilon t^2}{4\Lambda_B}) H_{tt}$.

Simplify: $ - \frac{t}{2} \cdot \frac{ \epsilon t }{2\Lambda_B} H_t = - \frac{\epsilon t^2}{4\Lambda_B} H_t$.

Thus derivative becomes $\frac{1}{2} (1 - \frac{\epsilon t^2}{4\Lambda_B}) H_t - \frac{\epsilon t^2}{4\Lambda_B} H_t + \frac{t}{2} (1 - \frac{\epsilon t^2}{4\Lambda_B}) H_{tt} = \frac{1}{2} (1 - \frac{\epsilon t^2}{2\Lambda_B}) H_t + \frac{t}{2} (1 - \frac{\epsilon t^2}{4\Lambda_B}) H_{tt}$.

Now multiply by $dt/du = 2\Lambda_B/t$ to get $(p_B H')'$:

$(p_B H')' = \frac{2\Lambda_B}{t} \left[ \frac{1}{2} (1 - \frac{\epsilon t^2}{2\Lambda_B}) H_t + \frac{t}{2} (1 - \frac{\epsilon t^2}{4\Lambda_B}) H_{tt} \right] = \frac{\Lambda_B}{t} (1 - \frac{\epsilon t^2}{2\Lambda_B}) H_t + \Lambda_B (1 - \frac{\epsilon t^2}{4\Lambda_B}) H_{tt}$.

Now add $q_B H$. We'll express $q_B$ in terms of $t$. Recall $q_B(u) = c_B - \frac{(r_B u - \alpha)^2}{4u(1-u/B)}$. With $u = t^2/(4\Lambda_B)$.

First compute $r_B u - \alpha = r_B \frac{t^2}{4\Lambda_B} - \alpha$.

And $4 u (1 - u/B) = 4 \frac{t^2}{4\Lambda_B} (1 - \frac{t^2}{4B\Lambda_B}) = \frac{t^2}{\Lambda_B} (1 - \frac{t^2}{4B\Lambda_B})$.

Thus $\frac{ (r_B u - \alpha)^2 }{4 u (1-u/B) } = \frac{ (r_B \frac{t^2}{4\Lambda_B} - \alpha)^2 }{ \frac{t^2}{\Lambda_B} (1 - \frac{t^2}{4B\Lambda_B}) } = \frac{ \Lambda_B (r_B \frac{t^2}{4\Lambda_B} - \alpha)^2 }{ t^2 (1 - \frac{t^2}{4B\Lambda_B}) }$.

So $q_B(u) = c_B - \frac{ \Lambda_B ( \frac{r_B}{4\Lambda_B} t^2 - \alpha )^2 }{ t^2 (1 - \frac{t^2}{4B\Lambda_B}) }$.

Simplify inside: $\frac{r_B}{4\Lambda_B} t^2 = \frac{r_B}{4\Lambda_B} t^2$. So $\frac{r_B}{4\Lambda_B} t^2 - \alpha = \alpha ( \frac{r_B}{4\alpha\Lambda_B} t^2 - 1 )$. Not helpful.

But the idea is that the ODE becomes, after multiplying by something, a Bessel equation perturbed. Actually the standard Bessel equation for $J_\alpha(t)$ in self-adjoint form is $(t y')' + (t - \alpha^2/t) y = 0$. Our equation after transformation will have leading coefficient $t$ times something.

Let's rewrite the full ODE in $t$:

\[
\Lambda_B (1 - \frac{t^2}{4B\Lambda_B}) H_{tt} + \frac{\Lambda_B}{t} (1 - \frac{t^2}{2B\Lambda_B}) H_t + q_B H = 0.
\]

Multiply by $t$ to get a self-adjoint-like form:

\[
\Lambda_B t (1 - \frac{t^2}{4B\Lambda_B}) H_{tt} + \Lambda_B (1 - \frac{t^2}{2B\Lambda_B}) H_t + t q_B H = 0.
\]

This doesn't look exactly like $(t H_t)_t$ due to the $(1 - \dots)$ factors. But we can write:

\[
\frac{d}{dt}\left( t (1 - \frac{t^2}{4B\Lambda_B}) H_t \right) = t (1 - \frac{t^2}{4B\Lambda_B}) H_{tt} + (1 - \frac{3t^2}{4B\Lambda_B}) H_t.
\]

Our expression has $\Lambda_B [ t (1 - \frac{t^2}{4B\Lambda_B}) H_{tt} + (1 - \frac{t^2}{2B\Lambda_B}) H_t ]$. So we need to adjust.

Compute $t (1 - \epsilon_1 t^2) H_{tt} + (1 - \epsilon_2 t^2) H_t$ vs $(t (1 - \epsilon_3 t^2) H_t)'$. The derivative gives $ (1 - 3 \epsilon_3 t^2) H_t + t(1 - \epsilon_3 t^2) H_{tt}$. So to match, we need $1 - \epsilon_2 t^2 = 1 - 3\epsilon_3 t^2$, i.e., $\epsilon_2 = 3\epsilon_3$, and $\epsilon_1 = \epsilon_3$. But we have $\epsilon_1 = \frac{1}{4B\Lambda_B}$, $\epsilon_2 = \frac{1}{2B\Lambda_B} = 2 \epsilon_1$. That does not match the derivative relation. So we cannot combine into a single term. This means the ODE is not a simple Bessel equation with a small parameter multiplying a perturbation. We may need to transform further.

The A1-Strategy says "after the Liouville-Bessel change $t=2\sqrt{\Lambda_B u}$, rewrite the endpoint equation as a Bessel equation plus a perturbation." This might involve applying a further transformation like $H = t^\alpha \tilde H$ or something to get Bessel. The exact approach: The endpoint equation is similar to the Laguerre equation. The Laguerre equation itself transforms under $t=2\sqrt{\lambda u}$ and $y = u^{-\alpha/2} e^{u/2} \ell$ to a Bessel equation? Actually the standard Laguerre equation $(u y')' + (\lambda - u/4 - \alpha^2/(4u)) y = 0$. If we set $t = 2\sqrt{\lambda u}$ and $y(u) = t^\alpha v(t)$, we can get Bessel. That is known. For the finite-$B$ endpoint equation, we have $p_B(u) = u(1-u/B)$, which adds a small perturbation. So the plan is to treat the $u/B$ terms as perturbation and compare with the limiting Laguerre case. The A1-Strategy's Bessel model $\mathcal B_{n,\alpha,B}(u) = M_{n,\alpha,B} J_\alpha(2\sqrt{\Lambda_B u})$ is the Bessel function that approximates $H(u)$ for the Laguerre limit $B\to\infty$. For finite $B$, there will be a perturbation.

So as algebra checker, I should verify that the limiting Laguerre equation indeed gives the Bessel function relationship. And that the Bessel normalization $M_{n,\alpha,B}$ correctly matches the leading Frobenius behavior. We already verified that $M_{n,\alpha,B}$ yields the correct $u^{\alpha/2}$ asymptote. For the full solution, the Bessel function is an exact solution to the equation where $p_B(u)$ is replaced by $u$ and $q_B(u)$ by the Laguerre limit $q_\infty(u) = n + \frac{\alpha+1}{2} - \frac{u}{4} - \frac{\alpha^2}{4u}$? Actually check the Laguerre limit: as $B\to\infty$, $r_B \to 1$, $c_B \to n + 1/2$, $\Lambda_B \to n + (\alpha+1)/2$, and $p_B(u) = u(1-u/B) \to u$. Then $q_B(u) \to n + \frac{\alpha+1}{2} - \frac{(u - \alpha)^2}{4u} = n + \frac{\alpha+1}{2} - \frac{u^2 - 2\alpha u + \alpha^2}{4u} = n + \frac{\alpha+1}{2} - \frac{u}{4} + \frac{\alpha}{2} - \frac{\alpha^2}{4u} = n + \alpha + \frac12 - \frac{u}{4} - \frac{\alpha^2}{4u}$? Wait compute: $\frac{(u-\alpha)^2}{4u} = \frac{u^2 - 2\alpha u + \alpha^2}{4u} = \frac{u}{4} - \frac{\alpha}{2} + \frac{\alpha^2}{4u}$. So $c_B - \frac{(r_B u - \alpha)^2}{4u} \to (n + 1/2) - ( \frac{u}{4} - \frac{\alpha}{2} + \frac{\alpha^2}{4u}) = n + \frac{\alpha+1}{2} - \frac{u}{4} - \frac{\alpha^2}{4u}$. Yes, that's the Laguerre potential. The Bessel function $J_\alpha(2\sqrt{\Lambda_\infty u})$ with $\Lambda_\infty = n + \frac{\alpha+1}{2}$ solves the equation where the term $u/4$ is dropped? Actually the Bessel equation $y'' + \frac{1}{t} y' + (1 - \frac{\alpha^2}{t^2}) y = 0$ after $t = 2\sqrt{\lambda u}$ and $y \propto t^\alpha$ or something yields a solution to $(u y')' + (\lambda - \frac{\alpha^2}{4u}) y = 0$, missing the $-u/4$ term. So the Bessel fundamental set solves the equation without the $-u/4$ term. The actual Laguerre limit includes $-u/4$, which causes the turning point and decay. But for small $u$ (near origin), the $-u/4$ term is small compared to $\lambda$, so Bessel is a good approximation for the first lobe. This is typical: the Bessel equation is the "inner approximation".

Thus the A1-Strategy's Bessel model is appropriate only for $u$ small, up to the first maximum. The perturbation analysis must bound the effect of the $-u/4$ term (and the $u/B$ corrections) on the first lobe. So the "Bessel perturbation theorem" is indeed not an exact transformation but an approximation. The A1-Strategy says: "after the Liouville-Bessel change $t=2\sqrt{\Lambda_B u}$, rewrite the endpoint equation as a Bessel equation plus a perturbation. Olver variation-of-constants estimates should give the computable bound $|R_B(u)|\le C_B/(n+1)$". So the exact ODE would be something like $\mathcal{L} H = 0$, and we compare with $\mathcal{L}_0 \mathcal{B} = 0$ where $\mathcal{L}_0$ is Bessel operator, and the difference is a perturbation. This is standard in asymptotic analysis of ODEs. So the algebra to verify is the exact form of the perturbed operator.

We can derive the perturbed Bessel equation for $W(t)$ related to $H(u)$. For the Laguerre limit, one often sets $t = 2\sqrt{\lambda u}$ and $H(u) = (t/2)^{\alpha} e^{-t^2/(8\lambda)}? No. Let's recall the standard connection: The Laguerre polynomial $L_n^{(\alpha)}(u)$ satisfies $u y'' + (\alpha+1-u) y' + n y = 0$. The normalized function $\ell_n^{(\alpha)}(u) = c u^{\alpha/2} e^{-u/2} L_n^{(\alpha)}(u)$ satisfies $u \ell'' + \ell' + ( \lambda - u/4 - \alpha^2/(4u) ) \ell = 0$. Then with $t = 2\sqrt{\lambda u}$ and $\ell = t^{\alpha+1/2} e^{-t^2/(8\lambda)} \tilde\ell$? Actually there are many transformations. The Bessel function emerges if you drop the $u/4$ term and use $\ell \approx J_\alpha(2\sqrt{\lambda u})$. More precisely, the equation $(u \ell')' + (\lambda - u/4 - \alpha^2/(4u)) \ell = 0$ under $t = 2\sqrt{\lambda u}$, $\ell(u) = t^{\alpha} v(t)$ leads to $t^2 v'' + t v' + (t^2 - \alpha^2) v - \frac{t^4}{16 \lambda^2} v = 0$. So the extra term is $-\frac{t^4}{16\lambda^2} v$, a perturbation of Bessel. For the finite $B$ case, there will be additional perturbations.

So the algebra checking here is to confirm that the transformation to $t$ and some scaling yields an ODE with principal part Bessel plus small remainders that can be bounded. I can write the transformed ODE explicitly.

Let's do the transformation: Let $H(u) = u^{\alpha/2} Y(u)$. Then the regular solution has $Y(0)$ finite. The ODE for $Y$? Probably standard to reduce to a form without the singularity. Alternatively, set $H(u) = t^{\alpha} Z(t)$ with $t = 2\sqrt{\Lambda_B u}$. Let's try that.

We have endpoint ODE: $(p_B H')' + q_B H = 0$, with $p_B = u (1 - u/B)$.

Let $u = t^2/(4\Lambda_B)$. Then $du = t/(2\Lambda_B) dt$.

Compute $H'(u) = dH/dt * dt/du = H_t \frac{2\Lambda_B}{t}$.

Then $p_B H' = u(1-u/B) H' = \frac{t^2}{4\Lambda_B} (1 - \frac{t^2}{4B\Lambda_B}) \cdot \frac{2\Lambda_B}{t} H_t = \frac{t}{2} (1 - \frac{t^2}{4B\Lambda_B}) H_t$.

Now, to get a Bessel equation, we often set $H(t) = t^{\alpha} Z(t)$ so that the $1/t$ term becomes manageable. Compute derivatives: $H_t = \alpha t^{\alpha-1} Z + t^{\alpha} Z_t$. Then $H_{tt} = \alpha(\alpha-1) t^{\alpha-2} Z + 2\alpha t^{\alpha-1} Z_t + t^{\alpha} Z_{tt}$.

Now plug into the ODE. But perhaps simpler: we can try to eliminate the first derivative term. Standard method: define new dependent variable $W(t)$ such that the coefficient of $W_t$ becomes something nice. For the Bessel equation, the normal form is $(t W_t)_t + (t - \frac{\nu^2}{t}) W = 0$. After setting $W = t^{\nu} Z$, we get $t Z_t + \nu Z$ etc. Actually the self-adjoint Bessel equation is $(t y')' + (t - \frac{\nu^2}{t}) y = 0$. If we let $y = t^{\nu} v$, then $t y' = t^{\nu+1} v' + \nu t^{\nu} v$, and $(t y')' = t^{\nu+1} v'' + (\nu+1) t^{\nu} v' + \nu^2 t^{\nu-1} v + \nu(\nu+1) t^{\nu-1} v? Wait, do properly: $y = t^{\nu} v$, then $y' = \nu t^{\nu-1} v + t^{\nu} v'$. Multiply by $t$: $t y' = \nu t^{\nu} v + t^{\nu+1} v'$. Then derivative: $(t y')' = \nu^2 t^{\nu-1} v + \nu t^{\nu} v' + (\nu+1) t^{\nu} v' + t^{\nu+1} v'' = \nu^2 t^{\nu-1} v + (2\nu+1) t^{\nu} v' + t^{\nu+1} v''$. Then the equation becomes $t^{\nu+1} v'' + (2\nu+1) t^{\nu} v' + \nu^2 t^{\nu-1} v + (t - \frac{\nu^2}{t}) t^{\nu} v = 0$. Multiply by $t^{1-\nu}$: $t^2 v'' + (2\nu+1) t v' + \nu^2 v + (t^2 - \nu^2) v = t^2 v'' + (2\nu+1) t v' + t^2 v = 0$. So $t^2 v'' + (2\nu+1) t v' + t^2 v = 0$. That's not simpler.

Better to keep the self-adjoint form. Our transformed ODE, before any substitution, is something like:

Let's just compute the full ODE in $t$ with $H$ directly, but try to rewrite as $(t (1 - \frac{t^2}{4B\Lambda_B}) H_t)_t + \dots = 0$. Actually from earlier we had:

$(p_B H')' = \frac{\Lambda_B}{t} (1 - \frac{t^2}{2B\Lambda_B}) H_t + \Lambda_B (1 - \frac{t^2}{4B\Lambda_B}) H_{tt}$.

Now define $P(t) = t (1 - \frac{t^2}{4B\Lambda_B})$. Then $P'(t) = 1 - \frac{3t^2}{4B\Lambda_B}$. And $(P H_t)_t = P H_{tt} + P' H_t = t (1 - \frac{t^2}{4B\Lambda_B}) H_{tt} + (1 - \frac{3t^2}{4B\Lambda_B}) H_t$.

We have $\frac{\Lambda_B}{t} (1 - \frac{t^2}{2B\Lambda_B}) H_t + \Lambda_B (1 - \frac{t^2}{4B\Lambda_B}) H_{tt} = \Lambda_B \left[ (1 - \frac{t^2}{4B\Lambda_B}) H_{tt} + \frac{1}{t} (1 - \frac{t^2}{2B\Lambda_B}) H_t \right]$.

Multiply the $(P H_t)_t$ by $\frac{\Lambda_B}{t}$? Not quite.

Let's compute $( \frac{\Lambda_B}{t} P H_t )_t = \frac{\Lambda_B}{t} (P H_{tt} + P' H_t) - \frac{\Lambda_B}{t^2} P H_t = \frac{\Lambda_B}{t} P H_{tt} + \frac{\Lambda_B}{t} (P' - \frac{P}{t}) H_t$.

Now $P = t (1 - \epsilon t^2)$ with $\epsilon = 1/(4B\Lambda_B)$. Then $P' = 1 - 3\epsilon t^2$. So $P' - P/t = (1 - 3\epsilon t^2) - (1 - \epsilon t^2) = -2\epsilon t^2$. So $( \frac{\Lambda_B}{t} P H_t )_t = \Lambda_B (1 - \epsilon t^2) H_{tt} + \frac{\Lambda_B}{t} (-2\epsilon t^2) H_t = \Lambda_B (1 - \epsilon t^2) H_{tt} - 2\Lambda_B \epsilon t H_t$.

But our ODE has $H_t$ coefficient: $\frac{\Lambda_B}{t} (1 - \frac{t^2}{2B\Lambda_B})$. Note that $\frac{t^2}{2B\Lambda_B} = 2 \epsilon t^2$. So $\frac{\Lambda_B}{t} (1 - 2\epsilon t^2) = \frac{\Lambda_B}{t} - 2\Lambda_B \epsilon t$. So the $H_t$ term is $\frac{\Lambda_B}{t} H_t - 2\Lambda_B \epsilon t H_t$. That matches the derivative of $\frac{\Lambda_B}{t} P H_t$ plus a $\frac{\Lambda_B}{t} H_t$ remainder? Let's see:

$\frac{\Lambda_B}{t} P H_t$ derivative gave $\Lambda_B (1 - \epsilon t^2) H_{tt} - 2\Lambda_B \epsilon t H_t$. The $H_t$ term is exactly $-2\Lambda_B \epsilon t H_t$. So to get the full $H_t$ term in the ODE, we need an extra $\frac{\Lambda_B}{t} H_t$. That extra term can be obtained from $(\Lambda_B \ln t H_t)_t$? Actually $\frac{d}{dt}(\Lambda_B \ln t \cdot H_t)$? Not helpful.

Instead, note that the ODE can be written as:

\[
\frac{d}{dt}\left( \frac{\Lambda_B}{t} P(t) H_t \right) + \frac{\Lambda_B}{t} H_t + q_B H = 0,
\]

or something. But the term $\frac{\Lambda_B}{t} H_t$ is $(\Lambda_B \ln t)' H_t$, which is part of $( \Lambda_B \ln t \cdot H_t )_t - \Lambda_B \ln t \cdot H_{tt}$. This seems messy.

However, the standard approach to get Bessel is to first multiply the original endpoint ODE by something to make it self-adjoint with weight, or use a Liouville transformation. Usually, for an equation of the form $(p y')' + q y = 0$, one can use the transformation $t = \int \sqrt{q/p} du$ etc., but that's for WKB. For Bessel comparison near $u=0$, it's simpler: Since $p(u) \sim u$ and $q(u) \sim \Lambda_B / u$ for small $u$ (because $(r_B u - \alpha)^2 \approx \alpha^2$ for small $u$). Indeed, $q(u) \approx c_B - \frac{\alpha^2}{4u}$ for small $u$, and $p(u) \approx u$. Then the equation approximates $(u H')' + (c_B - \frac{\alpha^2}{4u}) H = 0$. This is exactly the Bessel equation after $t = 2\sqrt{c_B u}$ and scaling. So the Bessel model is natural. The A1-Strategy uses $\Lambda_B$ instead of $c_B$; which one is better? $\Lambda_B = c_B + \alpha r_B/2$, which for small $u$ doesn't matter since the $u$ term in $q$ is small. So it's plausible.

Thus the exact perturbation analysis will compare the ODE to the Bessel equation with potential $c_B$ or $\Lambda_B$, and bound the difference. The algebra to check: verify that if we define $H(u) = \mathcal{B}(u) + R(u)$, then the remainder satisfies an inhomogeneous Bessel equation where the forcing term is small. That is standard Olver's method. We don't need to fully derive it now, but we should confirm that the fundamental Bessel equation used as comparison has the correct parameter. The A1-Strategy uses $\Lambda_B$ rather than $c_B$; is that optimal? Possibly to match the small-$u$ behavior up to $O(u)$? Let's check the Frobenius: the characteristic equation for $(u H')' + (c_B - \frac{\alpha^2}{4u}) H = 0$ leads to indices $\pm \alpha/2$. The regular solution $J_\alpha(2\sqrt{c_B u})$ has leading term $(c_B u)^{\alpha/2} / \Gamma(\alpha+1)$. The actual $H$ has leading term $A u^{\alpha/2}$. The ratio $\frac{A}{ (c_B)^{\alpha/2} / \Gamma(\alpha+1) }$ gives the Bessel normalization $M$ if we use $c_B$. But A1-Strategy uses $\Lambda_B$, so the normalization $M_{n,\alpha,B}$ involves $\Lambda_B^{-\alpha/2}$. Since $\Lambda_B = c_B + \alpha r_B/2$, they differ by $O(1)$ terms but not asymptotically small. The choice of $\Lambda_B$ might be because the $q_B$ contains a linear term that shifts the effective "frequency". In the Sonin functional, we had $K_B'(u) = \Lambda_B - 2\Delta_B u$, so $\Lambda_B$ is the derivative at $u=0$. For small $u$, $K_B(u) \approx \Lambda_B u$, so the equation near $u=0$ has $K_B(u) \approx \Lambda_B u$, and the coefficient of $H$ is $q_B \approx K_B/(p_B) \approx \Lambda_B$? Actually $p_B \approx u$, so $q_B \approx \Lambda_B / u$? No, $q_B = K_B / p_B \approx (\Lambda_B u) / u = \Lambda_B$, not $1/u$. Wait earlier I said $q(u) \sim \Lambda_B / u$? Let's recalc: $K_B(u) = -\alpha^2/4 + \Lambda_B u + O(u^2)$. $p_B(u) = u + O(u^2)$. So $q_B = K_B/p_B = \frac{-\alpha^2/4 + \Lambda_B u}{u} (1+O(u)) = -\frac{\alpha^2}{4u} + \Lambda_B + O(u)$. So indeed $q_B \sim -\alpha^2/(4u) + \Lambda_B$. So the dominant balance is $-\alpha^2/(4u)$ and constant $\Lambda_B$. So the Bessel equation with potential $\Lambda_B$ is natural: $(u H')' + ( \Lambda_B - \frac{\alpha^2}{4u} ) H = 0$. That's the Bessel equation, solutions are $J_\alpha(2\sqrt{\Lambda_B u})$ and $Y_\alpha$. So the choice of $\Lambda_B$ matches the small-$u$ behavior of $q_B$ up to the constant term, since $c_B$ vs $\Lambda_B$ differ by $\alpha r_B/2$, which is $O(1)$ but not negligible. However, $c_B$ includes only $n + r_B/2$, while $\Lambda_B = n + \frac{\alpha+1}{2} r_B$. For $\alpha>0$, $\Lambda_B > c_B$. Which one yields better approximation? The exact equation at $u=0$ has $q_B(0) = \infty$, but the regular solution's series expansion will involve both. The Bessel function $J_\alpha(2\sqrt{\Lambda_B u})$ has a series where the constant term in the potential $\Lambda_B$ controls the "effective frequency". I think the choice of $\Lambda_B$ is correct because it makes the approximate equation's turning point match? Not sure. But as an algebra checker, I should note that the A1-Strategy's choice is $\Lambda_B$, and it leads to the Bessel model. We can test numerically for some parameters whether $J_\alpha(2\sqrt{\Lambda_B u})$ is closer to $H(u)$ than using $c_B$. But that's numeric.

For the proof, the perturbation bound $|R_B(u)| \le C_B/(n+1)$ must be derived, and the constants must be valid for both choices as long as the remainder is bounded appropriately. The importance is that the Bessel maximum $B_*<0.680$ and the normalization factor $M_{n,\alpha,B}$ are defined with $\Lambda_B$. So I'll accept the A1-Strategy definitions.

Now, as part of the algebra audit, I should verify the Bessel normalization expression $M_{n,\alpha,B} = \left\{ \frac{\Gamma(B) \Gamma(n+\alpha+1)} {\Gamma(B-\alpha) \Gamma(n+1)} \right\}^{1/2} (B \Lambda_B)^{-\alpha/2}$ simplifies to something near 1. The A1-Strategy says the gamma-ratio should be bounded by $1 + C_\Gamma/(n+1)$. We need to check if that is plausible. The ratio inside the square root: $\frac{\Gamma(B) \Gamma(n+\alpha+1)}{\Gamma(B-\alpha) \Gamma(n+1)}$. As $B \to \infty$ (large $\beta$), $\frac{\Gamma(B)}{\Gamma(B-\alpha)} \sim B^\alpha$. So the ratio $\sim B^\alpha \cdot \frac{\Gamma(n+\alpha+1)}{\Gamma(n+1)}$. Then $M \sim \sqrt{ B^\alpha \frac{\Gamma(n+\alpha+1)}{\Gamma(n+1)} } \cdot (B \Lambda_B)^{-\alpha/2} = \left( \frac{ \Gamma(n+\alpha+1) }{ \Gamma(n+1) \Lambda_B^\alpha } \right)^{1/2}$. And $\Lambda_B \approx n + \frac{\alpha+1}{2}$ for large $B$. For large $n$, this tends to 1? Actually we need to bound it. The A1-Strategy's bound $1 + C_\Gamma/(n+1)$ suggests that for large $n$, $M$ is close to 1. This could be shown using Stirling or Gautschi's inequality. So that's a lemma to develop.

Now the Bessel maximum $B_*<0.680$: we need to verify that supremum of $|J_\nu(t)|$ for $\nu \ge 1/2$, $t\ge0$ is less than 0.680. The maximum of $J_0$ is about 1, but for $\nu \ge 1/2$, the maximum decreases. The known bound is $\sup_{t\ge0} |J_\nu(t)| \le c \nu^{-1/3}$ with $c \approx 0.6749$ (Landau). For $\nu\ge 1/2$, the maximum is $J_{1/2}(t) = \sqrt{2/\pi t} \sin t$, whose supremum is $\sqrt{2/\pi} \approx 0.7979$ at $t$ near? Actually $J_{1/2}(t) = \sqrt{2/(\pi t)} \sin t$. Its global maximum is not at $t=0$; it's about $0.7979$ at $t$ near $\pi/2$? Wait $\sqrt{2/\pi t} \sin t$; the maximum of $\sin t / \sqrt{t}$ on $(0,\infty)$ occurs at $t \approx 0.66$? Let's compute: $f(t) = \sin t / \sqrt{t}$. derivative zero => $\cos t / \sqrt{t} - (1/2) \sin t / t^{3/2} = 0 \Rightarrow \cos t = (1/2) \sin t / t \Rightarrow t = (1/2) \tan t$. Solving numerically gives $t \approx 0.6533$, $f(t) \approx 0.7979$. So $0.7979 > 0.680$. So the A1-Strategy's claim that $B_*<0.680$ for $\nu\ge 1/2$ is false for $\nu=1/2$. But maybe they consider $\nu \ge 1$? The A1-Strategy says $\nu\ge 1/2$, but then the bound $0.680$ is wrong. However, A1-Strategy also says "A convenient route is a Landau-type bound for $\nu\ge1$, plus rigorous interval verification for $1/2\le\nu\le1$." That suggests that the overall $B_*$ for $\nu\ge 1/2$ is actually higher, but they plan to handle $\nu$ near $1/2$ separately, and for $\nu\ge1$ the bound is smaller. So the claim $B_*<0.680$ is likely meant for $\nu\ge1$ or after some adjustment. Need to check the context: In A1-Strategy Phase 6, they write $B_*=\sup_{\nu\ge1/2,\ t\ge0}|J_\nu(t)|<0.680$. That is mathematically false if taken literally. However, perhaps they are considering the effective $\nu$ being $\alpha$ which is in $[1/2, \alpha_E]$, and they intend to use the bound for the Bessel model, but the Bessel function in the model has order $\alpha$, and for $\alpha=1/2$, the maximum is $~0.7979$, which exceeds $0.680$. So the constant $0.680$ cannot be a uniform bound for $\alpha\ge1/2$. They must be using a different Bessel function? Wait, the Bessel model uses $J_\alpha$, not $J_\nu$ with $\nu$ shifted. Actually the Bessel model is $J_\alpha(2\sqrt{\Lambda_B u})$, so the order is $\alpha$. For $\alpha=1/2$, the maximum of $|J_{1/2}(t)|$ is $0.7979$, which is less than 1 but greater than $0.680$. The target bound is $|H| < 2^{-1/4} \approx 0.8409$. So even $0.7979$ is below $0.8409$, but they need $B_* +$ perturbation $< 2^{-1/4}$. They propose $B_*<0.680$ which would give more slack. If $B_*$ is actually $0.7979$, the perturbation bound must be smaller to still be below $0.8409$. So the claim $B_*<0.680$ is an overstatement. However, the A1-Strategy's plan includes "a Landau-type bound for $\nu\ge1$, plus rigorous interval verification for $1/2\le\nu\le1$." That indicates they will verify the maximum directly, not rely on $0.680$. So the statement "$B_*<0.680$" might be a placeholder for the bound they will prove after verification. For $\alpha \in [1/2, \alpha_E]$, the actual sup of $|J_\alpha(t)|$ over $t\ge0$ is indeed $<0.80$ for $\alpha=1/2$, and decreases. So the first-lobe theorem could use a verified bound for the Bessel maximum that is below $0.8409$. The number $0.680$ might be for $\alpha\ge1$. So as algebra checker, I should note this discrepancy: the claimed $B_*<0.680$ is not true for $\alpha=1/2$, but the overall plan is to get a bound less than $2^{-1/4}$ anyway. I should flag this as a potential gap or overstatement. But the bootstrap judge itself adopted the A1-Strategy's expression $B_*<0.680$ without comment; maybe they assume the bound is after some transformation where the Bessel function is scaled? Or maybe they are using a different normalization that reduces the maximum. Actually the Bessel model $\mathcal B(u) = M J_\alpha(2\sqrt{\Lambda_B u})$ is multiplied by $M$ which might be less than 1, and the final bound on $H$ is $|H| \le M \sup |J_\alpha| + \text{error}$. So they need the product $M \times \sup|J_\alpha|$ to be small. The A1-Strategy's Theorem statement: $|H| \le (1 + C_\Gamma/(n+1)) ( B_* + C_B/(n+1) )$, and they require this $< 2^{-1/4}$. So $B_*$ is the supremum of the normalization factor times the Bessel max? Wait, they defined $B_* = \sup_{\nu\ge1/2, t\ge0} |J_\nu(t)|$. That is independent of $M$. So indeed $B_* \approx 0.7979$ for $\nu=1/2$. Then the product $(1+...)(B_*+...)$ must be less than $0.8409$. That is plausible if $C_B$ and $C_\Gamma$ small. But the statement $B_*<0.680$ is false. However, maybe they intend to bound $B_*$ by $0.680$ using a known result for $\nu \ge \nu_0$ where $\nu_0>1/2$. Since the strip includes $\alpha$ up to $\alpha_E$ which is $\sim n$, for large $n$ $\alpha$ could be large, then the Bessel maximum is indeed small. But for small $n$, $\alpha$ might be $1/2$, and then $B_*$ is larger. The finite verification for small $n$ could cover those cases, and for large $n$, $\alpha$ will be large enough that $B_* < 0.680$. So the claim might be that for $n\ge N_0$, $\alpha$ is large enough (say $\alpha \ge 1$) so that $B_*<0.680$. The A1-Strategy's residual strip is $\frac12 \le \alpha \le \alpha_E(n)$. They aim to prove the first-lobe theorem for all $n\ge N_0$, but they will need a uniform bound on $B_*$ that works for all $\alpha$ in that range. If for some $n\ge N_0$, $\alpha$ could be as small as $1/2$, unless $N_0$ is chosen so that for $n\ge N_0$, $\alpha_E(n) \ge 1$ but the lower bound is still $1/2$. So $\alpha$ can be $1/2$ for any $n$. So the Bessel maximum for $\alpha=1/2$ will always be $~0.7979$. So the bound $B_*<0.680$ cannot hold uniformly unless they exclude $\alpha$ near $1/2$ by some other argument (like the small endpoint theorem already covers $\alpha\le1/2$, so in the residual strip we only need $\alpha>1/2$ strictly). For $\alpha$ slightly above $1/2$, the maximum of $J_\alpha$ is slightly less than $0.7979$, but still could be >0.68. So a uniform bound $<0.680$ is not obvious. However, they might use a different $B_*$: maybe they consider the function $\tilde J_\alpha(t) = \sqrt{t} J_\alpha(t)$ or something? Actually the Bessel model $\mathcal B(u) = M_{n,\alpha,B} J_\alpha(2\sqrt{\Lambda_B u})$, and the factor $M$ itself might be small for $\alpha$ near $1/2$? Not necessarily. So this is a potential gap in the constant. But as an auditor, I should point out that the claimed $B_*<0.680$ is not generally true for $\alpha\ge1/2$; a more accurate bound is $0.7979$ for $\alpha=1/2$, decreasing for larger $\alpha$. The A1-Strategy must incorporate this. Actually reading A1-Strategy more carefully: "A convenient route is a Landau-type bound for $\nu\ge1$, plus rigorous interval verification for $1/2\le\nu\le1$." So they intend to bound the Bessel maximum by a rigorous interval verification (i.e., computed bound) for $\nu$ between $0.5$ and $1$. That verification could yield a number like $0.80$ or something, not $0.680$. The $B_*$ constant $0.680$ might be the Landau bound for $\nu\ge1$. So the overall $B_*$ in the theorem would then be the larger of the two, say $0.80$. But they wrote $B_*<0.680$ perhaps as an optimistic placeholder. The bootstrap judge didn't catch this. So I'll note it.

Now, the finite-$\beta$ bridge: the bootstrap judge lists G5 as a fallback theorem. The A1-Strategy's main route does not need a bridge because it works directly with finite $B$. So the fallback bridge is only needed if the direct endpoint theorem fails. So we don't need to focus heavily on that now.

Now, as A3, my job is to "produce a symbolic/numeric check plan for the formulas, but do not replace proof with sampling." So I'll outline a plan for verifying the algebraic derivations symbolically (using computer algebra, e.g., Mathematica/Maple) and also numeric sanity checks for the constants and inequalities. I'll also note any missing steps.

Now, I need to produce the output in the required Stage A format: Summary, Main claim or direction, Detailed reasoning, Dependencies, Potential gaps, Counterexample or obstruction search, Useful lemmas, What should be tested next, Confidence.

I need to focus on the algebra and endpoint reduction, not on broader strategy.

So my response will include:

- I have independently verified the transformation to endpoint ODE: $(p_B H')' + q_B H = 0$, with $p_B = u(1-u/B)$, $q_B = c_B - (r_B u - \alpha)^2/[4u(1-u/B)]$, $r_B = s/B = 1 - (n+1)/B$, $c_B = n + r_B/2$. Derived $K_B = p_B q_B = -\alpha^2/4 + \Lambda_B u - \Delta_B u^2$, with $\Lambda_B = c_B + \alpha r_B/2$, $\Delta_B = c_B/B + r_B^2/4$. All formulas certified.

- Verified $u_\sigma = nB/(B+n-1)$ and $0 \le u_\sigma \le n$ for $n\ge1$.

- Verified the monotonicity inequality $K_B'(u) \ge 1/4$ for $\alpha \ge 1/2$, $0\le u\le u_\sigma$, using the simplified expression $K_B'(u_\sigma) = 1/4 + \frac14(2\alpha+2n+1 - \frac{2(n+1)(n+\alpha+1)}{B})$ and noting that $B \ge n+\alpha+1$ implies $K_B'(u_\sigma) \ge 1/4 + (2\alpha-1)/4 \ge 1/4$. Since $K_B'$ is decreasing in $u$, the minimum on the interval is at $u_\sigma$. So the inequality holds.

- Verified the Frobenius coefficient $A_{n,\alpha,B}$ and Bessel normalization $M_{n,\alpha,B}$: $M_{n,\alpha,B} = \left( \frac{\Gamma(B)\Gamma(n+\alpha+1)}{\Gamma(B-\alpha)\Gamma(n+1)} \right)^{1/2} (B\Lambda_B)^{-\alpha/2}$. This matches the small-$u$ asymptotics $H \sim A u^{\alpha/2}$, with $A = \left( \frac{\Gamma(n+1)\Gamma(B)}{\Gamma(n+\alpha+1)\Gamma(B-\alpha)} \right)^{1/2} B^{-\alpha/2} \frac{\Gamma(n+\alpha+1)}{\Gamma(\alpha+1)\Gamma(n+1)}$, and $M = A \Gamma(\alpha+1) \Lambda_B^{-\alpha/2}$. Calculations consistent.

- Checked the Bessel model equation: For the approximate equation $(u H')' + (\Lambda_B - \alpha^2/(4u)) H = 0$, the solution proportional to $J_\alpha(2\sqrt{\Lambda_B u})$ indeed has Frobenius behavior $( \Lambda_B^{\alpha/2} / \Gamma(\alpha+1) ) u^{\alpha/2}$, so the normalization matches.

- Pointed out that the constant $B_*$ in the first-lobe theorem as stated ($<0.680$ uniformly for $\nu\ge1/2$) is not accurate; the actual sup for $\nu=1/2$ is $\approx 0.7979$. The proof must handle this by either a refined bound for $\nu\ge1/2$ or by noting that the coefficient $M_{n,\alpha,B}$ times the Bessel max plus perturbation yields a bound below $2^{-1/4}$. So the statement needs correction or clarification.

- The endpoint Sonin reduction (G1) is plausible but needs rigorous treatment: sign of $K_B$ in $[u_1, u_\sigma]$, existence of $u_1$, handling cases where $u_1 > u_\sigma$. The monotonicity of $S_B$ relies on $K_B > 0$ on the interval. We know $K_B(0) < 0$, and $K_B'(u) \ge 1/4 > 0$, so $K_B$ is increasing, with a single zero $u_0$. For $u > u_0$, $K_B>0$. The maximum point $u_1$ must satisfy $H''(u_1) = - q_B(u_1) H(u_1)$, so $K_B(u_1) = p_B(u_1) q_B(u_1) = -p_B(u_1) H''(u_1)/H(u_1)$. Since $H(u_1) > 0$ and $H''(u_1) < 0$ for a local max, we have $K_B(u_1) > 0$. So $u_1 > u_0$, and $K_B > 0$ on $[u_1, u_\sigma]$? Possibly there's a region where $K_B$ becomes negative again if $u_\sigma$ is beyond the next zero, but $K_B$ is concave quadratic, so after its maximum it could decrease and cross zero again. The quadratic $K_B = -\alpha^2/4 + \Lambda_B u - \Delta_B u^2$, with $\Delta_B>0$, opens downward. So it has two real roots: one near 0 (positive small) and one larger. Since discriminant $\Lambda_B^2 - \Delta_B \alpha^2$ is positive? Let's check: $\Lambda_B^2 - \Delta_B \alpha^2$. For large $B$, $\Lambda_B \approx n$, $\Delta_B \approx 1/4$, so discriminant positive. So $K_B$ has two positive zeros. The larger zero is $u_2 = (\Lambda_B + \sqrt{\Lambda_B^2 - \Delta_B \alpha^2})/(2\Delta_B)$ which could be less than $u_\sigma$? Might be an issue. So the interval where $K_B>0$ might be $u \in (u_0, u_2)$. The cap goes up to $u_\sigma$, which could be larger than $u_2$, and then Sonin functional not defined. However, the central argument already controls the region $x \ge \sigma$ to some extent? Actually if $u_\sigma > u_2$, then for $u \in (u_2, u_\sigma]$, $K_B<0$, and the Sonin argument fails. But maybe in that region the function is small anyway due to central bounds? We need to verify that $u_2 \ge u_\sigma$ for parameters in the residual strip. This is a crucial algebraic check! Let's examine.

$K_B(u) = -\alpha^2/4 + \Lambda_B u - \Delta_B u^2$. For $u$ large (near $B$?), $p_B(u) = u(1-u/B)$ becomes negative? Actually $p_B$ is positive for $0 < u < B$, so the ODE's coefficient $p_B$ is positive there. $q_B$ may become negative causing oscillatory behavior. But $K_B = p_B q_B$ can be negative. For the Sonin functional $S_B = H^2 + p_B^2 (H')^2 / K_B$, we need $K_B > 0$ so that the second term is positive. If $K_B < 0$, the functional is not usable. In standard Sonin-Polya, the interval of positivity of $F = p q$ determines where the solutions are oscillatory and where Sonin monotonicity holds. Specifically, for $(p y')' + q y = 0$, the condition $p q > 0$ is necessary for the Sonin functional $y^2 + p^2 (y')^2 / (p q)$ to be defined. In classical Jacobi, $A(x) = 1-x^2$, $F(x)$ is positive in the oscillatory region between turning points. For the endpoint cap, $p_B >0$, $K_B = p_B q_B$ is the analogue of $F$. We have $K_B(u) > 0$ corresponds to the oscillatory region in $u$. For $x$ close to 1 ($u$ small), $K_B$ is negative (because $-\alpha^2/4$ dominates), causing non-oscillatory behavior (the function is like $u^{\alpha/2}$). Then $K_B$ increases, crosses zero at $u_0$, becomes positive, then eventually turns down and crosses zero again at $u_2$. That's the right turning point of the cap. So $u_2$ is the right turning point in the $u$ variable for the cap problem. The central region ($x<\sigma$) corresponds to $u > u_\sigma$. But we are only interested in $u \le u_\sigma$. So the relevant question: is $u_2 > u_\sigma$? If $u_\sigma$ is less than the outer turning point, then $K_B>0$ on the whole interval $[u_0, u_\sigma]$ (except possibly near $u_0$). If $u_\sigma > u_2$, then for $u > u_2$, the equation becomes non-oscillatory again, and the maximum might be elsewhere. But the central argument already says that for $x \le \sigma$ ($u \ge u_\sigma$), the function is bounded by the central theorem. So if $u_2 < u_\sigma$, then the region $(u_2, u_\sigma]$ is non-oscillatory and the function is monotonic? Actually after the turning point, the solutions become exponential. The maximum of $|H|$ in that region might be at the turning point or beyond. But we already have the central bound at $u=u_\sigma$ which is $\le T$. So we could just use that bound. So the first-lobe reduction might still work if we bound the first lobe and then use Sonin monotonicity on $[u_1, u_2]$ (if $u_1 \le u_2$), and then for $u \in [u_2, u_\sigma]$ we use a separate argument. The A1-Strategy's Sonin reduction claim: "For $u_1 \le u \le u_\sigma$, $H(u)^2 \le S_B(u) \le S_B(u_1)$." For this to hold, we need $K_B(u) > 0$ on $[u_1, u_\sigma]$ or at least that $S_B' \le 0$ there. The derivative expression uses $K_B$ in denominator, so $K_B$ must be positive. If $K_B$ becomes negative, $S_B$ is not defined. So the claim that $S_B$ is decreasing on $[u_1, u_\sigma]$ requires that $K_B(u) > 0$ for all $u$ in that interval, or at least that the Sonin functional can be extended across zeros? Typically, one can use a modified functional or only apply the monotonicity on intervals where $K_B>0$. So the A1-Strategy implicit assumption is that $K_B(u) > 0$ on $[u_1, u_\sigma]$. But is that guaranteed? Let's analyze.

We need to check whether $u_\sigma$ can exceed the larger root $u_2$ of $K_B$. Since $u_2$ is where $K_B=0$, which is the right turning point of the endpoint equation. In the original variable, the right turning point is $x_+$. The relationship: $u$ at $x_+$ is $u_+ = B(1-x_+)/2$. The cap boundary $u_\sigma$ corresponds to $x=\sigma$. The central argument assumes $x_+ \le \sigma$? Actually the whole overflow condition is $x_+ > \sigma$, which defines the right overflow case. For right overflow, we have $x_+ > \sigma$, which implies $u_+ < u_\sigma$ (since $u$ decreases as $x$ increases). So $u_+ < u_\sigma$. That is, the turning point is closer to 1 than $\sigma$? Wait: $x=1$ corresponds to $u=0$. Larger $x$ gives smaller $u$. So $x_+$ is the turning point; if $x_+ > \sigma$, then $u_+ = B(1-x_+)/2 < B(1-\sigma)/2 = u_\sigma$. So the turning point $u_+$ is inside the cap, not beyond it. Actually the right turning point $x_+$ is where the oscillatory behavior ends (for the whole Jacobi interval). In the Sonin localization for the whole interval, the maximum occurs in the lobe before the turning point. For the endpoint cap after central clearance, the cap covers $x \in [\sigma, 1]$, i.e., $u \in [0, u_\sigma]$. The right turning point $x_+$ is either inside this interval or to the left of $\sigma$. The overflow condition $x_+ > \sigma$ means the turning point is inside the cap. Then the oscillatory region extends into the cap, and the last maximum might be at some $u_1$ with $0 < u_1 < u_+$. So we have $u_1 < u_+ < u_\sigma$. The Sonin functional for the original Jacobi had $F(x) > 0$ for $x \in (x_-, x_+)$. At $x_+$, $F(x_+)=0$. Similarly, for the endpoint ODE, $K_B(u_+) = 0$ because $u_+$ corresponds to $x_+$. Indeed, $F(x)$ and $K_B(u)$ are related. So $u_+$ is a root of $K_B$. Moreover, $K_B(u) > 0$ for $u \in (u_0, u_+)$ (where $u_0$ is the inner root), and $K_B < 0$ for $u > u_+$ (since $K_B$ is a downward parabola). So if $u_\sigma > u_+$, then for $u \in (u_+, u_\sigma]$, $K_B < 0$, and the Sonin functional is not defined. So the Sonin argument using $S_B$ can only be applied up to $u_+$, not up to $u_\sigma$. The A1-Strategy's claim that $S_B$ is decreasing on $[u_1, u_\sigma]$ and thus $H(u)^2 \le H(u_1)^2$ for $u \le u_\sigma$ would require $K_B(u) > 0$ on that whole interval, which would be false if $u_\sigma > u_+$. So the A1-Strategy's endpoint cap localization must consider whether $u_+ \ge u_\sigma$ or not. Let's check the relationship between $\sigma$ and $x_+$. The central region clearance is based on $|x| \le \sigma$. The right overflow case is $x_+ > \sigma$. So in the case we are interested in, $x_+ > \sigma$, hence $u_+ < u_\sigma$. So $u_\sigma$ is beyond the turning point, i.e., $u_\sigma > u_+$. Therefore, $K_B(u_\sigma) < 0$. Then the monotonicity lemma $K_B'(u) \ge 1/4$ on $[0, u_\sigma]$ might still hold, but it doesn't guarantee $K_B(u_\sigma) > 0$. Indeed we computed $K_B'(u_\sigma)$ and found a lower bound, but $K_B(u_\sigma)$ could be negative. Let's evaluate $K_B(u_\sigma)$. We have $K_B$ is quadratic, concave down. If $u_\sigma > u_+$, then $K_B(u_\sigma) < 0$. The Sonin function $S_B$ would be undefined there. So the A1-Strategy's claim of Sonin reduction to the first lobe may be flawed because they assume $K_B>0$ throughout the cap.

However, the original Jacobi Sonin functional $S(x) = g^2 + A^2 (g')^2 / F$ is defined on $(x_-, x_+)$ where $F>0$. Outside that, $F\le0$, and $S$ is not defined. The global maximum of $|g|$ is argued to be in the endpoint lobes because $S$ is monotone and at a local extremum $g'=0$, $S = g^2$. But that argument only says that the maximum among interior extrema must occur in the two lobes where $F>0$. It doesn't say that the maximum on $[\sigma,1]$ is controlled by the first lobe. Actually if $x_+ > \sigma$, then the interval $[\sigma, x_+]$ has $F>0$, and $[x_+, 1]$ has $F\le0$. On $[x_+,1]$, the function may be monotonic or have no internal extrema because $F\le0$ implies the ODE is non-oscillatory. In fact, for $F<0$, solutions are convex/concave away from axis, so maxima are at boundaries. So on $[x_+,1]$, the maximum occurs at $x_+$ or at $1$ (which is zero). At $x_+$, the function is controlled by the last extremum before the turning point. So the global max of $|g|$ for $x\ge \sigma$ is indeed either at the interface $x=\sigma$ (central bound) or at some point in $[\sigma, x_+]$ (first lobe) or at $x_+$ itself (which is near the last extremum). The A1-Strategy's endpoint cap analysis seems to rely on the transformed $u$ variable and the Sonin functional $S_B$, which should mirror the $S$ functional but on the cap. However, $S_B$ uses $K_B$, which is essentially $F$ expressed in $u$. So $K_B>0$ exactly when $F>0$. Thus $K_B>0$ on $u \in [u_+, u_\sigma]$? No, $u$ decreases as $x$ increases, so $x_+$ corresponds to $u_+$, and $x=\sigma$ corresponds to $u_\sigma$. Since $x_+ > \sigma$, we have $u_+ < u_\sigma$? Wait, $x$ and $u$ are inversely related: $u = B(1-x)/2$. So $x$ larger implies $u$ smaller. So if $x_+ > \sigma$, then $1-x_+ < 1-\sigma$, so $u_+ = B(1-x_+)/2 < B(1-\sigma)/2 = u_\sigma$. So $u_+ < u_\sigma$. And $F>0$ for $x \in (x_-, x_+)$ means $u \in (u_+, u_-)$? Actually $x_- < x_+$, and $x \in (x_-, x_+)$. For $u$, since $u$ is decreasing function of $x$, $u_- = B(1-x_-)/2$ and $u_+ = B(1-x_+)/2$. Since $x_- < x_+$, we have $1-x_- > 1-x_+$, so $u_- > u_+$. The oscillatory interval in $u$ is $u \in (u_+, u_-)$, with $u_+$ the right turning point (closer to 0) and $u_-$ the left turning point (closer to $B$). So $F>0$ corresponds to $K_B(u)>0$ for $u \in (u_+, u_-)$? Actually check near $x=1$ ($u=0$), $F(1) = -\alpha^2 \le 0$, and $K_B(0) = -\alpha^2/4 < 0$. So $K_B$ is negative near $u=0$. It crosses zero at $u_0$ (some small positive), becomes positive, peaks, then crosses zero again at $u_+$? Wait, we need to map the roots. The quadratic $F(x)$ has two roots $x_\pm$. At $x=1$, $F(1) = -\alpha^2 \le 0$. So for $x$ near 1, $F(x) \le 0$. The oscillatory region is $x \in (x_-, x_+)$ where $x_+ < 1$. So near $x=1$, $F<0$. In $u$, $u=0$ corresponds to $x=1$, so $K_B(0)<0$, then as $u$ increases, $x$ decreases, we move leftward. For $x$ just less than 1, $u$ small, $K_B<0$. At $x = x_+$, $F=0$, so $K_B(u_+) = 0$. For $x \in (x_+, x_-)$? Actually $x_+$ is the rightmost root, then moving left, $F>0$ until $x_-$. So $F>0$ for $x \in (x_-, x_+)$? That is the interval between the roots, with $x_-$ left root. Since $x_-$ might be negative. Anyway, in $u$, the interval $u \in (u_+, u_-)$ corresponds to $x \in (x_-, x_+)$, where $F>0$. So $K_B(u) > 0$ for $u \in (u_+, u_-)$. And $u_+$ is the smaller root. So for $u \in [0, u_+]$, $K_B \le 0$? Actually $K_B(0) < 0$, then there is a root at $u_0$ (the inner root)? But earlier we said $K_B$ has two positive roots if discriminant positive. Let's solve $K_B(u)=0$: $-\alpha^2/4 + \Lambda_B u - \Delta_B u^2 = 0 \Rightarrow \Delta_B u^2 - \Lambda_B u + \alpha^2/4 = 0$. Roots: $u = \frac{\Lambda_B \pm \sqrt{\Lambda_B^2 - \Delta_B \alpha^2}}{2\Delta_B}$. Since $\Delta_B >0$, this is an upward parabola? Wait, the equation is $\Delta_B u^2 - \Lambda_B u + \alpha^2/4 = 0$. The quadratic in standard form has positive leading coefficient, so it opens upward. Its minimum is at $u = \Lambda_B/(2\Delta_B)$. Since $\Lambda_B>0$, the larger root is $u_+ = (\Lambda_B + \sqrt{...})/(2\Delta_B)$, the smaller root is $u_0 = (\Lambda_B - \sqrt{...})/(2\Delta_B)$. Both are positive because $\alpha>0$. So $K_B(u) = -\Delta_B u^2 + \Lambda_B u - \alpha^2/4 = -(\Delta_B u^2 - \Lambda_B u + \alpha^2/4)$, so the quadratic factor is the negative of that. Thus $K_B > 0$ when $\Delta_B u^2 - \Lambda_B u + \alpha^2/4 < 0$, i.e., $u \in (u_0, u_+)$. So indeed $K_B > 0$ exactly for $u$ between the two roots. The smaller root $u_0$ is the inner turning point near $u=0$ (since $K_B(0)<0$). The larger root $u_+$ is the right turning point in the $u$ variable. Note: this $u_+$ is not the $x_+$ turning point; it's the $u$ value at which $K_B=0$. By relation, $x_+$ corresponds to $u_+$? Let's check: The original $F(x)=0$ at $x_\pm$. Since $F$ and $K_B$ are related by $K_B(u) = \frac{4}{B^2} u(1-u/B) ?$ No, we had $F = B A q_B = B \cdot \frac{4}{B} u(1-u/B) \cdot q_B = 4 u(1-u/B) q_B = 4 K_B(u)$. Actually $K_B = p_B q_B = u(1-u/B) q_B$. And $F(x) = \kappa (1-x^2) - \frac14(d - s x)^2$. But we derived $\frac{F}{B A} = q_B$, so $F = B A q_B = B \cdot \frac{4}{B} u(1-u/B) \cdot q_B = 4 u(1-u/B) q_B = 4 K_B(u)$. So $F(x) = 4 K_B(u)$. Therefore $F(x)=0$ exactly when $K_B(u)=0$. And $x=1-2u/B$. So the roots $x_\pm$ correspond to $u_\pm$ where $K_B(u_\pm)=0$. Indeed, $F(x)$ is a concave quadratic in $x$, while $K_B(u)$ is a concave quadratic in $u$. So the two roots of $K_B$ are $u_0$ and $u_+$, with $0 < u_0 < u_+$. The right turning point in $x$, $x_+$, is the one closer to $1$, which corresponds to the smaller $u$? Since $x=1$ gives $u=0$, $x_+$ is the root nearer $1$, so its $u$ is smaller than the other root. Thus $x_+$ corresponds to the smaller root $u_0$, not the larger root! Because if $x$ is larger (closer to 1), $u$ is smaller. So $x_+$ is the rightmost root in $x$, so the corresponding $u_+$ (call it $u_{x_+}$) is the smallest positive root of $K_B$, i.e., $u_0$. Let's verify with an example. Earlier test: $n=1,\alpha=1,\beta=0$, $B=3$. Then $K_B(u) = -1/4 + \Lambda_B u - \Delta_B u^2$. With $\Lambda_B=4/3$, $\Delta_B=5/12$, solve $K_B(u)=0$: $\frac{5}{12} u^2 - \frac{4}{3} u + \frac14 =0$ multiply 12: $5u^2 - 16 u + 3 = 0$, roots: $u = \frac{16 \pm \sqrt{256 - 60}}{10} = \frac{16 \pm 14}{10}$, so $u=3$ or $u=0.2$. So $u_0 = 0.2$, $u_+ = 3$. Now original $F(x)$: $\sigma = s/(s+2n) = 1/(1+2)=1/3$. $x_+$ can be computed. Alternatively, we know $F(x)=0$ at $x$ such that $u=0.2$ or $3$. Since $u = B(1-x)/2 = 1.5(1-x)$. For $u=0.2$, $1-x = 0.2/1.5 \approx 0.1333$, $x=0.8667$; for $u=3$, $1-x = 2$, $x=-1$. $x=1$ gives $u=0$. The interval $x\in[-1,1]$ corresponds to $u\in[0, B] = [0,3]$. The root $u=3$ corresponds to $x=-1$. So the right turning point in $x$ is $x_+ \approx 0.8667$, which is $u=0.2$. So indeed the smaller root $u_0$ is the right turning point. Therefore, the oscillatory region in $u$ is $u \in (u_0, u_+)$ where $u_+$ is the larger root (which corresponds to $x_-$ maybe negative). So $K_B(u)>0$ for $u \in (u_0, u_+)$. The cap is $u \in [0, u_\sigma]$. In the overflow case, we have $x_+ > \sigma$, which means $1-x_+ < 1-\sigma \Rightarrow u_0 = u_{x_+} < u_\sigma$. So $u_0 < u_\sigma$. The larger root $u_+$ could be greater than $u_\sigma$ or not. In many cases, $u_+$ is large, possibly larger than $B$? Actually $u_+ = (\Lambda_B + \sqrt{...})/(2\Delta_B)$. Let's evaluate for the test: $u_+ = 3$, and $B=3$, so $u_+ = B$. $u_\sigma = nB/(B+n-1) = 1*3/(3) = 1$. So $u_0=0.2$, $u_+ = 3$, $u_\sigma=1$. So $K_B(u)>0$ on $(0.2, 3)$, which includes $(0.2, 1]$. So $K_B(u_\sigma) > 0$ indeed. For $n=1$, $\alpha=1/2$, $\beta=0$, $B=2.5$, we can compute: $n=1,\alpha=1/2,\beta=0$, $s=1/2$, $B=2.5$. Then $r_B = s/B = 0.5/2.5=0.2$. $c_B = 1 + 0.1 = 1.1$. $\Lambda_B = 1.1 + 0.5*0.2/2 = 1.1 + 0.05 = 1.15$. $\Delta_B = 1.1/2.5 + (0.04)/4 = 0.44 + 0.01 = 0.45$. $K_B(u) = -0.0625 + 1.15 u - 0.45 u^2$. Roots: $0.45 u^2 - 1.15 u + 0.0625 = 0$, discriminant $1.3225 - 0.1125 = 1.21$, sqrt=1.1. Roots: $(1.15 \pm 1.1)/(0.9) => u1 = (2.25)/0.9 = 2.5, u2 = (0.05)/0.9 \approx 0.0556$. So $u_0 \approx 0.0556$, $u_+ = 2.5 (which equals $B$). $u_\sigma = nB/(B+n-1) = 1*2.5/(2.5+1-1)=2.5/2.5=1$. So $K_B(1) = -0.0625 + 1.15 - 0.45 = 0.6375 > 0$. So indeed $u_\sigma = 1$ is inside the positivity interval. In fact, $u_\sigma \le n \le B$ typically, and $u_+$ is often close to $B$ because when $u$ approaches $B$, $x$ approaches $-1$, the left endpoint. In the right overflow regime, $\beta$ is large or moderate? Actually for right overflow, we have large $\beta$ or small $\alpha$. It seems $u_+$ is generally greater than $u_\sigma$. Let's test an extreme: $n=10, \alpha=10, \beta=0$ (but $\alpha$ can't exceed overflow bound? overflow bound $\alpha < n+\sqrt{4n^2+2n} \approx 10+ \sqrt{400+20}\approx 10+20.5=30.5$, so $\alpha=10$ is allowed). Then $s=10$, $B = 10+10+1=21$. Compute approx: $r_B = 10/21 \approx 0.476$, $c_B = 10 + 0.238 = 10.238$, $\Lambda_B = 10.238 + 10*0.238/2 = 10.238 + 1.19 = 11.428$. $\Delta_B = 10.238/21 + (0.476^2)/4 = 0.4875 + 0.0566 = 0.5441$. $K_B(u) = -25 + 11.428 u - 0.5441 u^2$. Roots: $0.5441 u^2 - 11.428 u + 25 = 0$. Discriminant: $11.428^2 - 4*0.5441*25 = 130.6 - 54.41 = 76.19$, sqrt=8.73. Roots: $u_0 = (11.428 - 8.73)/(1.0882) \approx 2.48$, $u_+ = (11.428 + 8.73)/1.0882 \approx 18.5$. $B=21$. $u_\sigma = nB/(B+n-1) = 10*21/(21+10-1)=210/30=7$. So $u_\sigma=7$ lies between $u_0=2.48$ and $u_+=18.5$. So $K_B(u_\sigma) > 0$. In this case, overflow condition: $x_+ > \sigma$? Let's check $\sigma = s/(s+2n) = 10/30 = 1/3$. $x_+$ from $u_0=2.48$ gives $x_+ = 1 - 2*2.48/21 = 1 - 0.236 = 0.764$, which is $>1/3$, so right overflow. So $u_\sigma < u_+$ typically. It seems $u_+$ is often close to $B$ (the left endpoint). Since $u_\sigma \le n$ and $B \ge n+\alpha+1 \ge n+1$, $u_\sigma$ is less than $B$ and likely less than $u_+$ because the larger root $u_+$ is near $B$? In fact, $u_+$ is the $u$ coordinate of the left turning point $x_-$, which is often near $-1$ unless $\beta$ is very small. When $\beta=0$, $x_-$ may be inside $(-1,1)$, but $u_+$ still could be less than $B$? In the $\beta=0$ case, $B = n+\alpha+1$, so $B$ is not huge. In the test with $n=10,\alpha=10,\beta=0$, $B=21$, $u_+ \approx 18.5$, $u_\sigma=7$, so indeed $u_\sigma < u_+$. It seems plausible that $u_+$ is always greater than $u_\sigma$ if $n\ge1$. But is it guaranteed? Let's analyze the relative magnitude. $u_+$ is the root where $K_B(u_+)=0$. Since $K_B$ is a downward parabola, $u_+$ can be expressed. We need to see if $u_+ \ge u_\sigma$ always holds under the residual strip conditions. This might be provable. If it holds, then $K_B(u) > 0$ for all $u \in (u_0, u_\sigma]$, so the Sonin functional is defined and decreasing on $[u_1, u_\sigma]$ (since $u_1 > u_0$). That would validate the Sonin reduction. So we need to check the inequality $u_+ \ge u_\sigma$. Let's attempt to verify algebraically.

We have $u_+ = \frac{\Lambda_B + \sqrt{\Lambda_B^2 - \Delta_B \alpha^2}}{2 \Delta_B}$ (since the larger root). And $u_\sigma = \frac{n B}{B+n-1}$.

We need to prove $u_+ \ge u_\sigma$ for $n\ge1$, $\alpha\ge1/2$, $\beta\ge0$, $B = n+\alpha+\beta+1$, with perhaps the overflow condition $\alpha < n + \sqrt{4n^2+2n}$? But the residual strip includes all $\beta\ge0$ and $\alpha \le \alpha_E(n)$, which is about $n$. So we can attempt to derive an inequality.

Alternatively, note that $u_+$ is the $u$ value where $K_B(u)=0$, and we know $K_B(u)$ is related to $F(x)$. Since $F(x)$ is positive on $(x_-, x_+)$, the interval of positivity in $u$ is $(u_0, u_+)$ where $u_0$ corresponds to $x_+$ and $u_+$ corresponds to $x_-$. The central region boundary $\sigma$ is such that $|x| \le \sigma$. The left turning point $x_-$ is often $\le -\sigma$, meaning $x_- \le -\sigma$. In terms of $u$, $x_- \le -\sigma$ would imply $u_- \ge B(1-\sigma)/2? Actually $x_-$ is negative, so $1-x_- \ge 1+\sigma$, so $u_- = B(1-x_-)/2 \ge B(1+\sigma)/2$. For $x_- \le -\sigma$, we need $1-x_- \ge 1+\sigma$, which holds if $x_- \le -\sigma$. Is $x_- \le -\sigma$ guaranteed? The central region clearance says $[x_-, x_+] \subseteq [-\sigma, \sigma]$ is the easy case; the hard case is when overflow happens, i.e., $x_+ > \sigma$. But what about $x_-$? In the right overflow case, we only know $x_+ > \sigma$; $x_-$ could still be $\ge -\sigma$ or not. The A1-Strategy's residual strip after central clearance is when $x_+ > \sigma$ but $x_-$ could be anything. However, they apply Sonin localization from right endpoint only, not using left. The endpoint cap is only on the right side, and the Sonin reduction there only needs $K_B>0$ on $(u_0, u_\sigma]$. This is equivalent to $u_\sigma \le u_+$. Since $u_+$ corresponds to $x_-$, this condition is $u_\sigma \le u_{x_-}$, i.e., $1-x_\sigma \le 1-x_- \Rightarrow x_- \le x_\sigma = \sigma$. So $u_\sigma \le u_+$ is equivalent to $x_- \le \sigma$. So the Sonin reduction on the right cap requires that the left turning point $x_-$ is not to the right of $\sigma$; i.e., the oscillatory region extends at least up to $\sigma$ on the left. If $x_- > \sigma$, then the whole oscillatory region $(x_-, x_+)$ would be inside $[\sigma, 1]$, and the right cap would contain both turning points, making the maximum possibly at the left endpoint of the cap. That would be a different regime. But in the residual strip defined by right overflow, we only know $x_+ > \sigma$. The condition $x_- \le \sigma$ is not automatically true. So the Sonin reduction as stated might be invalid if $x_- > \sigma$. That is a potential gap.

Let's examine: The central region clearance says that if $[x_-, x_+] \subseteq [-\sigma, \sigma]$, then the conjecture is proved. The complement is when either $x_+ > \sigma$ or $x_- < -\sigma$ (or both). The A1-Strategy focuses on right overflow $x_+ > \sigma$. In that case, the right lobe extends beyond the central region. But the left lobe might still be within $[-\sigma, \sigma]$ or also overflow. If left overflow $x_- < -\sigma$, then the whole interval $[-\sigma, \sigma]$ may contain the oscillatory region, but the right overflow case alone doesn't guarantee $x_- \le \sigma$. Actually $x_-$ is the left turning point; it could be greater than $-\sigma$ but still less than $\sigma$? By definition, $x_- \le x_+$. If $x_+ > \sigma$, it's possible that $x_- \le \sigma$ or $x_- > \sigma$. Since $\sigma$ is positive (if $s>0$), $x_-$ could be positive if $\alpha$ is very small compared to $\beta$? Let's check. $x_-$ is given by formula. In the right overflow regime, $\beta$ is large or moderate, $\alpha$ could be small. For example, take $n=1$, $\alpha=0.6$, $\beta=10$. Then $s=10.6$, $\sigma = s/(s+2)=10.6/12.6 \approx 0.84$. $B=1+10.6+1=12.6$. Compute $x_\pm$ using formula? Might be messy. But it's possible that $x_-$ is positive and less than $\sigma$? If $x_- > \sigma$, then the entire oscillatory region is to the right of $\sigma$, i.e., $x \in (x_-, x_+)$ with $x_- > \sigma$. That would mean there is no oscillatory region overlapping the central zone, and the central bound at $x=\sigma$ would be in the non-oscillatory region. The Sonin localization for the whole interval would then force the maximum to be in the right lobe, but the cap $[\sigma,1]$ would contain the entire oscillatory region plus some. In that case, the Sonin reduction on the cap might still work if we consider the cap's $u$ variable. The condition $u_\sigma \le u_+$ would become $u_\sigma \le u_{x_-}$, i.e., $x_- \le \sigma$, which is false if $x_- > \sigma$. So $u_+$ would be less than $u_\sigma$, meaning $K_B(u_\sigma)$ might be negative. So the Sonin functional wouldn't be defined at $u_\sigma$. However, we could still bound the maximum by the last extremum before $u_+$, and then note that for $u > u_+$, the solution is non-oscillatory and the maximum is at $u_+$ or at the boundary. But since the central bound applies at $u_\sigma$, we might still get a bound. This suggests the Sonin reduction lemma needs a careful case breakdown.

The A1-Strategy does not discuss this. The bootstrap judge's G1 lists these cases to be handled. So I'll flag this as a gap.

Now, about the finite-$\beta$ bridge: The A1-Strategy's direct endpoint theorem doesn't require a bridge, but if it fails, the fallback is a bridge from Laguerre limit. The bootstrap judge provides a precise bridge theorem: If we have a Laguerre certificate $|\ell|^4 \le 1/2$ on $0\le u\le n$, and a perturbation bound $|H_B - \ell| \le \varepsilon_{n,B}$, then if $\varepsilon$ is small enough, the KKT holds for finite $B$. That is a concrete statement. So the bridge is well-defined.

Now, as A3, I should produce a symbolic/numeric check plan. For example, use a computer algebra system to verify the transformation and the identities, and to test the inequality $u_+ \ge u_\sigma$ for parameter ranges. But since I'm an API, I can't run CAS, but I can outline the plan.

The output should be a Stage A reasoning: Summarize the verified formulas, point out the Sonin reduction gap, note Bessel max constant issue, and propose next checks.

Let's draft the response.

Summary: Independent algebra audit of endpoint ODE, cap bound, monotonicity, Frobenius/Bessel normalization. Confirmed formulas; found a potential gap in Sonin first-lobe reduction due to $u_+$ possibly less than $u_\sigma$; noted that $B_*<0.680$ is inaccurate for $\nu=1/2$; provided a check plan.

Detailed reasoning: step-by-step rederivation, with comments.

I'll structure:

**Summary:** brief.

**Main claim or direction:** Verify endpoint ODE and associated constants; identify missing cases in Sonin reduction.

**Detailed reasoning:** Rederive the transformation; show $K_B$ and $u_\sigma$; verify monotonicity lemma; compute Frobenius and Bessel normalization; analyze Sonin functional domain; discuss $u_+$ vs $u_\sigma$; note Bessel maximum constant.

**Dependencies:** existing literature on Jacobi ODE, Sonin-Polya, Bessel functions.

**Potential gaps:** (Gap A) Sonin reduction assumes $K_B>0$ on $[u_1, u_\sigma]$, which requires $u_\sigma \le u_+$. The condition $u_\sigma \le u_+$ is equivalent to $x_- \le \sigma$, which is not guaranteed by the overflow condition alone. Need to either prove $x_- \le \sigma$ in the residual strip or adapt the argument. (Gap B) Claimed $B_*<0.680$ for $\nu\ge1/2$ is false; $\sup |J_{1/2}(t)| \approx 0.7979$. Need corrected statement.

**Counterexample or obstruction search:** Suggest testing parameters where $x_-$ might be > $\sigma$ (e.g., small $n$, moderate $\alpha$, very large $\beta$?) to see if Sonin reduction fails.

**Useful lemmas:** Algebraic lemma for $K_B'(u) \ge 1/4$ proven; $u_\sigma \le n$ proven; Frobenius coefficient formula verified; Bessel normalization formula verified.

**What should be tested next:** 

- Prove or disprove that $u_\sigma \le u_+$ (equivalently $x_- \le \sigma$) for all parameters in the residual strip. If not, modify the endpoint Sonin lemma accordingly.
- Obtain an accurate bound for $\sup_{\alpha \ge 1/2, t\ge0} |J_\alpha(t)|$, or implement the A1-Strategy's plan of Landau + interval verification.
- Derive explicit constants for gamma-ratio bound $M_{n,\alpha,B} \le 1 + C_\Gamma/(n+1)$ using Gautschi or Kershaw inequalities.
- Provide a rigorous perturbation analysis for the Bessel model.

**Confidence:** High confidence in the algebraic derivations and monotonicity lemma. Moderate confidence in the Sonin reduction as stated, due to the $u_+$ gap. Low confidence in the current constant estimates until they are rigorously derived.

Now produce the output.

# Final Answer

Summary:
The algebraic core of the endpoint‑cap route is verified: the exact endpoint ODE, the cap bound $u_\sigma\le n$, the monotonicity lemma $K_B'(u)\ge1/4$, and the Frobenius/Bessel normalization constants. Two hidden assumptions are exposed: the Sonin first‑lobe reduction implicitly requires $u_\sigma\le u_+$ (equivalently $x_-\le\sigma$), which is not proved for the full residual strip; and the claimed uniform Bessel maximum $B_*<0.680$ for $\nu\ge1/2$ is false for $\nu=1/2$. The missing pieces are a rigorous treatment of the Sonin domain and corrected constants.

Main claim or direction:
The finite‑$\beta$ endpoint‑cap program is algebraically sound, but the step from the exact ODE to a first‑lobe bound needs a careful Sonin‑domain analysis and an accurate Bessel‑envelope constant.

Detailed reasoning:

### 1. Endpoint transformation (verified)
Start from the Jacobi self‑adjoint form for the weighted normalized function
$$
g(x)=g_n^{(\alpha,\beta)}(x),\qquad
(Ag')'+\frac{F(x)}{A}g=0,
$$
with $A(x)=1-x^2$, $s=\alpha+\beta$, $d=\beta-\alpha$, $\kappa=n(n+s+1)+s/2$, and
$$
F(x)=\kappa(1-x^2)-\frac14(d-sx)^2 .
$$
Introduce
$$
B=n+s+1,\qquad u=\frac{B(1-x)}2,\qquad H(u)=g\!\left(1-\frac{2u}B\right).
$$
Then $dx=-\frac2B\,du$, $A(x)=\frac4B\,u\bigl(1-\frac uB\bigr)$, and the ODE becomes
$$
\bigl(p_B(u)H'(u)\bigr)'+q_B(u)H(u)=0,
$$
with
$$
p_B(u)=u\Bigl(1-\frac uB\Bigr),\qquad
r_B=\frac sB=1-\frac{n+1}B,\qquad
c_B=n+\frac{r_B}2,
$$
$$
q_B(u)=c_B-\frac{(r_Bu-\alpha)^2}{4u\bigl(1-\frac uB\bigr)} .
$$
This matches the formulas in A1‑Strategy and the bootstrap judge.

The product $K_B(u):=p_B(u)q_B(u)$ simplifies to
$$
K_B(u)=-\frac{\alpha^2}4+\Lambda_Bu-\Delta_Bu^2,
$$
$$
\Lambda_B=c_B+\frac{\alpha r_B}2,\qquad
\Delta_B=\frac{c_B}B+\frac{r_B^2}4 .
$$
The relation to the original factor is $F(x)=4K_B(u)$.

### 2. Interface variable $u_\sigma$ (verified)
The central/endcap interface is defined by
$$
\sigma=\frac s{s+2n}=\frac{B-n-1}{B+n-1},
\qquad
u_\sigma=\frac{B(1-\sigma)}2=\frac{nB}{B+n-1}.
$$
For $n\ge1$ one has $0<u_\sigma\le n$, with equality $u_\sigma=n$ only when $n=1$.
Thus the endpoint cap is always contained in the compact interval $0\le u\le n$.

### 3. Monotonicity lemma $K_B'(u)\ge\frac14$ (verified)
Because $K_B$ is a concave quadratic, its derivative $K_B'(u)=\Lambda_B-2\Delta_Bu$ is decreasing. On $0\le u\le u_\sigma$ the minimum is at $u=u_\sigma$.
A direct algebraic simplification gives
$$
K_B'(u_\sigma)-\frac14
=\frac14\Bigl(2\alpha+2n+1-\frac{2(n+1)(n+\alpha+1)}B\Bigr).
$$
Since $B=n+\alpha+\beta+1\ge n+\alpha+1$, the subtracted term is at most $2(n+1)$; therefore
$$
K_B'(u_\sigma)-\frac14 \ge \frac{2\alpha-1}4 .
$$
For $\alpha\ge\frac12$ this is non‑negative, so $K_B'(u)\ge\frac14$ holds on the whole cap.
**Status:** Rigorous algebraic lemma.

### 4. Frobenius coefficient and Bessel normalization (verified)
Near $u=0$ the regular solution behaves as
$$
H(u)\sim A_{n,\alpha,B}\,u^{\alpha/2},
$$
$$
A_{n,\alpha,B}=
\Bigl(\frac{\Gamma(n+1)\Gamma(B)}{\Gamma(n+\alpha+1)\Gamma(B-\alpha)}\Bigr)^{\!1/2}
B^{-\alpha/2}\,\frac{\Gamma(n+\alpha+1)}{\Gamma(\alpha+1)\Gamma(n+1)} .
$$
The Bessel model
$$
\mathcal B(u)=M_{n,\alpha,B}\,J_\alpha\!\bigl(2\sqrt{\Lambda_Bu}\,\bigr)
$$
with
$$
M_{n,\alpha,B}=A_{n,\alpha,B}\,\Gamma(\alpha+1)\,\Lambda_B^{-\alpha/2}
=\Bigl(\frac{\Gamma(B)\Gamma(n+\alpha+1)}{\Gamma(B-\alpha)\Gamma(n+1)}\Bigr)^{\!1/2}
(B\Lambda_B)^{-\alpha/2}
$$
reproduces the same leading term because $J_\alpha(z)\sim\frac1{\Gamma(\alpha+1)}(z/2)^\alpha$.
**Status:** Algebraic derivation correct.

### 5. Hidden assumption in the Sonin first‑lobe reduction
A1‑Strategy introduces the endpoint Sonin functional
$$
S_B(u)=H(u)^2+\frac{p_B(u)^2\,H'(u)^2}{K_B(u)},
\qquad
S_B'(u)=-\frac{p_B(u)^2\,K_B'(u)}{K_B(u)^2}\,H'(u)^2,
$$
which is valid only where $K_B(u)>0$. The claimed reduction
$$
\sup_{0\le u\le u_\sigma}|H(u)| = H(u_1)\quad (u_1\text{ first positive maximum}),
$$
rests on the assumption $K_B(u)>0$ on $[u_1,u_\sigma]$ (so that $S_B$ decreases and bounds $H^2$).

Now $K_B$ is a concave quadratic with two positive roots
$$
u_0=\frac{\Lambda_B-\sqrt{\Lambda_B^2-\Delta_B\alpha^2}}{2\Delta_B},
\qquad
u_+=\frac{\Lambda_B+\sqrt{\Lambda_B^2-\Delta_B\alpha^2}}{2\Delta_B},
$$
and $K_B>0$ exactly on $(u_0,u_+)$. The smaller root $u_0$ corresponds to the right Jacobi turning point $x_+$, while the larger root $u_+$ corresponds to the left turning point $x_-$.

The cap extends up to $u_\sigma$. For the right‑overflow case we are guaranteed $x_+>\sigma$, which is equivalent to $u_0<u_\sigma$. **However**, the Sonin interval requires $u_\sigma\le u_+$, i.e. the cap stays inside the positivity region of $K_B$. The condition $u_\sigma\le u_+$ is exactly $x_-\le\sigma$ (the left turning point does not lie to the right of the interface).

The right‑overflow condition alone does **not** enforce $x_-\le\sigma$. It is possible that both turning points lie to the right of $\sigma$ (i.e. $x_->\sigma$), in which case $u_+<u_\sigma$ and $K_B(u_\sigma)<0$, invalidating the direct Sonin argument on the whole cap. The Sonin reduction therefore needs a case distinction or an additional proof that in the residual strip one indeed has $x_-\le\sigma$ (or that the argument can be repaired when $x_->\sigma$).

**Gap A:** Prove (or disprove) $u_\sigma\le u_+$ for all parameters
$$
n\ge1,\quad \tfrac12\le\alpha\le\alpha_E(n),\quad \beta\ge0,
$$
under the overflow condition $x_+>\sigma$. If the inequality fails, provide a modified reduction that still bounds the cap maximum.

### 6. Bessel maximum constant
A1‑Strategy writes $B_*=\sup_{\nu\ge1/2,\,t\ge0}|J_\nu(t)|<0.680$, intending to use it in the first‑lobe bound
$$
|H(u)|\le\Bigl(1+\frac{C_\Gamma}{n+1}\Bigr)\Bigl(B_*+\frac{C_B}{n+1}\Bigr).
$$
However, for $\nu=1/2$,
$$
\sup_{t\ge0}|J_{1/2}(t)|
=\sup_{t>0}\sqrt{\frac2{\pi t}}\,|\sin t|
\approx0.7979,
$$
which exceeds $0.680$. The constant $0.680$ is not a uniform bound for all $\nu\ge1/2$.
A1‑Strategy acknowledges this by proposing a Landau bound for $\nu\ge1$ plus interval verification for $\frac12\le\nu\le1$; the eventual theorem must reflect the actual maximum (e.g. $0.798$ for the worst case) and still satisfy $M_{n,\alpha,B}\times(\text{Bessel max})+\text{perturbation}<2^{-1/4}$.

**Gap B:** The statement $B
