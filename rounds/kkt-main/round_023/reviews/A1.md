Summary:

Source note: this is the requested Round 23 Stage B cross-review of the uploaded `A1_review_23.md` packet and its A2, A3, and A4 Stage A outputs. The Round 23 directive explicitly says this is an artifact-completion round whose main objective is to complete the $n=1$ residual endpoint theorem and freeze the lemma bank, not to add more broad architecture. :contentReference[oaicite:0]{index=0} :contentReference[oaicite:1]{index=1}

My review conclusion is:

$$
\boxed{\text{A3 is the most reliable Round 23 output. A2 is useful but overlabels conditional Volterra claims. A4 contains serious algebra and numerical errors.}}
$$

No Round 23 agent proves the real-parameter KKT conjecture. No Round 23 agent proves the finite-$B$ first-lobe amplitude theorem. The endpoint-cap first-lobe route remains the selected main route, but the next round must focus on concrete proof artifacts: first a corrected $n=1$ certificate, then lemma-bank consolidation, then $n=2$ root isolation.

External-reference check: KKT’s arXiv page confirms the paper title, authors, and the Jacobi Bernstein/discrete Laguerre connection. :contentReference[oaicite:2]{index=2} Landau’s paper is bibliographically verified as L. J. Landau, “Bessel Functions: Monotonicity and Bounds,” *J. London Math. Soc.* 61(1), 197–215, DOI `10.1112/S0024610799008352`; the available abstract says it concerns monotonicity in the order of Bessel-function magnitudes at positive stationary points. :contentReference[oaicite:3]{index=3} Dunster--Gil--Segura’s “Sharp error bounds for turning point expansions” is verified as a paper deriving computable sharp error bounds for linear ODEs with a simple turning point and Airy functions. :contentReference[oaicite:4]{index=4} Arb is verified as a midpoint-radius, or ball-arithmetic, interval library supporting real/complex numbers, polynomials, power series, matrices, and many special functions. :contentReference[oaicite:5]{index=5} DLMF §2.8 confirms the standard Liouville-transform framework and the distinction between simple turning-point Airy cases and simple-pole Bessel cases. :contentReference[oaicite:6]{index=6} Gamma-ratio references are available through DLMF §5.6 and Wendel’s original note listing, but no Round 23 output instantiates them into the required four-gamma inequality. :contentReference[oaicite:7]{index=7}

Most valuable input from others:

### A2

A2’s valuable input is the rational-coordinate Bessel normal form and its emphasis that pointwise residual size at the hard edge is not the same thing as Volterra-weighted variation. The exact normal form

$$
Y''+
\left(
\frac{\Lambda_B}{z}
+
\frac{1-\alpha^2}{4z^2}
+
\Delta Q(z)
\right)Y=0,
$$

with

$$
\Delta Q(z)
=
-\frac{\Lambda_B}{B+z}
-\frac{\Delta_BB^2}{(B+z)^2},
$$

is a useful object. It should be retained as a small-$\alpha$/hard-edge track. The Round 22 packet had explicitly asked A2 to produce this exact Volterra lemma, define the Bessel reference solutions, prove or retract the sign-definite kernel claim, and provide constants rather than only $O(\alpha^3/n^2)$ scaling. :contentReference[oaicite:8]{index=8}

A2 also correctly resists treating the monolithic unweighted Langer route as sufficient. This is consistent with the prior judge’s statement that A2’s rational-coordinate residual formula is likely correct but the sign-definite Volterra kernel and $\mathcal V_{\mathrm{Bess}}=O(\alpha^3/n^2)$ scaling are not theorem-level as written. :contentReference[oaicite:9]{index=9}

### A3

A3’s output is the strongest. It gives the most complete lemma-bank algebra: affine endpoint ODE, quadratic $K_B$, cap length, cap monotonicity, Frobenius coefficient, Bessel normalization, rational-coordinate equation, invariant product, critical-point equation, and exact Bessel residual. The retrieved packet shows A3’s certified lemma list including L23.1 through L23.7, for example:

$$
(p_BH')'+q_BH=0,
$$

$$
K_B(u)=p_B(u)q_B(u)
=
-\frac{\alpha^2}{4}+\Lambda_Bu-\Delta_Bu^2,
$$

$$
u_\sigma=\frac{nB}{B+n-1}\le n,
$$

and

$$
K_B'(u)\ge K_B'(u_\sigma)
=
\frac{(\alpha+\beta)(n+\alpha+1)}{2B}
\ge \frac{\alpha}{2}.
$$

These are exactly the identities the project needs frozen in the lemma bank. :contentReference[oaicite:10]{index=10}

A3’s most important negative contribution is also valuable: the residual against a naive Bessel core is algebraically exact but not uniformly $O(1/B)$ when $\alpha=O(n)$. This is a real obstruction to crude Bessel perturbation. A3 therefore supports the regime split rather than a single static-Bessel closure.

### A4

A4’s useful contribution is the attempt to turn low-degree certification into explicit critical equations. The idea of using exact $n=1$ and $n=2$ critical equations plus compactified interval root isolation is the correct low-degree strategy. A4 also correctly focuses attention on the Bessel maximum, the $n=1$ scalar proof, and $n=2$ interval isolation.

However, A4 did not provide actual certified enclosures for the $n=1$ theorem, despite the prompt explicitly requiring them. The retrieved packet shows that the requested deliverable was outward-rounded interval or exact rational enclosures proving

$$
E(\alpha)<0.39
\qquad
\left(\frac12\le\alpha\le\frac65\right),
$$

not pseudo-code or heuristic arithmetic. :contentReference[oaicite:11]{index=11}

Claims that look correct:

1. **Endpoint-cap algebra remains stable.** A3’s affine endpoint ODE, cap bound, and quadratic $K_B$ product match the accumulated judge state and should be promoted to the lemma bank after final formatting. :contentReference[oaicite:12]{index=12}

2. **The rational-coordinate residual formula is correct.** A2’s and A3’s formula

$$
\Delta Q(z)
=
-\frac{\Lambda_B}{B+z}
-\frac{\Delta_BB^2}{(B+z)^2}
$$

relative to the reference equation

$$
Y''+
\left(
\frac{\Lambda_B}{z}
+
\frac{1-\alpha^2}{4z^2}
\right)Y=0
$$

is algebraically credible and consistent with prior judge synthesis. The residual is regular at $z=0$ but not necessarily small there. :contentReference[oaicite:13]{index=13}

3. **A2’s pointwise-versus-integrated-residual distinction is correct.** The fact that $\Delta Q(0)=O(1)$ does not automatically kill the rational-Bessel track; the proof-relevant object is a weighted Volterra integral. But A2 must still prove the kernel bound and constants.

4. **Landau is a legitimate Bessel dependency, but not an amplitude theorem.** Landau’s theorem can support reducing the Bessel maximum over $\nu\ge1/2$ once the proof genuinely reduces to Bessel stationary-point maxima. It does not prove any Jacobi first-lobe bound by itself. :contentReference[oaicite:14]{index=14}

5. **Arb is a suitable certification platform.** A4’s proposed interval direction is methodologically valid only if it produces actual outward-rounded logs; Arb itself is an appropriate tool for that. :contentReference[oaicite:15]{index=15}

Claims that need proof:

1. **A2’s sign-definite Volterra kernel.** A2 marks the kernel sign profile as proved, but the proof depends on zero interlacing, $Y_\alpha/J_\alpha$ monotonicity, and sign control up to the first peak. The response does not state exact hypotheses or show that the relevant interval stays within the sign regime. This must be downgraded to conditional.

2. **A2’s Watson/Nicholson product bound.** The proposed inequality

$$
|J_\alpha(x)Y_\alpha(x)|
\le
\frac{1}{\pi\sqrt{\alpha^2-x^2}}
\qquad
(0\le x<\alpha)
$$

is not verified in the response and was not verified in my web pass. A2 must either cite the exact theorem with hypotheses and constants or replace it by a weaker, proved bound.

3. **A2’s variation estimate.** The formula

$$
\mathcal V_{\mathrm{Bess}}
\le
\frac{3}{16}\frac{\alpha^3}{\Lambda_B^2}
+
O\left(\frac{\alpha^{8/3}}{\Lambda_B^2}\right)
$$

is not a theorem because the transition-tail constant is not bounded. The claimed threshold $\alpha\le1.04n^{2/3}$ is therefore only a heuristic scalar target.

4. **A3’s Langer removable value.** A3 provides excellent endpoint and rational-coordinate algebra, but the Round 23 output does not visibly complete the requested CAS-style derivation of

$$
\Psi_B(0)
=
\frac{10\gamma K_{\tau\tau\tau}(\tau_0)-9K_{\tau\tau}(\tau_0)^2}
{140\gamma^{8/3}}
$$

with explicit cancellation of the $\zeta^{-2}$ and $\zeta^{-1}$ terms. This remains convention-sensitive and should not be fully certified until that calculation is logged. The prompt explicitly asked for that cancellation. :contentReference[oaicite:16]{index=16}

5. **A3’s $n=1$ scalar theorem.** A3 did not finish the requested proof of $E(\alpha)<0.39$ on $[1/2,6/5]$.

6. **A4’s Bessel maximum proof.** The external Landau dependency is plausible, but A4’s numerical calculation of the half-order maximum is arithmetically wrong. Correctly, if $t_0$ solves $\tan t=2t$, then

$$
J_{1/2}(t_0)^2
=
\frac{8t_0}{\pi(1+4t_0^2)}
\approx 0.4613019151,
$$

so

$$
J_{1/2}(t_0)\approx0.6791921047.
$$

A4 reports approximately $0.679417$, which is too high due to a numerical error in $4t_0^2$. The final inequality $<0.680$ is still true, but A4’s certificate as written is not reliable. Earlier judge material already records the correct half-order value near $0.6791921047$. :contentReference[oaicite:17]{index=17}

7. **A4’s $n=1$ critical quadratic.** A4 uses the wrong hypergeometric parameter shift. For $n=1$,

$$
P_1^{(\alpha,\beta)}(1-2v)=\alpha+1-Bv,
\qquad
B=\alpha+\beta+2,
$$

not $\alpha+1-(B+1)v$. Therefore the correct critical equation in $v=u/B$ is

$$
B^2v^2
-
\left(2\alpha^2+2\alpha\beta+5\alpha+3\beta+4\right)v
+
\alpha(\alpha+1)
=0.
$$

A4’s coefficient $(B+1)^2$ is wrong. Consequently, the claimed stress-test roots and the associated “cap trivialization” conclusion are unreliable.

8. **A4’s $n=2$ cubic.** The same parameter-shift error propagates. For $n=2$ the hypergeometric second parameter is $B=n+\alpha+\beta+1=\alpha+\beta+3$, not $B+1$. Hence

$$
P_2^{(\alpha,\beta)}(1-2v)
=
\frac{(\alpha+1)(\alpha+2)}{2}
-
B(\alpha+2)v
+
\frac{B(B+1)}{2}v^2.
$$

A4’s use of $(B+1)(B+2)/2$ and $-(B+1)(\alpha+2)$ shifts all cubic coefficients. The $n=2$ certificate must be rebuilt from A3’s corrected compactified formula, not A4’s cubic.

Possible errors or hidden assumptions:

### A2

A2 overuses the word “proved.” The rational-coordinate residual identity is proved; the sign-definite kernel, the Watson product bound, the transition-tail constant, and the $1.04n^{2/3}$ threshold are not. The hidden assumption is that the Bessel reference comparison can be made one-sided without modulus inflation all the way to the first peak. That is exactly the hard part.

A2 also assumes a gamma-ratio envelope for $M_{n,\alpha,B}$ of the form $1+C/(n+1)$. This is not currently certified, and in previous rounds $M>1$ was already identified as a genuine issue.

### A3

A3’s algebra is strong, but it does not complete the artifact requested by the human Round 23 directive: a certified $n=1$ residual theorem and a final convention-audited Langer removable-value log. The hidden assumption is that algebra-bank readiness is enough for Round 23. It is not; the directive demanded at least one theorem-level artifact.

A3’s L23.9 Bessel residual is useful, but it should not be called “Langer residual” in the same sense as the global Airy/Langer residual $\Psi_B(\zeta)$. Those are different residual objects.

### A4

A4 has the most serious issues.

First, the half-order Bessel maximum arithmetic is wrong, though the desired inequality $<0.680$ survives.

Second, the low-degree critical equations are shifted by one in $B$. This invalidates A4’s $n=1$ root examples, the beta-zero factorization as stated, and the $n=2$ cubic.

Third, A4 claims the $n=1$ cap may be “trivialized” when the first maximum is outside the cap, but its test case is based on the wrong quadratic. With the corrected $n=1$ equation at $\alpha=1/2,\beta=0$, one root is

$$
v=\frac{\alpha(\alpha+1)}{B^2}=\frac{0.75}{6.25}=0.12,
$$

which lies inside the cap $v_\sigma=1/B=0.4$. So the cap is not trivially monotone in that example.

Fourth, A4 states a large-$n$ Volterra error $O(1/B)$ through the first lobe, but earlier rounds already found that such estimates can fail or become too lossy when $\alpha=O(n)$. A4’s statement “the integration domain is bounded by the first lobe $t_1=O(\sqrt n)$” is not justified uniformly in the residual strip; for large order Bessel behavior the first peak is governed by order-scale behavior.

Suggested synthesis:

The synthesis should preserve only the parts that survive audit.

1. **Promote A3’s endpoint-cap algebra.** Freeze L23.1–L23.8 and the Frobenius/Bessel normalization identity. Mark the global Airy/Langer removable value as pending unless A3 supplies the explicit cancellation log.

2. **Promote A2’s rational residual identity only as algebra.** Add the normal form

$$
Y''+
\left(
\frac{\Lambda_B}{z}
+
\frac{1-\alpha^2}{4z^2}
+
\Delta Q(z)
\right)Y=0,
\qquad
\Delta Q(z)=
-\frac{\Lambda_B}{B+z}
-\frac{\Delta_BB^2}{(B+z)^2}.
$$

Do not promote the sign-definite kernel, $O(\alpha^3/n^2)$ variation, or $1.04n^{2/3}$ threshold.

3. **Reject A4’s low-degree equations as written.** The $n=1$ and $n=2$ critical equations must be recomputed using the correct hypergeometric parameter $B$, not $B+1$.

4. **Keep the $n=1$ theorem as the immediate target.** The scalar-envelope route from Round 22 remains likely viable:

$$
E(\alpha)
=
\left(
\frac{
4\alpha^\alpha(\alpha+1)^{\alpha+2}
}{
(\alpha+2)^{\alpha+2}\Gamma(\alpha+2)
}
\right)^2
<
0.39
<
\frac58
\le
T_{1,\alpha,\beta}^4.
$$

The latest judge material says the constant $E(6/5)\approx0.383396$ leaves a large margin. :contentReference[oaicite:18]{index=18} This is still the best near-term theorem artifact.

5. **Use external references only as dependencies.** Landau, DGS/Olver, DLMF, Wendel/Gautschi/Kershaw, and Arb support theorem-building, but none should be cited as closing the KKT first-lobe estimate without the KKT-specific constants.

Research strategy adjustments:

Round 24 should be narrower than Round 23. The panel should not spend another round on broad rational-Bessel narrative, global Langer architecture, or simulated interval plans.

The next round should be an artifact-repair and certification round:

1. **Primary target:** complete the $n=1$ residual endpoint theorem. A1 and A4 should work on the same scalar inequality $E(\alpha)<0.39$, using rigorous interval or analytic trigamma/gamma bounds. A4 must not use the incorrect $B+1$ quadratic.

2. **Lemma-bank target:** A3 should finalize the endpoint-cap algebra and correct low-degree critical equations. It should explicitly include the corrected $n=1$ equation and corrected $n=2$ cubic in compactified form.

3. **Rational-Bessel target:** A2 should either prove the sign-definite Volterra kernel with exact hypotheses and constants or retract it and give an absolute-value Volterra bound. The output must contain an actual scalar inequality, not only $O(\alpha^3/n^2)$.

4. **Numerical certification target:** If computation is used, it must be outward-rounded. Arb is suitable, but “planned interval arithmetic” is not a result. :contentReference[oaicite:19]{index=19}

5. **Literature target:** A1 should write exact theorem statements for Landau and DGS/Olver in the notation actually used. Landau may justify $B_*<0.680$ only after the proof has genuinely reduced to a Bessel maximum. DGS may justify Airy error control only after the exact residual and interval of integration are inserted.

Score by agent:

| Agent reviewed | Score (0-10) | Main reason | Must verify next |
|---|---:|---|---|
| A2 | 6.2 | Valuable rational-coordinate residual and useful regime-split perspective, but overlabels kernel sign and Volterra scaling as proved; no explicit constants or finished theorem. | Prove or retract the sign-definite Volterra kernel; give a constant-level bound for $\int_0^{z_1}|\mathcal K(z,s)\Delta Q(s)|\,ds$ and derive a threshold from KKT slack. |
| A3 | 8.1 | Strongest algebra audit; endpoint ODE, $K_B$ quadratic, cap monotonicity, Frobenius coefficient, rational ODE, and invariant product are lemma-bank quality. | Finish the convention-sensitive Langer removable-value cancellation log and produce corrected compactified $n=1,n=2$ critical equations. |
| A4 | 3.8 | Useful intent and low-degree focus, but no actual certified enclosures; Bessel maximum arithmetic is wrong; $n=1$ and $n=2$ critical equations use an incorrect $B+1$ shift. | Recompute low-degree equations from $P_n(1-2v)=\frac{(\alpha+1)_n}{n!}{}_2F_1(-n,B;\alpha+1;v)$ and provide outward-rounded proof of $E(\alpha)<0.39$. |

Next-round recommendation:

### For A1

Write the Round 24 synthesis around a single theorem attempt:

**Degree-one residual endpoint certificate.**

Prove or isolate the last scalar gap in

$$
E(\alpha)
=
\left(
\frac{
4\alpha^\alpha(\alpha+1)^{\alpha+2}
}{
(\alpha+2)^{\alpha+2}\Gamma(\alpha+2)
}
\right)^2
<
0.39
\qquad
\left(\frac12\le\alpha\le\frac65\right).
$$

Use either rigorous interval enclosures or analytic bounds for $\log\Gamma$, $\psi$, and $\psi'$. Do not rely on floating values. Include a short literature appendix with exact Landau, DGS, Wendel/Gautschi/Kershaw, DLMF, and Arb dependencies.

### For A2

Produce one theorem-level rational-Bessel Volterra lemma or explicitly state failure.

Required content:

$$
Y''+
\left(
\frac{\Lambda_B}{z}
+
\frac{1-\alpha^2}{4z^2}
+
\Delta Q(z)
\right)Y=0,
\qquad
\Delta Q(z)=
-\frac{\Lambda_B}{B+z}
-\frac{\Delta_BB^2}{(B+z)^2}.
$$

Define the reference Bessel solutions exactly, including Wronskian and normalization. Then either:

- prove the kernel has a one-sided sign on the needed interval, with cited or derived Bessel zero/interlacing hypotheses; or
- replace it by a modulus bound.

The output must include a constant-level inequality for the Volterra integral. Do not use $O(\alpha^3/n^2)$ without constants.

### For A3

Freeze the lemma bank and correct the low-degree algebra.

Required corrections:

For $n=1$,

$$
P_1^{(\alpha,\beta)}(1-2v)=\alpha+1-Bv,
\qquad
B=\alpha+\beta+2,
$$

and the corrected critical equation is

$$
B^2v^2
-
\left(2\alpha^2+2\alpha\beta+5\alpha+3\beta+4\right)v
+
\alpha(\alpha+1)
=0.
$$

For $n=2$,

$$
P_2^{(\alpha,\beta)}(1-2v)
=
\frac{(\alpha+1)(\alpha+2)}{2}
-
B(\alpha+2)v
+
\frac{B(B+1)}{2}v^2,
\qquad
B=\alpha+\beta+3.
$$

Derive the corrected cubic and its compactified $\theta$-form. Also provide the requested CAS-style Langer residual cancellation log.

### For A4

Repair the $n=1$ and $n=2$ artifacts.

Do not provide pseudo-code in place of results. Produce one of:

1. an outward-rounded interval proof of $E(\alpha)<0.39$ on $[1/2,6/5]$; or
2. an analytic proof using rigorous rational enclosures for $\log\Gamma$, $\psi$, and $\psi'$.

Then begin $n=2$ with the corrected cubic from A3. Provide at least one real interval root-isolation log with parameter box, root interval, inclusion test, and interval evaluation of $H_2^4-T^4$.

Overall confidence:

- Confidence that A3’s endpoint-cap algebra should be frozen: **0.90**.
- Confidence that A2’s rational residual identity is correct: **0.85**.
- Confidence that A2’s current Volterra scaling is theorem-level: **0.30**.
- Confidence that A4’s current $n=1,n=2$ equations are correct: **0.20**.
- Confidence that the $n=1$ residual theorem is true and certifiable after correcting algebra: **0.85**.
- Confidence that Round 23 proves KKT: **0.05**.
- Confidence that endpoint-cap first-lobe plus low-degree certification plus regime-split amplitude remains the best route: **0.82**.