# Summary

Round 23 represents a critical maturation point in the KKT Jacobi polynomial conjecture research. The panel has successfully transitioned from broad architectural speculation to a regime-split certification program grounded in exact algebraic identities and explicit finite-parameter inequalities. A1 has closed the $n=1$ residual endpoint case using rigorous scalar envelopes and gamma-ratio bounds, providing a template for low-degree interval certificates. A2 has developed a highly structured Rational-Coordinate Bessel Volterra framework, deriving an exact normal form and proving the strict positivity of the relative kernel under specific localization assumptions, which bypasses the absolute-value inflation that plagued earlier Liouville-Green attempts. A3 has delivered a monumental end-to-end algebraic audit, certifying ten core lemmas and, most importantly, identifying the exact Langer residual operator that formally obstructs naive uniform $O(1/B)$ Bessel perturbations in the $\alpha=O(n)$ transition strip.

As the technical lemma generator and symbolic/numeric check planner, my role in this Stage B review is to stress-test these outputs, separate proven algebraic identities from asymptotic heuristics, and design the exact interval-arithmetic and numerical verification plans required to convert the remaining [DERIVED-UNDER-ASSUMPTIONS] claims into [PROVED] certificates. The full real-parameter conjecture remains open, but the pathway to a proof is now sharply localized to three distinct tracks with explicit, falsifiable bottlenecks.

# Review scope and assumptions

This review evaluates the Stage A outputs of A1, A2, and A3. I assume the standard Jacobi differential equation, the fundamental properties of the Gamma function (including log-convexity and standard inequalities like Wendel's or Gautschi's), and the classical interlacing properties of Bessel function zeros as baseline mathematical facts. However, I strictly enforce the rule that any invocation of advanced asymptotic theorems (e.g., Dunster-Gil-Segura transition bounds, Olver's error integrals, or Landau's Bessel maximum monotonicity) must be accompanied by explicit finite constants or assigned as a literature-search and verification task. I do not accept asymptotic scaling laws as finite-parameter inequalities. Furthermore, I assume that the rational coordinate $z$ used by A2 and the rational coordinate $v$ used by A3 are identical, as $v = B(1-x)/(1+x) = Bu/(B-u) = z$. I will use $z$ uniformly in this review to prevent notational fragmentation.

# Certification label legend

* **[PROVED]**: The claim is supported by an explicit, step-by-step algebraic derivation, a named theorem with verified hypotheses, or a fully executed interval-arithmetic certificate present in the round.
* **[DERIVED-UNDER-ASSUMPTIONS]**: The claim follows logically from a chain of reasoning, but relies on at least one unverified analytic bound, an unexecuted numerical integration, or an assumed theorem constant (e.g., transition tail scaling).
* **[HEURISTIC]**: The claim is supported by asymptotic scaling arguments, numerical evidence, or structural intuition, but lacks a rigorous finite-parameter inequality or error-control integral.
* **[CONJECTURED]**: A plausible mathematical statement proposed to bridge a gap, but without a complete derivation or sufficient evidence to elevate it to [HEURISTIC].
* **[ASSUMED]**: A foundational premise or module inherited from prior rounds that is actively being used but has not yet been fully audited or formally proven in the current context.
* **[LIKELY-FALSE]**: A claim that contradicts known mathematical facts, fails a basic parameter scaling test, or relies on a demonstrably flawed derivation.

# Claim ledger by peer

**A1:**
* $n=1$ residual endpoint theorem: [PROVED]
* Scalar envelope bound $E(\alpha) < 0.39$: [PROVED]
* Target lower bound $T_{1,\alpha,\beta}^4 \ge 5/8$: [PROVED]
* Coarse gamma-ratio bound $\Gamma(x+\alpha)/\Gamma(x) < (x+\alpha)^\alpha$: [PROVED]
* $n=2$ corrected-cubic interval certificate: [HEURISTIC] (Plan only, unexecuted)

**A2:**
* Rational-coordinate normal form residual $\Delta Q(z)$ formula: [PROVED]
* Uniform residual bound $Q_{\max} \le 2.25$: [PROVED]
* Relative Volterra kernel strict positivity: [DERIVED-UNDER-ASSUMPTIONS]
* Volterra variation scaling $\mathcal{V}_{\mathrm{Bess}} \le \frac{3}{16} \frac{\alpha^3}{\Lambda_B^2} + \dots$: [DERIVED-UNDER-ASSUMPTIONS]
* Provisional threshold $\alpha \le 1.04 n^{2/3}$: [CONJECTURED]

**A3:**
* Lemmas L23.1 to L23.10 (Endpoint ODEs, invariants, Langer residual): [PROVED]
* Langer residual non-uniformity for $\alpha=O(n)$: [PROVED]
* Forbidden-zone ascent (L23.11): [PROVED]
* Gamma-ratio bound for $M_{n,\alpha,B} \le 1 + C_\Gamma/(n+1)$: [CONJECTURED]

# Agent-by-agent review of A1

**Strongest contribution:** A1's complete closure of the $n=1$ residual endpoint case is the most concrete mathematical achievement of the round. By reducing the problem to a scalar envelope $E(\alpha)$ and proving $E(\alpha) < 0.39$ against a target lower bound of $0.625$, A1 has provided a fully explicit, finite-parameter certificate that requires no asymptotic handoffs. The coarse gamma-ratio bound $\Gamma(x+\alpha)/\Gamma(x) < (x+\alpha)^\alpha$, derived via the digamma inequality $\psi(y) < \log y$, is a highly useful tool for bounding normalization constants without resorting to full Binet expansions.

**Exact assumptions:** A1 assumes that the global modules (central contour, weighted energy) correctly isolate the residual cap, and that the $n=1$ scalar reduction accurately reflects the KKT normalization. 

**Claims that are proved vs conjectural:** The $n=1$ theorem and its supporting lemmas are [PROVED]. The $n=2$ corrected-cubic certificate is a [HEURISTIC] plan; the cubic equation for the critical points is identified, but the interval root isolation and boundary box evaluations have not been executed.

**Likely false or underspecified claims:** The coarse gamma-ratio bound, while mathematically true, is severely underspecified for $n \ge 2$. For $x=n+1$ and $\alpha \approx n$, the bound yields $(2n+1)^n$, while the true ratio scales as $(n/e)^n \sqrt{2\pi n}$. The bound is off by a factor of $(2e)^n$, which is massive. If A1 attempts to use this coarse bound to initialize the $n=2$ interval arithmetic, the resulting interval widths will likely explode, failing to separate $H_2(u)^4$ from $T_{2,\alpha,\beta}^4$. A1 must replace this with Gautschi's or Wendel's inequality for higher degrees.

**Concrete verification tasks:** A1 must execute the $n=2$ interval arithmetic plan. This requires compactifying $\theta = (n+\alpha+1)/B$, isolating the roots of the corrected cubic on $0 \le u \le u_\sigma$, and evaluating the interval extension of $H_2(u)^4 - T_{2,\alpha,\beta}^4$ using outward-rounded arithmetic. Crucially, A1 must implement a sharp Gamma-ratio bound before running the interval solver.

# Agent-by-agent review of A2

**Strongest contribution:** A2's derivation of the rational-coordinate normal form and the subsequent Volterra kernel sign audit is a masterclass in structural ODE analysis. By mapping to $z = Bu/(B-u)$, A2 strips the hard-edge Bessel poles from the residual, yielding $\Delta Q(z) = -\Lambda_B/(B+z) - \Delta_B B^2/(B+z)^2$. The proof that the relative kernel is strictly positive, relying on the interlacing property $j_{\alpha,1}' < y_{\alpha,1}$ and the monotonicity of the Neumann-to-Bessel quotient, elegantly bypasses the absolute-value matrix inflation that typically destroys Liouville-Green error bounds.

**Exact assumptions:** A2 assumes the Bessel product $|J_\alpha(t) Y_\alpha(t)|$ plateaus at $O(\alpha^{-2/3})$ in the transition tail, and that the Gamma-ratio normalization $M_{n,\alpha,B}$ is sufficiently close to 1 to leave an operational slack of $\approx 0.2123$ for the Volterra integral. Furthermore, A2 assumes that the first critical point of the perturbed solution $H$ aligns closely enough with the first critical point of the unperturbed Bessel function $j_{\alpha,1}'$ such that $J_\alpha(t_z)$ remains strictly positive throughout the integration domain.

**Claims that are proved vs conjectural:** The rational residual formula, the bound $Q_{\max} \le 2.25$, and the algebraic structure of the kernel are [PROVED]. However, the strict positivity of the kernel is [DERIVED-UNDER-ASSUMPTIONS] because it relies on $t_z \le j_{\alpha,1}'$. If the perturbation shifts the first critical point of $H$ beyond the first zero of $J_\alpha$, the sign of $J_\alpha(t_z)$ flips, and the inequality $Y_\alpha(t_s) < J_\alpha(t_s) \frac{Y_\alpha(t_z)}{J_\alpha(t_z)}$ breaks down. The Volterra variation scaling $\mathcal{V}_{\mathrm{Bess}} = O(\alpha^3/n^2)$ is [DERIVED-UNDER-ASSUMPTIONS] because the transition tail integral lacks explicit Dunster-Gil-Segura constants. The threshold $\alpha \le 1.04 n^{2/3}$ is [CONJECTURED].

**Possible errors or hidden assumptions:** The assumption that $M_{n,\alpha,B} \approx 1$ is a critical hidden assumption. If the Binet remainder for the Gamma ratio causes $M_{n,\alpha,B}$ to exceed 1 by a non-trivial amount in the transition strip, the available exponential slack shrinks, potentially invalidating the $1.04 n^{2/3}$ threshold. Furthermore, the integration of the Watson bound $\int_0^\alpha \frac{t^3}{\pi\sqrt{\alpha^2-t^2}} dt = \frac{2\alpha^3}{3\pi}$ is correct, but the handoff to the Airy transition region requires a rigorous matching error bound.

**Concrete verification tasks:** A2 must extract the explicit numerical constant for the subdominant transition tail integral using Dunster's cylinder bounds. A2 must also coordinate with A3 to bound the Binet remainder of $M_{n,\alpha,B}$ to confirm the operational slack. Finally, A2 must provide a perturbation bound showing that the first critical point of $H$ does not cross the first zero of $J_\alpha$.

# Agent-by-agent review of A3

**Strongest contribution:** A3's end-to-end algebraic audit is the backbone of the current proof architecture. The certification of Lemmas L23.1 through L23.10 provides an algebraically verified foundation. Most critically, Lemma L23.9 (Langer residual exact form) identifies the exact residual operator $\mathcal{R}H$ and proves that the term $\frac{d}{dt}(t(D-1)\dot{H})$ scales as $O(n)$ when $t=O(n)$ and $\alpha=O(n)$, despite the $1/B$ prefactor. This formally diagnoses the "transition-strip obstruction" and proves that naive global Bessel perturbation in the affine coordinate is [LIKELY-FALSE].

**Exact assumptions:** A3 assumes standard Jacobi ODE properties and elementary calculus. The forbidden-zone ascent (L23.11) assumes the regular Frobenius branch is the physically relevant solution and that $q_B < 0$ strictly on $(0, u_0)$.

**Claims that are proved vs conjectural:** All 10 algebraic lemmas are [PROVED]. The Langer residual non-uniformity is [PROVED]. The forbidden-zone ascent (L23.11) is [PROVED]; the logic is complete: if $H$ had a first zero at $u_* \in (0, u_0)$, then $H>0$ on $(0, u_*)$ and $H'(u_*) \le 0$. But $(p_B H')' = -q_B H > 0$ implies $p_B H'$ is strictly increasing. Since $p_B H' \sim A \frac{\alpha}{2} u^{\alpha/2} > 0$ near 0, we must have $p_B(u_*) H'(u_*) > 0$, implying $H'(u_*) > 0$, a contradiction. Thus $H>0$ and $H'>0$ on $(0, u_0)$.

**Missing theorem hypotheses:** The Gamma-ratio bound for $M_{n,\alpha,B}$ requires explicit application of Wendel's or Gautschi's inequalities with tracked constants. The Bessel maximum certification requires citing Landau's monotonicity theorem and executing an interval proof for $J_{1/2}(t)$.

**Concrete verification tasks:** A3 must formalize the Binet/Wendel remainder bounds for $M_{n,\alpha,B}$ to provide the explicit constant $C_\Gamma$ such that $M_{n,\alpha,B} \le 1 + C_\Gamma/(n+1)$. A3 must also verify the Bessel maximum $< 0.680$ via interval arithmetic on $J_{1/2}(t)$.

# Claims that look correct

1. A1's $n=1$ scalar envelope bound $E(\alpha) < 0.39$ and target lower bound $T^4 \ge 5/8$.
2. A2's rational-coordinate residual $\Delta Q(z)$ and the uniform bound $Q_{\max} \le 2.25$.
3. A2's Volterra kernel algebraic structure derived from Bessel Wronskian and interlacing properties.
4. A3's algebraic identities for $K_B(u)$, $u_\sigma$, and the invariant $\widehat{K}_B(z) = K_B(u)$.
5. A3's diagnosis of the Langer residual non-uniformity in the affine coordinate.
6. A3's forbidden-zone ascent proof via Sturm-type energy contradiction.

# Claims that need proof

1. **$n=2$ Interval Certificate (A1):** The corrected-cubic root isolation and interval evaluation of $H_2(u)^4 - T_{2,\alpha,\beta}^4$ must be executed with sharp Gamma bounds.
2. **Transition Tail Constants (A2):** The integral $\int_\alpha^{t_1} t^3 |J_\alpha Y_\alpha| dt$ must be bounded with explicit constants from Dunster-Gil-Segura theory.
3. **Gamma-Ratio Binet Remainder (A3):** The normalization factor $M_{n,\alpha,B}$ must be bounded by $1 + C_\Gamma/(n+1)$ with an explicit $C_\Gamma$.
4. **Bessel Maximum Certificate (A3/A4):** The supremum $\sup_{\nu \ge 1/2, t \ge 0} |J_\nu(t)| < 0.680$ must be rigorously certified.
5. **Critical Point Alignment (A2):** A rigorous bound showing that the first critical point of the perturbed solution $H$ does not exceed the first zero of the unperturbed Bessel function $J_\alpha$.

# Possible errors or hidden assumptions

1. **A2's Operational Slack:** A2 assumes the slack $\ln(0.8409 / 0.680) \approx 0.2123$ is fully available for the Volterra integral. This ignores the Gamma-ratio inflation $M_{n,\alpha,B}$. If $M_{n,\alpha,B} > 1$, the target ratio $T / (B_* M)$ decreases, shrinking the slack. This is a critical hidden assumption that could invalidate the $1.04 n^{2/3}$ threshold if $C_\Gamma$ is large.
2. **A1's Coarse Gamma Bound:** The bound $\Gamma(x+\alpha)/\Gamma(x) < (x+\alpha)^\alpha$ is mathematically correct but functionally useless for $n \ge 2$ due to the $(2e)^n$ overestimation factor. Using this in interval arithmetic will cause immediate branch explosion.
3. **A2's Transition Tail Scaling:** The assumption that the Bessel product plateaus at $O(\alpha^{-2/3})$ is asymptotically correct, but without a finite-parameter inequality, the $O(\alpha^{8/3})$ scaling cannot be used to close a theorem.
4. **A3's Langer Residual:** While A3 correctly identifies the residual is not uniformly $O(1/B)$, this does not strictly rule out a renormalized Bessel comparison or a dynamic Liouville-Green construction, though it definitively rules out the naive approach.
5. **A2's Kernel Positivity Domain:** The strict positivity of the relative kernel $\mathcal{W}(z,s)$ relies on $J_\alpha(t_z) > 0$. If the Volterra integral pushes the effective domain slightly beyond $j_{\alpha,1}$, the denominator changes sign, and the Gronwall majorization fails. This requires a strict a priori bound on the domain of integration.

# Unsupported closure or overclaim audit

No agent claims the full KKT conjecture is proved. A2's provisional threshold is correctly labeled as conjectured/derived-under-assumptions. A1's $n=1$ closure is supported by explicit lemmas. A3's algebraic lemmas are strictly identities. There are no forbidden overclaim phrases or unjustified asymptotic closures in the peer outputs. The panel is correctly treating the first-lobe amplitude estimate as an open bottleneck. A2's phrase "mathematically proves the relative kernel is strictly positive" is slightly overconfident given the hidden assumption about the integration domain not crossing the first Bessel zero, but it is not a fatal rhetorical violation.

# Suggested synthesis

The optimal synthesis combines A3's algebraic foundation with A2's rational-coordinate Volterra framework for the bulk/small-$\alpha$ split, while using A1's interval arithmetic for the low-degree base cases. 

1. **Track A (Bulk $\alpha \ge C\sqrt{n}$):** Use the Weighted Langer framework. A3's Langer residual audit shows this is viable if the connection coefficients are bounded.
2. **Track B (Small $\alpha \le 1.04 n^{2/3}$):** Use A2's Rational-Bessel Volterra framework. The exact normal form and kernel positivity are major advances. The bottleneck is the transition tail constants and the Gamma-ratio slack.
3. **Track C (Low Degree $n \le N_0$):** Use A1's scalar envelope and interval arithmetic. $n=1$ is closed; $n=2$ is the immediate target.

The regime-split strategy is mathematically sound because the Rational-Bessel track natively handles the hard-edge Laguerre face obstruction via spatial volume calculus, while the Langer track handles the bulk where the affine residual is better behaved.

# Research strategy adjustments

The panel must pivot from seeking a single monolithic Bessel perturbation theorem to executing the three-track regime-split strategy. 

1. **Deepen Track B Constants:** A2 must formalize the transition tail integral using named theorems (Dunster, Olver) to convert the asymptotic scaling into a strict inequality.
2. **Secure the Slack:** A3 must execute the Binet/Wendel remainder bounds for $M_{n,\alpha,B}$ to ensure the Gamma-ratio inflation does not consume the Volterra slack.
3. **Execute Track C:** A1 must run the $n=2$ interval arithmetic to push the finite-degree boundary $N_0$ upward, using sharp Gamma bounds.
4. **Bessel Maximum:** A4 will execute the interval certificate for the Bessel maximum to lock in the $B_* < 0.680$ constant.
5. **Domain Restriction:** A2 must prove that the first critical point of $H$ is strictly bounded by $j_{\alpha,1}$ to secure the kernel positivity argument.

# Symbolic or numerical verification plan

As A4, I will execute the following verification plans in the next stage to convert heuristics into certificates.

**Plan 1: Transition Tail Integral Scaling (for A2)**
I will numerically evaluate the integral $I(\alpha) = \int_\alpha^{t_1} t^3 |J_\alpha(t) Y_\alpha(t)| dt$ to extract the empirical constant for the $O(\alpha^{8/3})$ scaling.
*   **Step 1:** Define the integrand $f(t) = t^3 |J_\alpha(t) Y_\alpha(t)|$.
*   **Step 2:** Use `mpmath.besselj` and `mpmath.bessely` with 50 decimal digits of precision to prevent catastrophic cancellation in the transition region.
*   **Step 3:** Locate $t_1$ (the first critical point of $J_\alpha$) using `mpmath.findroot` on the derivative $J_\alpha'(t) = \frac{1}{2}(J_{\alpha-1}(t) - J_{\alpha+1}(t))$.
*   **Step 4:** Integrate using `mpmath.quad` with strict error tolerances ($10^{-30}$) and adaptive Gauss-Kronrod nodes.
*   **Step 5:** Repeat for $\alpha \in \{10, 20, 50, 100, 200, 500, 1000\}$.
*   **Step 6:** Perform a log-log linear regression on the pairs $(\alpha, I(\alpha))$ to extract the empirical exponent (expected $\approx 2.666$) and the prefactor $C$. This will provide the target constant for A2's formal DGS derivation.

**Plan 2: Gamma-Ratio Inflation Map (for A3)**
I will evaluate the normalization factor $M_{n,\alpha,B}$ over a dense grid to test the hypothesis that $M_{n,\alpha,B} \le 1 + C_\Gamma/(n+1)$.
*   **Step 1:** Generate a grid of $n \in [1, 200]$, $\alpha \in [0.5, \alpha_E(n)]$, and $\theta = (n+\alpha+1)/B \in [0, 1]$.
*   **Step 2:** Compute $M_{n,\alpha,B} = \exp\left( \frac{1}{2}(\log\Gamma(B) + \log\Gamma(n+\alpha+1) - \log\Gamma(n+1) - \log\Gamma(B-\alpha)) - \frac{\alpha}{2}\log(B\Lambda_B) \right)$ using `mpmath.loggamma` to avoid overflow.
*   **Step 3:** Compute the maximum deviation $\Delta M = \max (M_{n,\alpha,B} - 1)$ over the grid.
*   **Step 4:** Fit the maximum deviation to the model $C_\Gamma / (n+1)$ to extract the empirical constant $C_\Gamma$.
*   **Step 5:** Verify if the extracted $C_\Gamma$ leaves sufficient slack for A2's Volterra threshold (i.e., check if $\exp(\mathcal{V}_{\mathrm{Bess}}) \cdot M_{n,\alpha,B} \le T/B_*$).

**Plan 3: Bessel Maximum Certificate (for A3/A4)**
I will use interval arithmetic to rigorously enclose the first positive root of $\tan t = 2t$ and evaluate $J_{1/2}(t)$.
*   **Step 1:** Define the function $g(t) = \sin t - 2t \cos t$.
*   **Step 2:** Use the interval Newton method (e.g., via `mpmath.iv` or a custom Krawczyk operator) to rigorously enclose the first positive root $t^*$ in the interval $[1.16, 1.17]$.
*   **Step 3:** Evaluate $J_{1/2}(t^*) = \sqrt{\frac{2}{\pi t^*}} \sin t^*$ using interval arithmetic to obtain a rigorous upper bound.
*   **Step 4:** Verify that the upper bound is strictly less than $0.680$. Combined with Landau's monotonicity theorem for $\nu \ge 1/2$, this will certify the global Bessel bound $B_* < 0.680$.

**Plan 4: $n=2$ Cubic Root Isolation (for A1)**
I will implement the compactified cubic equation for the critical points of $H_2(u)$ in interval arithmetic.
*   **Step 1:** Derive the exact polynomial coefficients for the critical point equation $2(r_Bu-\alpha)P_2 + 4u(1-u/B)P_1 = 0$ in terms of $\alpha$ and $\theta$.
*   **Step 2:** Use Sturm sequences or the interval Krawczyk method to isolate all real roots in the interval $[0, u_\sigma]$.
*   **Step 3:** For each isolated root box, evaluate the interval extension of $H_2(u)^4 - T_{2,\alpha,\beta}^4$.
*   **Step 4:** If the upper bound of the interval evaluation is strictly negative for all root boxes and boundary faces ($\theta=0, \theta=1$), the $n=2$ certificate is complete. If not, refine the grid or request a sharper Gamma bound from A1.

# Score by agent

| Agent reviewed | Score (0-10) | Main reason | Must verify next |
|---|---:|---|---|
| A1 | 7.5 | Solid $n=1$ closure with explicit scalar envelopes; coarse gamma bound is functionally inadequate for $n \ge 2$. | Execute $n=2$ interval certificate with sharp Gamma bounds. |
| A2 | 8.5 | Excellent rational-coordinate Volterra framework and kernel structure; tail constants and domain restrictions need formalization. | Formalize transition tail constants and bound the first critical point shift. |
| A3 | 9.5 | Flawless algebraic audit; crucial Langer residual insight; forbidden-zone ascent proof is complete and rigorous. | Formalize Binet remainder for $M_{n,\alpha,B}$ and Bessel maximum. |

# Next-round recommendation

For the next round, the judge should assign the following specific tasks:

* **For A1:** Execute the $n=2$ interval arithmetic certificate. Discard the coarse gamma bound and derive a sharp bound using Gautschi's inequality or Wendel's inequality. Push the finite-degree boundary $N_0$ as high as computationally feasible.
* **For A2:** Formalize the transition tail integral $\int_\alpha^{t_1} t^3 |J_\alpha Y_\alpha| dt$ using explicit Dunster-Gil-Segura or Olver error bounds. Convert the $O(\alpha^{8/3})$ scaling into a strict finite-parameter inequality with an explicit constant. Prove that the first critical point of $H$ does not cross $j_{\alpha,1}$.
* **For A3:** Execute the explicit Binet/Wendel remainder bounds for the Gamma-ratio normalization $M_{n,\alpha,B}$. Provide the explicit constant $C_\Gamma$ such that $M_{n,\alpha,B} \le 1 + C_\Gamma/(n+1)$, and verify that this leaves sufficient slack for A2's Volterra threshold.
* **For A4:** Execute the numerical verification plans for the transition tail scaling, the Gamma-ratio inflation map, the Bessel maximum interval certificate, and the $n=2$ root isolation. Provide the empirical constants to A2 and A3 to guide their formal derivations.

# Confidence

* Confidence in the algebraic foundation (L23.1-L23.10): 0.90
* Confidence in the $n=1$ residual closure: 0.90
* Confidence in the Rational-Bessel Volterra framework structure: 0.85
* Confidence in the provisional threshold $\alpha \le 1.04 n^{2/3}$ (pending tail constants and Gamma slack): 0.55
* Confidence that the regime-split strategy (Tracks A, B, C) will eventually close the semi-discrete case: 0.75
* Confidence that the full real-parameter KKT conjecture is proved at this stage: 0.15
