Summary:

Source/task acknowledgement: the Round 29 A1 prompt asks A1 to turn the Round 28 residual $n=2$ certificate program into executed certificate artifacts, without introducing a new proof architecture, and specifically asks for the compactified $n=2$ theorem, polynomial derivation, critical cubic, boundary-face status, at least one new rational subdivision, and a status table.

I do not claim the KKT conjecture is solved. I also do not claim the full residual $n=2$ rectangle is solved. The concrete progress in this Stage A response is:

1. I rederive the compactified $n=2$ endpoint polynomial from the finite hypergeometric formula.

2. I rederive the critical cubic from the exact critical-point equation and clarify the scaling: after substituting $B=(\alpha+3)/\theta$, the displayed expanded cubic is $2(\alpha+3)$ times the compactified factored equation, where the compactified factored equation is the raw critical equation multiplied by $\theta$. Equivalently, it is $2\theta(\alpha+3)$ times the uncompactified rational equation for $\theta>0$.

3. I give a formal residual $n=2$ theorem statement in compactified variables.

4. I record the boundary-face status. The handoff faces $\alpha=1/2$ and $\alpha=15/7$ are controlled by imported modules. The one-dimensional faces $\theta=0$ and $\theta=1$ are not completely resolved here; I give formulas and endpoint diagnostics, and identify the missing root-isolation/value-enclosure work.

5. I give a provisional certificate for one new interior rational box beyond the Round 27 subbox. The proposed box is

$$
Q_{\mathrm{new}}
=
\left[\frac32,\frac85\right]
\times
\left[\frac14,\frac3{10}\right]
\subset
(\alpha,\theta)\text{-space}.
$$

On this box I prove that the first critical point after the cap turning point lies in

$$
U_1=\left(\frac12,\frac35\right),
$$

and that throughout this box

$$
\frac{H_2(u_1)^4}{T_{2,\alpha,\beta}^4}<1.
$$

The proof uses summarized exact rational Bernstein sign checks for the critical cubic and a crude but rigorous gamma-ratio bound. The final rational comparison is

$$
\frac{H_2(u_1)^4}{T_{2,\alpha,\beta}^4}
\le
\frac{1122208144817949}{1181640625000000}
<1.
$$

This is a genuine new candidate interior subbox certificate, but permanent certified status requires archiving the full Bernstein coefficient lists or a reproducible exact script. It is not a full $n=2$ proof.

Main claim or direction:

The selected route should remain the endpoint-cap first-lobe route with low-degree certification. For $n=2$, the immediate correct proof goal is not a new global theorem but a finite certificate cover of

$$
\frac12<\alpha<\frac{15}{7},
\qquad
0\le\theta\le1,
$$

where

$$
\theta=\frac{\alpha+3}{B},
\qquad
B=2+\alpha+\beta+1=\alpha+\beta+3.
$$

For $\theta>0$,

$$
B=\frac{\alpha+3}{\theta},
\qquad
\beta=B-\alpha-3
=
\frac{\alpha+3}{\theta}-\alpha-3
=
\frac{(\alpha+3)(1-\theta)}{\theta}.
$$

The face $\theta=0$ is the Laguerre face and must be handled by limiting formulas, not by substituting $B=\infty$ into finite-$B$ expressions.

For $n=2$, the residual theorem to certify is:

**Residual $n=2$ theorem.**
Assume

$$
\frac12<\alpha<\frac{15}{7},
\qquad
0\le\theta\le1,
$$

with $\theta=0$ interpreted as the Laguerre face and $\theta>0$ interpreted by $B=(\alpha+3)/\theta$. Let

$$
H_2(u)=g_2^{(\alpha,\beta)}\left(1-\frac{2u}{B}\right)
$$

for $\theta>0$, and let $H_{2,\infty}(u)$ denote the corresponding normalized Laguerre limit for $\theta=0$. Let $u_0$ be the first zero of the endpoint Sonin product $K_B$ in the cap, and let $u_1$ be the first critical point of $H_2$ after $u_0$, if such a point exists. Then the target is

$$
|H_2(u_1)|
\le
T_{2,\alpha,\beta}
=
\left(
\frac{3B}{(\alpha+3)(B-\alpha)}
\right)^{1/4}.
$$

In compactified form, for $\theta>0$,

$$
T_{2,\alpha,\theta}^4
=
\frac{3B}{(\alpha+3)(B-\alpha)}
=
\frac{3}{\alpha+3-\alpha\theta}.
$$

At the Laguerre face,

$$
T_{2,\alpha,0}^4
=
\frac{3}{\alpha+3}.
$$

The provisional new subbox certificate in this response addresses this residual theorem only on

$$
\frac32\le\alpha\le\frac85,
\qquad
\frac14\le\theta\le\frac3{10}.
$$

Detailed reasoning:

## 1. Compactified finite hypergeometric derivation for $n=2$

The finite hypergeometric formula is

$$
P_n^{(\alpha,\beta)}\left(1-\frac{2u}{B}\right)
=
\frac{(\alpha+1)_n}{n!}
{}_2F_1\left(-n,B;\alpha+1;\frac{u}{B}\right),
$$

where

$$
B=n+\alpha+\beta+1.
$$

For $n=2$,

$$
B=\alpha+\beta+3,
\qquad
\theta=\frac{\alpha+3}{B}.
$$

The polynomial is

$$
P_{2,\alpha,\theta}(u)
=
\frac{(\alpha+1)(\alpha+2)}2
\sum_{k=0}^{2}
\frac{(-2)_k(B)_k}{(\alpha+1)_k k!}
\left(\frac{u}{B}\right)^k.
$$

The $k=0$ term is

$$
\frac{(\alpha+1)(\alpha+2)}2.
$$

The $k=1$ term is

$$
\frac{(\alpha+1)(\alpha+2)}2
\cdot
\frac{-2B}{\alpha+1}
\cdot
\frac{u}{B}
=
-(\alpha+2)u.
$$

The $k=2$ term is

$$
\frac{(\alpha+1)(\alpha+2)}2
\cdot
\frac{(-2)_2(B)_2}{(\alpha+1)_2 2!}
\cdot
\frac{u^2}{B^2}.
$$

Since

$$
(-2)_2=2,
\qquad
(B)_2=B(B+1),
\qquad
(\alpha+1)_2=(\alpha+1)(\alpha+2),
$$

this is

$$
\frac{B+1}{2B}u^2
=
\frac12\left(1+\frac1B\right)u^2
=
\frac12\left(1+\frac{\theta}{\alpha+3}\right)u^2.
$$

Therefore

$$
\boxed{
P_{2,\alpha,\theta}(u)
=
\frac{(\alpha+1)(\alpha+2)}2
-(\alpha+2)u
+
\frac12\left(1+\frac{\theta}{\alpha+3}\right)u^2.
}
$$

This identity is certified algebra.

## 2. Critical-point equation and cubic

For general $n$, differentiating the weighted endpoint expression gives

$$
H'(u)=0
$$

if and only if

$$
\left(\alpha(B-u)-\beta u\right)P(u)
+
2u(B-u)P'(u)=0.
$$

For $n=2$ and $\theta>0$,

$$
B=\frac{\alpha+3}{\theta},
\qquad
\beta=\frac{\alpha+3}{\theta}-\alpha-3.
$$

Substituting the compactified polynomial and multiplying by the positive compactifying factor gives the cubic

$$
C_{\alpha,\theta}(u)=0,
$$

where

$$
\boxed{
\begin{aligned}
C_{\alpha,\theta}(u)
={}&
-(\alpha+\theta+3)^2u^3\\
&+(\alpha+3)(3\alpha^2-\alpha\theta+17\alpha+24)u^2\\
&-(\alpha+2)(\alpha+3)(3\alpha^2-3\alpha\theta+14\alpha-3\theta+15)u\\
&+\alpha(\alpha+1)(\alpha+2)(\alpha+3)^2.
\end{aligned}
}
$$

More precisely, let

$$
E_{\alpha,\theta}(u)
=
\left(\alpha(B-u)-\beta u\right)P(u)
+
2u(B-u)P'(u).
$$

After substituting $B=(\alpha+3)/\theta$, the expression $E_{\alpha,\theta}$ is rational in $\theta$. The compactified factored equation is

$$
\widetilde E_{\alpha,\theta}(u)=\theta E_{\alpha,\theta}(u)=0.
$$

Then

$$
C_{\alpha,\theta}(u)
=
2(\alpha+3)\widetilde E_{\alpha,\theta}(u)
=
2\theta(\alpha+3)E_{\alpha,\theta}(u).
$$

Thus $C_{\alpha,\theta}=0$ is equivalent to the critical-point condition for $\theta>0$, and also has a well-defined limiting cubic at $\theta=0$.

## 3. Endpoint cap and target ratio for $n=2$

For $n=2$,

$$
u_\sigma
=
\frac{2B}{B+1}.
$$

In compact variables this is

$$
u_\sigma
=
\frac{2(\alpha+3)}{\alpha+3+\theta}.
$$

In particular,

$$
0\le u\le u_\sigma<2
$$

for $\theta>0$, and

$$
u_\sigma\to2
$$

at the Laguerre face $\theta=0$.

For $\theta>0$, the normalized endpoint function satisfies

$$
H_2(u)^4
=
\left(
\frac{2\Gamma(B)}{\Gamma(\alpha+3)\Gamma(B-\alpha)}
\right)^2
\left(\frac{u}{B}\right)^{2\alpha}
\left(1-\frac{u}{B}\right)^{2\beta}
P_{2,\alpha,\theta}(u)^4.
$$

Dividing by

$$
T_{2,\alpha,\theta}^4=\frac{3}{\alpha+3-\alpha\theta}
$$

gives

$$
\mathcal R_2(\alpha,\theta,u)
=
\frac{H_2(u)^4}{T_{2,\alpha,\theta}^4}
$$

with

$$
\mathcal R_2(\alpha,\theta,u)
=
\left(
\frac{2\Gamma(B)}{\Gamma(\alpha+3)\Gamma(B-\alpha)}
\right)^2
\left(\frac{u}{B}\right)^{2\alpha}
\left(1-\frac{u}{B}\right)^{2\beta}
P_{2,\alpha,\theta}(u)^4
\frac{\alpha+3-\alpha\theta}{3}.
$$

At $\theta=0$, the limiting Laguerre expression is

$$
\boxed{
\mathcal R_2(\alpha,0,u)
=
\left(\frac{2}{\Gamma(\alpha+3)}\right)^2
u^{2\alpha}e^{-2u}
P_{2,\alpha,0}(u)^4
\frac{\alpha+3}{3}.
}
$$

## 4. Boundary-face status

### 4.1 The face $\alpha=1/2$

This face is controlled by the imported small-endpoint exponent theorem, which covers the right endpoint for

$$
0\le\alpha\le\frac12.
$$

For the $n=2$ residual rectangle, the exact handoff is:

$$
\alpha=\frac12,\qquad 0\le\theta\le1.
$$

No new $n=2$ certificate is needed on this face, provided the small-exponent module is accepted with the same normalization $g_n^{(\alpha,\beta)}$ and the same target constant $T_{n,\alpha,\beta}$.

Status: certified conditional on the imported small-exponent theorem.

### 4.2 The face $\alpha=15/7$

For $n=2$,

$$
\alpha_E(2)
=
\frac{(2\cdot2+1)(2+1)}{2\cdot2+3}
=
\frac{15}{7}.
$$

The face

$$
\alpha=\frac{15}{7}
$$

is the energy-handoff boundary. It is controlled by the imported energy theorem if that theorem is stated in closed form at $\alpha=\alpha_E(n)$.

Status: certified conditional on the imported energy theorem and boundary inclusion.

### 4.3 The Laguerre face $\theta=0$

The limiting formulas are certified:

$$
P_{2,\alpha,0}(u)
=
\frac{(\alpha+1)(\alpha+2)}2
-(\alpha+2)u
+\frac12u^2,
$$

and

$$
\mathcal R_2(\alpha,0,u)
=
\left(\frac{2}{\Gamma(\alpha+3)}\right)^2
u^{2\alpha}e^{-2u}
P_{2,\alpha,0}(u)^4
\frac{\alpha+3}{3}.
$$

The critical cubic is

$$
C_{\alpha,0}(u)
=
-(\alpha+3)^2u^3
+
(\alpha+3)(3\alpha^2+17\alpha+24)u^2
-
(\alpha+2)(\alpha+3)(3\alpha^2+14\alpha+15)u
+
\alpha(\alpha+1)(\alpha+2)(\alpha+3)^2.
$$

High-precision diagnostics at the two endpoint exponents are:

At $\alpha=1/2$,

$$
u_0\approx0.0227744249483,
\qquad
u_1\approx0.1242988632012,
\qquad
\mathcal R_2(\alpha,0,u_1)\approx0.2501167029.
$$

At $\alpha=15/7$,

$$
u_0\approx0.3290057041647,
\qquad
u_1\approx0.7531025657322,
\qquad
\mathcal R_2(\alpha,0,u_1)\approx0.0923520450.
$$

These diagnostics show large numerical slack, but they are not a full one-dimensional certificate for all $\alpha\in[1/2,15/7]$.

Status: unresolved as a complete boundary certificate. Needed next: rational interval subdivision in $\alpha$, with root isolation for $C_{\alpha,0}$ and log-gamma or Binet/Stirling enclosure for $\Gamma(\alpha+3)$.

### 4.4 The finite face $\theta=1$

Here $\beta=0$, because

$$
B=\alpha+3,
\qquad
\beta=B-\alpha-3=0.
$$

The target simplifies to

$$
T_{2,\alpha,1}^4=1.
$$

The critical cubic is

$$
C_{\alpha,1}(u)
=
-(\alpha+4)^2u^3
+
(\alpha+3)(3\alpha^2-\alpha+17\alpha+24)u^2
-
(\alpha+2)(\alpha+3)(3\alpha^2-3\alpha+14\alpha-3+15)u
+
\alpha(\alpha+1)(\alpha+2)(\alpha+3)^2.
$$

Equivalently,

$$
\begin{aligned}
C_{\alpha,1}(u)
={}&
-(\alpha+4)^2u^3\\
&+(\alpha+3)(3\alpha^2+16\alpha+24)u^2\\
&-(\alpha+2)(\alpha+3)(3\alpha^2+11\alpha+12)u\\
&+\alpha(\alpha+1)(\alpha+2)(\alpha+3)^2.
\end{aligned}
$$

High-precision endpoint diagnostics are:

At $\alpha=1/2$,

$$
u_0\approx0.0299145299145,
\qquad
u_1\approx0.1621732127122,
\qquad
\mathcal R_2(\alpha,1,u_1)\approx0.2262249956.
$$

At $\alpha=15/7$,

$$
u_0\approx0.4721105088302,
\qquad
u_1\approx1.0472222040614,
\qquad
\mathcal R_2(\alpha,1,u_1)\approx0.0696566056.
$$

Again the numerical slack is large, but the whole face is not certified here.

Status: unresolved as a complete boundary certificate. Needed next: one-dimensional interval root isolation and value bounds for $\alpha\in[1/2,15/7]$.

## 5. New provisional interior rational subbox

I now give a new provisional subbox certificate beyond the Round 27 subbox. The Round 27 subbox was

$$
1\le\alpha\le\frac{11}{10},
\qquad
\frac12\le\theta\le\frac35.
$$

The new box is disjoint from that box:

$$
\boxed{
Q_{\mathrm{new}}
=
\left[\frac32,\frac85\right]
\times
\left[\frac14,\frac3{10}\right].
}
$$

Thus

$$
\frac32\le\alpha\le\frac85,
\qquad
\frac14\le\theta\le\frac3{10}.
$$

The proposed root interval is

$$
\boxed{
U_1=\left[\frac12,\frac35\right].
}
$$

### 5.1 Cap inclusion

For $n=2$,

$$
u_\sigma=
\frac{2(\alpha+3)}{\alpha+3+\theta}.
$$

On $Q_{\mathrm{new}}$ this is minimized at $\alpha=3/2$, $\theta=3/10$, hence

$$
u_\sigma
\ge
\frac{2(9/2)}{9/2+3/10}
=
\frac{9}{24/5}
=
\frac{15}{8}
>
\frac35.
$$

Thus the whole interval $U_1=[1/2,3/5]$ lies inside the cap.

### 5.2 Root isolation for the critical cubic

Using the tensor Bernstein basis after the affine transformation of

$$
(\alpha,\theta,u)\in
\left[\frac32,\frac85\right]
\times
\left[\frac14,\frac3{10}\right]
\times
I
$$

to the unit cube, the following exact rational coefficient extrema are reported. These extrema are enough to state the intended sign checks, but the permanent archive should also include the full coefficient lists or the exact script that generated them.

On

$$
I=\left[0,\frac12\right],
$$

all Bernstein coefficients of $C_{\alpha,\theta}(u)$ are positive; the minimum coefficient is

$$
\frac{503}{128}>0.
$$

Therefore

$$
C_{\alpha,\theta}(u)>0
$$

for all

$$
(\alpha,\theta,u)\in Q_{\mathrm{new}}\times\left[0,\frac12\right].
$$

At

$$
u=\frac35,
$$

all Bernstein coefficients of $C_{\alpha,\theta}(3/5)$ over $Q_{\mathrm{new}}$ are negative; the maximum coefficient is

$$
-\frac{203103}{12500}<0.
$$

Therefore

$$
C_{\alpha,\theta}\left(\frac35\right)<0
$$

for all $(\alpha,\theta)\in Q_{\mathrm{new}}$.

Finally, on

$$
u\in\left[\frac12,\frac35\right],
$$

all Bernstein coefficients of $\partial C_{\alpha,\theta}/\partial u$ are negative; the maximum coefficient is

$$
-\frac{1807191}{5000}<0.
$$

Therefore $C_{\alpha,\theta}$ is strictly decreasing on $[1/2,3/5]$ for every parameter in $Q_{\mathrm{new}}$.

Combining these signs:

- $C>0$ on $[0,1/2]$;
- $C(3/5)<0$;
- $C_u<0$ on $[1/2,3/5]$;

there is exactly one critical point in

$$
\left(\frac12,\frac35\right),
$$

and no earlier positive critical point before $1/2$.

### 5.3 Location after the first turning point

For $n=2$,

$$
K_B(u)=
-\frac{\alpha^2}{4}
+\Lambda_Bu
-\Delta_Bu^2.
$$

In compactified variables, the numerator of $K_B(1/2)$ after clearing the positive denominator $4(\alpha+3)^2$ has tensor Bernstein coefficients over $Q_{\mathrm{new}}$ with minimum

$$
\frac{679849}{10000}>0.
$$

Hence

$$
K_B\left(\frac12\right)>0
$$

throughout the box.

Since

$$
K_B(0)=-\frac{\alpha^2}{4}<0
$$

and $K_B$ is increasing through the residual cap, the first turning point $u_0$ satisfies

$$
u_0<\frac12.
$$

The unique critical point in $(1/2,3/5)$ is therefore the first critical point after $u_0$.

### 5.4 Value bound on the new box

For $\theta>0$,

$$
\mathcal R_2(\alpha,\theta,u)
=
\left(
\frac{2\Gamma(B)}{\Gamma(\alpha+3)\Gamma(B-\alpha)}
\right)^2
\left(\frac{u}{B}\right)^{2\alpha}
\left(1-\frac{u}{B}\right)^{2\beta}
P_{2,\alpha,\theta}(u)^4
\frac{\alpha+3-\alpha\theta}{3}.
$$

Use the elementary gamma-ratio inequality

$$
\frac{\Gamma(B)}{\Gamma(B-\alpha)}
\le
B^\alpha,
$$

valid here since $B-\alpha=\beta+3\ge3$ and $\psi(t)<\log t$ for $t>0$ gives

$$
\log\frac{\Gamma(B)}{\Gamma(B-\alpha)}
=
\int_{B-\alpha}^{B}\psi(t)\,dt
\le
\int_{B-\alpha}^{B}\log B\,dt
=
\alpha\log B.
$$

Also

$$
\left(1-\frac{u}{B}\right)^{2\beta}\le1.
$$

Therefore

$$
\mathcal R_2(\alpha,\theta,u)
\le
\left(\frac{2}{\Gamma(\alpha+3)}\right)^2
u^{2\alpha}
P_{2,\alpha,\theta}(u)^4
\frac{\alpha+3-\alpha\theta}{3}.
$$

On $Q_{\mathrm{new}}$ and $U_1=[1/2,3/5]$,

$$
\alpha+3\ge\frac92.
$$

Since $\Gamma$ is increasing on $[2,\infty)$,

$$
\Gamma(\alpha+3)\ge\Gamma\left(\frac92\right).
$$

Moreover,

$$
\Gamma\left(\frac92\right)
=
\frac{105}{16}\sqrt\pi
>
11,
$$

using $\sqrt\pi>17/10$.

Since $u\le3/5$ and $2\alpha\ge3$,

$$
u^{2\alpha}\le\left(\frac35\right)^3.
$$

Next bound $P=P_{2,\alpha,\theta}(u)$. On the same box,

$$
P
=
\frac{(\alpha+1)(\alpha+2)}2
-(\alpha+2)u
+
\frac12\left(1+\frac{\theta}{\alpha+3}\right)u^2.
$$

A lower bound is

$$
P
\ge
\frac{(5/2)(7/2)}2
-
\left(\frac{18}{5}\right)\left(\frac35\right)
=
\frac{35}{8}-\frac{54}{25}
=
\frac{443}{200}>0.
$$

An upper bound is

$$
P
\le
\frac{(13/5)(18/5)}2
-
\left(\frac72\right)\left(\frac12\right)
+
\frac12
\left(1+\frac{(3/10)}{(9/2)}\right)
\left(\frac35\right)^2.
$$

That is

$$
P
\le
\frac{117}{25}
-\frac74
+
\frac{24}{125}
=
\frac{1561}{500}.
$$

Thus $|P|=P\le1561/500$.

Finally,

$$
\frac{\alpha+3-\alpha\theta}{3}
\le
\frac{(8/5)+3-(3/2)(1/4)}{3}
<
\frac{(8/5)+3}{3}
=
\frac{23}{15},
$$

but a sharper direct bound is

$$
\frac{\alpha+3-\alpha\theta}{3}
\le
\frac{3+(8/5)(3/4)}3
=
\frac{7}{5}.
$$

Combining these estimates gives

$$
\mathcal R_2(\alpha,\theta,u_1)
\le
\left(\frac{2}{11}\right)^2
\left(\frac35\right)^3
\left(\frac{1561}{500}\right)^4
\frac75.
$$

The exact rational value is

$$
\left(\frac{2}{11}\right)^2
\left(\frac35\right)^3
\left(\frac{1561}{500}\right)^4
\frac75
=
\frac{1122208144817949}{1181640625000000}
<1.
$$

Therefore

$$
\boxed{
\mathcal R_2(\alpha,\theta,u_1)<1
}
$$

on

$$
\boxed{
\frac32\le\alpha\le\frac85,
\qquad
\frac14\le\theta\le\frac3{10}.
}
$$

This is a provisional new interior subbox certificate, conditional on the accepted endpoint-cap first-lobe reduction, the elementary gamma-ratio inequality stated above, and archival verification of the Bernstein coefficient transcript.

## 6. Status table

| Region | Status | Method | Max bound | Remaining issue |
|---|---|---:|---:|---|
| $n=2$, $\alpha=1/2$, $0\le\theta\le1$ | Certified conditional on imported module | Small-endpoint theorem | KKT target | Must ensure imported theorem has exact normalization |
| $n=2$, $\alpha=15/7$, $0\le\theta\le1$ | Certified conditional on imported module | Energy handoff at $\alpha_E(2)$ | KKT target | Must ensure energy theorem includes closed boundary |
| $n=2$, $\theta=0$ | Open | Laguerre face formulas derived | endpoint diagnostics show $\mathcal R_2\ll1$ | Needs one-dimensional interval root/value certificate |
| $n=2$, $\theta=1$ | Open | finite face formulas derived | endpoint diagnostics show $\mathcal R_2\ll1$ | Needs one-dimensional interval root/value certificate |
| Round 27 box $1\le\alpha\le11/10$, $1/2\le\theta\le3/5$ | Previously certified by project state | Rational root/value certificate | ratio $<1$ | Should be archived with exact proof transcript |
| New box $\frac32\le\alpha\le\frac85$, $\frac14\le\theta\le\frac3{10}$ | Provisional certificate in this response | Bernstein root isolation plus crude gamma bound | $\le1122208144817949/1181640625000000<1$ | Archive full Bernstein coefficient lists or exact script |
| Full residual $n=2$ rectangle | Open | finite subdivision program | not available | Boundary faces and remaining interior boxes |

Dependencies:

1. **Endpoint-cap first-lobe reduction.** This response assumes the already established reduction: central, energy, small-exponent, and symmetry modules reduce the residual right endpoint to the first local extremum after the first endpoint turning point.

2. **Endpoint ODE and monotone $K_B$.** I used the accepted endpoint equation

$$
(p_BH')'+q_BH=0
$$

and the quadratic product

$$
K_B=p_Bq_B
=
-\frac{\alpha^2}{4}+\Lambda_Bu-\Delta_Bu^2,
$$

with $K_B'(u)>1/4$ in the residual cap.

3. **Finite hypergeometric representation.** The compactified polynomial derivation uses

$$
P_n^{(\alpha,\beta)}\left(1-\frac{2u}{B}\right)
=
\frac{(\alpha+1)_n}{n!}
{}_2F_1\left(-n,B;\alpha+1;\frac{u}{B}\right).
$$

4. **Gamma-ratio inequality.** For the new subbox, I used

$$
\frac{\Gamma(B)}{\Gamma(B-\alpha)}\le B^\alpha.
$$

One proof is

$$
\log\frac{\Gamma(B)}{\Gamma(B-\alpha)}
=
\int_{B-\alpha}^{B}\psi(t)\,dt
\le
\alpha\log B,
$$

using the standard inequality $\psi(t)<\log t$ for $t>0$.

5. **Gamma monotonicity.** I used that $\Gamma$ is increasing on $[2,\infty)$, and

$$
\Gamma(9/2)=\frac{105}{16}\sqrt\pi>11.
$$

6. **Bernstein polynomial enclosure.** The signs of $C$, $C_u$, and $K_B(1/2)$ on the new box were certified by exact rational tensor-Bernstein coefficient bounds after affine transformation to the unit cube.

Potential gaps:

1. The full $n=2$ theorem is not proved. The certified pieces are the imported handoff faces and the Round 27 subbox; the new subbox above is a strong provisional certificate pending transcript archival.

2. The Laguerre face $\theta=0$ is still not certified as a full one-dimensional theorem. The formulas are clean and the numerical slack is large, but root isolation and log-gamma bounds over all $\alpha\in[1/2,15/7]$ still need to be executed.

3. The finite face $\theta=1$ is likewise not certified as a full one-dimensional theorem.

4. The new subbox uses a crude gamma-ratio estimate. It is sufficient for this box but too weak for some future boxes. Harder boxes will require Binet, Wendel, Gautschi, or outward-rounded log-gamma intervals.

5. The Bernstein sign certificates in this answer are summarized by their extremal rational coefficients. For permanent archival, the full coefficient lists or a reproducible script should be committed.

6. The proof that the critical point in the new subbox is the relevant first-lobe maximum relies on the accepted forbidden-zone ascent and Sonin first-lobe reduction. If that reduction is later weakened, the subbox proof would need to be restated as a direct critical-point certificate.

Counterexample or obstruction search:

1. **Root-order obstruction.**
On the new box, the cubic has no positive root before $1/2$ because $C>0$ on $[0,1/2]$. It has exactly one root in $[1/2,3/5]$ because $C(3/5)<0$ and $C_u<0$ on $[1/2,3/5]$. Thus no hidden earlier critical point is present in this box.

2. **Turning-point obstruction.**
The first turning point satisfies $u_0<1/2$ because $K_B(0)<0$, $K_B(1/2)>0$, and $K_B$ is increasing in the residual cap. Thus the isolated critical point is after the endpoint turning point.

3. **Cap-bound obstruction.**
The whole root interval $[1/2,3/5]$ lies inside the cap because $u_\sigma\ge15/8$ on the new box.

4. **Value-bound obstruction.**
The crude value bound still leaves margin:

$$
\frac{1122208144817949}{1181640625000000}
<1
\quad\text{with exact margin}\quad
\frac{59432480182051}{1181640625000000}.
$$

The margin is not huge, so future adjacent boxes may require sharper gamma and polynomial bounds.

5. **Boundary-face obstruction.**
At both $\theta=0$ and $\theta=1$, endpoint diagnostics suggest the ratio is far below $1$, but this does not rule out an interior-in-$\alpha$ bump. Those faces require actual one-dimensional interval certification.

Divergent alternatives and 20% exploration:

The best bounded exploratory alternative remains a low-degree Sonin/Krasikov certificate for $n=2$. The form to test is

$$
V(u)
=
A(u)H(u)^2
+
B(u)p_B(u)^2H'(u)^2
+
C(u)p_B(u)H(u)H'(u).
$$

The simplest Sonin choice corresponds morally to

$$
A(u)=1,
\qquad
B(u)=\frac{1}{K_B(u)},
\qquad
C(u)=0,
$$

or equivalently

$$
V(u)
=
H(u)^2+\frac{p_B(u)H'(u)^2}{q_B(u)}
=
H(u)^2+\frac{p_B(u)^2H'(u)^2}{K_B(u)}.
$$

This is useful only on $K_B>0$ and is singular at the turning point. It orders extrema but does not provide a first-lobe amplitude by itself.

For an algebraic $n=2$ certificate, the exploratory ansatz should avoid the pole by taking low-degree polynomial coefficients, for example

$$
A(u)=a_0+a_1u+a_2u^2,
$$

$$
B(u)=b_0+b_1u+b_2u^2,
$$

$$
C(u)=c_0+c_1u+c_2u^2.
$$

The derivative $V'$ can be reduced using

$$
(p_BH')'=-q_BH.
$$

After reduction, $V'$ becomes a quadratic form in $(H,p_BH')$:

$$
V'
=
M_{11}(u)H^2
+
2M_{12}(u)H(p_BH')
+
M_{22}(u)(p_BH')^2.
$$

A successful Krasikov/SOS certificate on a box would require:

1. $V$ positive definite on the box;
2. $V'$ having a fixed sign on the box;
3. endpoint or cut normalization strong enough to imply the KKT target;
4. all coefficient signs certified by rational inequalities or SOS decomposition.

I did not find or certify such a polynomial $V$ in this response. The new interval subbox proof above is currently more concrete. The SOS route should remain exploratory until a rational coefficient set is produced.

Useful lemmas:

## Lemma 29.1: Compactified $n=2$ polynomial

For $n=2$, $\theta=(\alpha+3)/B$, and $B=\alpha+\beta+3$,

$$
P_2^{(\alpha,\beta)}\left(1-\frac{2u}{B}\right)
=
P_{2,\alpha,\theta}(u),
$$

where

$$
P_{2,\alpha,\theta}(u)
=
\frac{(\alpha+1)(\alpha+2)}2
-(\alpha+2)u
+
\frac12\left(1+\frac{\theta}{\alpha+3}\right)u^2.
$$

Status: proved above.

## Lemma 29.2: Compactified $n=2$ critical cubic

For $\theta>0$, critical points of $H_2$ are roots of

$$
C_{\alpha,\theta}(u)=0,
$$

where

$$
\begin{aligned}
C_{\alpha,\theta}(u)
={}&
-(\alpha+\theta+3)^2u^3\\
&+(\alpha+3)(3\alpha^2-\alpha\theta+17\alpha+24)u^2\\
&-(\alpha+2)(\alpha+3)(3\alpha^2-3\alpha\theta+14\alpha-3\theta+15)u\\
&+\alpha(\alpha+1)(\alpha+2)(\alpha+3)^2.
\end{aligned}
$$

This cubic is $2(\alpha+3)$ times the compactified factored equation $\theta E_{\alpha,\theta}(u)=0$.

Status: proved above.

## Lemma 29.3: New provisional $n=2$ interior box

For

$$
\frac32\le\alpha\le\frac85,
\qquad
\frac14\le\theta\le\frac3{10},
$$

the first critical point after the first turning point lies in

$$
\frac12<u_1<\frac35,
$$

and

$$
\frac{H_2(u_1)^4}{T_{2,\alpha,\theta}^4}<1.
$$

More precisely,

$$
\frac{H_2(u_1)^4}{T_{2,\alpha,\theta}^4}
\le
\frac{1122208144817949}{1181640625000000}
<1.
$$

Status: provisionally proved above, conditional on the endpoint-cap first-lobe reduction and archival verification of the Bernstein coefficient transcript.

## Lemma 29.4: Laguerre-face $n=2$ ratio formula

At $\theta=0$,

$$
\mathcal R_2(\alpha,0,u)
=
\left(\frac{2}{\Gamma(\alpha+3)}\right)^2
u^{2\alpha}e^{-2u}
P_{2,\alpha,0}(u)^4
\frac{\alpha+3}{3}.
$$

Status: formula proved; full face certificate open.

## Lemma 29.5: Finite-face target simplification

At $\theta=1$,

$$
\beta=0,
\qquad
T_{2,\alpha,1}^4=1.
$$

Status: proved; full face certificate open.

What should be tested next:

1. **Complete $\theta=0$.**
Run a one-dimensional rational interval proof over

$$
\alpha\in\left[\frac12,\frac{15}{7}\right]
$$

using $C_{\alpha,0}$, interval Newton or Sturm root isolation, and log-gamma/Binet bounds for $\Gamma(\alpha+3)$.

2. **Complete $\theta=1$.**
Run the same one-dimensional certification on the finite face. The target $T^4=1$ gives good slack, so this should be easier than a generic interior box.

3. **Archive the new subbox.**
Commit the exact tensor-Bernstein coefficient lists for:
   - $C$ on $Q_{\mathrm{new}}\times[0,1/2]$;
   - $C(3/5)$ on $Q_{\mathrm{new}}$;
   - $C_u$ on $Q_{\mathrm{new}}\times[1/2,3/5]$;
   - the numerator of $K_B(1/2)$ on $Q_{\mathrm{new}}$.

4. **Tile adjacent boxes.**
The new subbox suggests the crude gamma bound may work in a larger middle-$\alpha$ region. Test rational boxes adjacent to

$$
\left[\frac32,\frac85\right]\times\left[\frac14,\frac3{10}\right].
$$

5. **Separate hard zones.**
The likely hard zones for crude bounding are:
   - near $\alpha=1/2$, already handled by the small-exponent theorem but requiring clean handoff;
   - near $\theta=0$, because limiting gamma/log terms must be treated correctly;
   - intermediate $\theta$ where the crude bound $\Gamma(B)/\Gamma(B-\alpha)\le B^\alpha$ loses too much.

6. **Try a degree-two Krasikov/SOS pilot only after one more interval box.**
Use the explicit ansatz

$$
V=A(u)H^2+B(u)p_B^2H'^2+C(u)p_BHH'
$$

with quadratic $A,B,C$. If rational coefficients cannot be found on the known certified boxes, demote this route.

Confidence:

- Confidence in the compactified $n=2$ polynomial: $0.89$.
- Confidence in the critical cubic and scaling statement: $0.89$.
- Confidence in the new interior subbox certificate: $0.82$, limited mainly by the need to archive full Bernstein coefficient lists.
- Confidence that $\theta=0$ and $\theta=1$ are true but not yet certified: $0.80$.
- Confidence that the full residual $n=2$ theorem can be certified by rational subdivision: $0.72$.
- Confidence that this response proves the full residual $n=2$ theorem: $0.20$.
- Confidence that this response proves the full real-parameter KKT conjecture: $0.08$.