# Stage B Cross-Review for Round 25

## Most valuable input from others

A1 supplies a mature, almost publication-ready theorem package: the right endpoint cap reduction, first-lobe reduction, a nearly complete $n=1$ residual theorem, and a crisp $n=2$ certification target.  This is the strongest structural synthesis of the whole workflow and provides the precise “what must be proved next” statement.  The convexity proof for the $n=1$ scalar envelope is rigorous and uses only elementary digamma/trigamma bounds, making the $n=1$ theorem **almost closed** – the only missing piece is a permanent outward‑rounded enclosure for $\Gamma(16/5)$ or an equivalent rational Binet appendix.  The corrected $n=2$ compactified polynomial and critical cubic are correctly derived and ready for interval arithmetic.

A2 gives the first **sign‑definite relative amplitude identity** for the rational‑coordinate Bessel track:
\[
(W_1^2 h')' = |\Delta Q(z)|\,W_1^2 h,
\qquad h = \frac{Y}{W_1},
\]
and uses elementary monotonicity of $J_\alpha$ before its first peak to obtain a **clean, explicit Volterra bound** $\mathcal V_{\mathrm{Bess}}\le M_Q (j_{\alpha,1}')^4/(64\Lambda_B^2)$ without Watson/Nicholson asymptotics.  The derivation of the bound $M_Q\le 2.75$ is also systematic and, if verified, turns a previous heuristic constant into a rigorous global constant.  The resulting $\mathcal O(\alpha^4/n^2)$ scaling pins down the viable regime for this track and justifies a split $\alpha\le C\sqrt n$ versus bulk Langer.

A4 produces the most **concrete computational lemmas**: a strict Robbins‑based gamma‑ratio envelope (with explicit remainder bounds), an interval‑Newton certificate for the half‑order Bessel maximum, and a stable compactified $n=2$ cubic ready for Arb implementation.  The Robbins bound for $\ln M_{n,\alpha,B}$ is a real advance, replacing earlier heuristic Stirling entropy arguments by a finite‑parameter inequality (though the trapezoidal remainder bound still needs to be fully written out).  A4 also gives an explicit $N_0$ threshold idea via a computable inequality, which is exactly what is needed to demarcate the analytic large‑$n$ regime from the finite verification.

## Claims that look correct

**From A1**  
- Lemma‑bank text for the right endpoint cap (ODE, $K_B$ quadratic, cap length, cap monotonicity $K_B'(u)\ge\alpha/2$).  
- Forbidden‑zone ascent argument (regular branch positive and increasing before $u_0$).  
- Sonin ordering and reduction to the first critical point $u_1$.  
- $n=1$ residual theorem: the reduction to $H_1(u)^2\le F_\alpha(u)$, the maximisation at $u_*=\alpha(\alpha+1)/(\alpha+2)$, and the convexity of $\log\sqrt{E(\alpha)}$.  
- The corrected $n=2$ compactified polynomial and critical cubic are algebraically correct (up to a final coefficient audit).  
- The statement of external theorem dependencies (Landau, DGS, Wendel etc.) is accurate and well‑scoped.

**From A2**  
- The rational‑coordinate normal form and the exact residual $\Delta Q(z)$ are correctly derived.  
- The identity $(W_1^2 h')' = |\Delta Q| W_1^2 h$ is algebraically exact; the proof is clean.  
- The bound $M_Q = \frac{\Lambda_B}{B}+\Delta_B$ and the estimate $M_Q<2.75$ are plausible and, after a detailed arithmetic audit, likely rigorous.  
- The Volterra bound $\mathcal V_{\mathrm{Bess}}\le \frac{M_Q (j_{\alpha,1}')^4}{64\Lambda_B^2}$ follows logically from the monotonicity of $J_\alpha$ on $(0, j_{\alpha,1}']$; the cancellation step is elegant.  
- The $\mathcal O(\alpha^4/n^2)$ scaling and the resulting threshold $\alpha\le C\sqrt n$ are mathematically sound conditional on the remaining phase‑shift and gamma‑normalization controls.

**From A4**  
- The Robbins‑based gamma‑ratio envelope is correctly set up; the strict bounds $1/(12x+1)<r(x)<1/(12x)$ are correctly quoted, and the combination for $\ln M_{n,\alpha,B}^2$ is algebraically accurate.  
- The Interval‑Newton certificate for the half‑order Bessel maximum is rigorously presented (root isolation of $\tan t = 2t$, outward‑rounded enclosure).  
- The $n=1$ scalar envelope convexity proof using $\psi'(x)<1/(x-1)$ is rigorous.  
- The compactified $n=2$ cubic after rescaling with $B=(\alpha+3)/\theta$ is algebraically correct and stable at $\theta=0$.

## Claims that need proof

**A1**  
- The $n=1$ theorem is not yet “proved” in the permanent lemma‑bank sense because the arithmetic appendix (enclosure $E(6/5)<0.39$ and $\Gamma(16/5)>121/50$) is not supplied.  A1 itself marks it as “proved after endpoint arithmetic appendix”.  The missing piece is a short but necessary outward‑rounded or rational‑inequality certificate.  
- The forbidden‑zone ascent is described in words but lacks a fully written case‑analysis (e.g., the no‑zero case, $u_0=u_\sigma$, absence of $u_1$).  A final polished lemma is needed.  
- The $n=2$ certification plan is only a target; no root isolation or interval evaluation has been executed.

**A2**  
- Several claims are labelled **[proved]** while they are only **derived under assumptions** that are not yet certified for the full KKT setting:  
  * The bound $M_Q\le 2.75$ is derived using $c_B< n+1/2$ and $r_B<1$; however the expression for $c_B=n+\frac12-\frac{n+1}{2B}$ and the combination $2c_B/B$ etc. require a careful treatment of strictness when $B$ is close to $n+\alpha+1$ (the worst case).  A complete audit with explicit rational inequalities is still needed before marking this as a lemma.  
  * The phase‑shift argument that the true critical point $z_1^*$ lies before the first Bessel zero $j_{\alpha,1}$ uses an asymptotic $\mathcal O(n^{-1})$ estimate and is not yet backed by a finite constant.  This must be turned into a rigorous bound (or the Volterra integral must be evaluated up to a proved upper envelop of $z_1^*$, not merely up to $j_{\alpha,1}'$).  
  * The variation scaling $\mathcal V_{\mathrm{Bess}}=O(\alpha^4/n^2)$ is **not** “proved” until the constants in the Bessel peak $j_{\alpha,1}'$ and the denominator control are bounded by explicit nontrivial inequalities; currently it is an asymptotic scaling.  A2's label “PROVED” for the scaling is an overstatement.  
- The “Sign‑definite” nature of the Volterra kernel relies on $W_1>0$ on the entire integration interval.  A2 argues that $j_{\alpha,1} - j_{\alpha,1}' \approx O(\alpha^{1/3})$ and the phase delay is $O(n^{-1})$, but for small $n$ this gap can be tiny.  A rigorous enclosure of $z_1^*$ for small $n$ is missing; the argument is not yet theorem‑level.  
- A2's claim that the additional variation after $j_{\alpha,1}'$ is $O(n^{-3})$ is purely asymptotic and not substantiated.

**A4**  
- The Robbins‑based gamma‑ratio bound is given as a formula, but the trapezoidal remainder bound is **not explicitly evaluated**.  The claim that it yields a strict computable upper bound is promising, but until the actual numerical constants or a symbolic inequality are written out, the envelope is [DERIVED‑UNDER‑ASSUMPTIONS] – A4’s own label is appropriate but the claim is still “needs proof”.  
- The Olver perturbation integral is still labelled [ASSUMED] and no bound is supplied; therefore the analytic large‑$n$ closure for the Bessel track remains speculative.  
- The interval‑arithmetic plan for $n=2$ is described but **not executed**; A4 does not provide actual Arb logs or certified boxes.  The compactified cubic is ready for implementation, but the claim that it will resolve without excessive subdivision is heuristic.  
- The Krasikov‑style higher‑order Sonin envelopes are labelled [HEURISTIC] and no explicit functional or derivative sign is given; this is purely exploratory and not yet a claim that can be assessed.

## Possible errors or hidden assumptions

**A1**  
- The $n=1$ proof uses the inequality $\left(1-\frac{u}{B}\right)^\beta\le 1$, which is true for $u\le 1$ and $B>1$.  However, $B$ can be as small as $\alpha+\beta+2$; for $\beta=0$ and $\alpha=1/2$, $B=2.5$, still $>1$, so it's fine.  But the inequality $H_1(u)^2\le \frac{u^\alpha(\alpha+1-u)^2}{\Gamma(\alpha+2)}$ also uses $\frac{\Gamma(B)}{\Gamma(\beta+2)}\le B^\alpha$, which is derived via $\psi(t)<\log t$.  That inequality only gives $\frac{\Gamma(x+a)}{\Gamma(x)} < (x+a)^a$ for $x>0, a>0$.  Here $x=\beta+2$, $a=\alpha$; so $\frac{\Gamma(B)}{\Gamma(\beta+2)} < B^\alpha$ is correct.  But A1 writes $\frac{\Gamma(B)}{\Gamma(\beta+2)}\le B^\alpha$; it's actually strict, so $<$ is fine.  So no error.  
- The reduced envelope $F_\alpha(u)$ is maximised at $u_*$; A1 claims $u_*\le1$ for $\alpha\le6/5<\sqrt2$.  Indeed $\alpha\le1.2$, $\sqrt2\approx1.414$, so $u_*=\frac{\alpha(\alpha+1)}{\alpha+2}\le 1$ is correct.  
- The convexity proof uses $\psi'(x) < 1/(x-1)$ for $x>1$; this is a known bound and can be made rigorous by series.  So the $n=1$ envelope proof is solid.

- The $n=2$ compactified cubic $C_{\alpha,\theta}(u)$ is given; we should check that the coefficient $\tilde a_3 = -(\alpha+3+\theta)^2$ is correct after scaling.  A4's derived polynomial: $\tilde a_3 = -(\alpha+3+\theta)^2$, which is negative, and matches the required sign.  It's plausible.  No immediate error.

**A2**  
- The bound $M_Q\le 2.75$ uses $2c_B/B < (2n+1)/(n+\alpha+1)$ and then $\le 2$.  However, $c_B = n+\frac12 - \frac{n+1}{2B}$ can be as low as $n+\frac12 - \frac{n+1}{2(n+\alpha+1)}$.  The upper bound $c_B < n+1/2$ is safe.  But then $2c_B/B < (2n+1)/B$.  Since $B\ge n+\alpha+1$, we need to ensure $(2n+1)/B \le (2n+1)/(n+\alpha+1)$.  That's true because $B\ge n+\alpha+1$.  Now $(2n+1)/(n+\alpha+1)\le 2$ for $\alpha\ge -1/2$, which holds.  So the chain is valid.  However, note that the term $\frac{\alpha r_B}{2B}$ is bounded by $\frac{\alpha}{2B}$ because $0<r_B<1$.  $\frac{\alpha}{2B}\le \frac{\alpha}{2(n+\alpha+1)} < 1/2$, correct.  The remaining $\Delta_B$ term includes $c_B/B + r_B^2/4$.  They bound $c_B/B$ by $(n+1/2)/B \le (n+1/2)/(n+\alpha+1) \le 1$ (for $n\ge1$).  So $c_B/B \le 1$.  Then $r_B^2/4 \le 1/4$.  Summing $2c_B/B + \alpha r_B/(2B) + r_B^2/4$ gives $2\cdot 1 + 1/2 + 1/4 = 2.75$, but they used $2c_B/B\le2$ (which is the same).  So the bound is rigorous.  However, they wrote “2c_B/B < 2” and “alpha r_B/(2B) < 1/2”, “r_B^2/4 < 1/4”.  So indeed $M_Q<2.75$.  That is a strict inequality.  So this part is solid.  

- The Volterra bound uses $J_\alpha(w)^2\le J_\alpha(t)^2$ for $0\le w\le t\le j_{\alpha,1}'$.  This holds because $J_\alpha$ is positive and increasing on that interval.  However, this is a property of Bessel functions for $\alpha\ge0$, but one must ensure that $j_{\alpha,1}'$ exists (first derivative zero).  For $\alpha\ge0$, $J_\alpha$ does have a first positive maximum at $j_{\alpha,1}'$, and is positive before that.  So it's correct.  So the bound is rigorous.

- The bound $\mathcal V_{\mathrm{Bess}} \le \frac{M_Q (j_{\alpha,1}')^4}{64\Lambda_B^2}$ is then rigorous under the assumption that the true critical point $u_1$ corresponds to a $z\le$ the $z$ giving $T=j_{\alpha,1}'$.  Since the true $u_1$ may be located slightly beyond the reference Bessel peak, the integral must be evaluated up to that later $T_1^*$.  A2's argument that $T_1^* < j_{\alpha,1}$ (the zero) is plausible, but the Volterra bound uses $T\le j_{\alpha,1}'$; if $T_1^* > j_{\alpha,1}'$, then the bound must be larger.  A2 asserts that the additional variation from $j_{\alpha,1}'$ to $T_1^*$ is $O(n^{-3})$, which is not proved.  So the claim that the bound $\frac{M_Q (j_{\alpha,1}')^4}{64\Lambda_B^2}$ is a valid upper bound for the actual variation may be **invalid** if $T_1^* > j_{\alpha,1}'$ and the integrand does not remain small.  This is a critical gap.  The step where they estimated the phase delay as $O(\alpha^2/n^2)$ is asymptotic; for finite $n$, it could be larger.  So the bound on the Volterra integral is **not** a theorem before a rigorous enclosure of $T_1^*$ is provided.  A2's labelling of the variation bound as proved is an overstatement.

- A2's claim that the “phase delay induced by the residual potential ... scales as $O(\alpha^2/n^2)$” is unsupported; they assert without derivation.  It needs a formal perturbation argument.

- The threshold extraction $C\le 1.235$ uses the approximate relation $(j_{\alpha,1}')^4 \approx \alpha^4$, which holds for large $\alpha$ but not for small $\alpha$.  Yet the regime considered is $\alpha = C\sqrt n$, so for large $n$, $\alpha$ is large, the approximation is valid.  But then the bound on $M_Q$ uses constants independent of $n$; that's okay.  However, the derived $C\le 1.23$ is based on $\mathcal V_{\mathrm{Bess}} \le 0.1$ as a “requirement” to fit KKT slack; this requirement is not derived from the full KKT inequality (with gamma normalization and target).  So the threshold is conditional.

**A4**  
- The Robbins bound: the term $\frac{1}{12(B-\alpha)+1}$ appears in the subtraction $r(B) - r(B-\alpha)$.  The bound is $r(x) > 1/(12x+1)$.  So for $r(B-\alpha)$, we have $r(B-\alpha) > 1/(12(B-\alpha)+1)$.  Then $-r(B-\alpha) < -1/(12(B-\alpha)+1)$.  This is correct.  The sum is $\Delta r = r(B) - r(B-\alpha) + r(n+\alpha+1) - r(n+1)$.  The upper bound given is correct.  However, the trapezoidal approximation error for the integral of $f(t)$ is not fully derived; A4 just sketches it.  The claim that the error is bounded by $\frac{\alpha^3}{12} \max_{t\in [B-\alpha, B]} |f''(t)|$ is standard.  $f''(t) = 1/t + 1/(2t^2)$ is positive and decreasing.  So the bound can be made explicit.  So the method is sound, but not yet executed.  

- The Interval‑Newton for $\tan t = 2t$ uses $[t] = [1.165561, 1.165562]$, but does not provide the actual outward-rounded interval arithmetic.  The statement is plausible, but to be a [PROVED] certificate, one must provide the interval arithmetic log.  Still, the method is correct.

- The $n=1$ convexity proof uses $\psi'(x) < 1/(x-1)$ for $x>1$, then simplifies to $L''(\alpha) > 0$.  However, the simplification $L''(\alpha) > \frac{2}{\alpha(\alpha+2)} - \frac{1}{(\alpha+1)^2}$ and then claiming positivity may require a check.  Let's verify: $L''(\alpha) = 1/\alpha + 1/(\alpha+1) - 1/(\alpha+2) - 1/(\alpha+1)^2 - \psi'(\alpha+2)$.  Using $\psi'(\alpha+2) < 1/(\alpha+1)$ (since $x=\alpha+2$, $x-1 = \alpha+1$).  So $L''(\alpha) > 1/\alpha + 1/(\alpha+1) - 1/(\alpha+2) - 1/(\alpha+1)^2 - 1/(\alpha+1)$.  Combine $1/(\alpha+1) - 1/(\alpha+1)$ cancels! So $L''(\alpha) > 1/\alpha - 1/(\alpha+2) - 1/(\alpha+1)^2$.  That is exactly what A4 writes.  Now is $1/\alpha - 1/(\alpha+2) = 2/(\alpha(\alpha+2))$.  So $L''(\alpha) > 2/(\alpha(\alpha+2)) - 1/(\alpha+1)^2$.  For $\alpha\ge 1/2$, we can check numeric: at $\alpha=0.5$, $2/(0.5*2.5)=2/1.25=1.6$, $1/(1.5^2)=1/2.25\approx0.444$, difference positive.  To prove it's always positive, one could cross-multiply: $2(\alpha+1)^2 - \alpha(\alpha+2) = 2(\alpha^2+2\alpha+1) - (\alpha^2+2\alpha) = 2\alpha^2+4\alpha+2 -\alpha^2-2\alpha = \alpha^2+2\alpha+2$, which is positive.  So yes, the lower bound is positive.  So convexity is proved.

- The $n=2$ compactified cubic: A4 gives $\tilde a_3 = -(\alpha+3+\theta)^2$, but earlier they derived $a_3 = -\frac{(B+1)^2}{2B}$.  Substitute $B=(\alpha+3)/\theta$, $B+1 = (\alpha+3)/\theta + 1 = (\alpha+3+\theta)/\theta$.  Then $(B+1)^2/(2B) = ((\alpha+3+\theta)^2/\theta^2) / (2(\alpha+3)/\theta) = (\alpha+3+\theta)^2 / (2\theta(\alpha+3))$.  Multiplying by $2\theta$ to clear denominators: $2\theta \cdot a_3 = 2\theta \cdot ( - \frac{(B+1)^2}{2B} ) = -\theta \cdot \frac{(B+1)^2}{B} = -\theta \cdot \frac{ ((\alpha+3+\theta)^2/\theta^2) }{ (\alpha+3)/\theta } = -\theta \cdot \frac{ (\alpha+3+\theta)^2/\theta^2 }{ (\alpha+3)/\theta } = -\theta \cdot \frac{(\alpha+3+\theta)^2}{\theta^2} \cdot \frac{\theta}{\alpha+3} = -\frac{(\alpha+3+\theta)^2}{\alpha+3}$.  Wait, that's not what A4 got.  A4 claimed $\tilde a_3 = -(\alpha+3+\theta)^2$.  Let's recompute carefully.  Actually A4's expression: "$\tilde{a}_3 = -(\alpha+3+\theta)^2$".  My calculation yields $-\frac{(\alpha+3+\theta)^2}{\alpha+3}$.  So there is a discrepancy.  Let's see A4's derivation: they started from $C(u) = -\frac{(B+1)^2}{2B}u^3 + ...$ and substituted $B=(\alpha+3)/\theta$ and then multiplied by $2\theta$ to clear denominators.  Multiplying the cubic term by $2\theta$: $2\theta \cdot (-\frac{(B+1)^2}{2B}) = -\theta \frac{(B+1)^2}{B}$.  Now $(B+1)^2 = ( (\alpha+3)/\theta + 1 )^2 = (\alpha+3+\theta)^2/\theta^2$.  So $-\theta \frac{ (\alpha+3+\theta)^2/\theta^2 }{ (\alpha+3)/\theta } = -\theta \frac{ (\alpha+3+\theta)^2/\theta^2 }{ (\alpha+3)/\theta } = -\theta \cdot \frac{(\alpha+3+\theta)^2}{\theta^2} \cdot \frac{\theta}{\alpha+3} = -\frac{ (\alpha+3+\theta)^2 }{ \alpha+3 }$.  So indeed the coefficient after scaling is $-\frac{(\alpha+3+\theta)^2}{\alpha+3}$, not $-(\alpha+3+\theta)^2$.  So A4's $\tilde a_3$ is incorrect.  This is a serious algebraic error that must be corrected before any interval implementation.  A3 should verify and supply the correct coefficients.  This is a hidden assumption that A4's compactified cubic is stable; it is stable but with a different expression.  So the claim that $\tilde a_3 = -(\alpha+3+\theta)^2$ is *wrong*.  Consequently, A4's claim that the polynomial is "perfectly stable as $\theta\to0$" may still be true (the correct expression still has no blow-up), but the specific formula must be fixed.

- A4's "Certified Half-Order Bessel Maximum" label [PROVED] is a bit overclaimed: they gave an interval Newton argument without showing actual outward-rounded interval arithmetic.  But the reasoning is sound; it could be considered a proof if the interval arithmetic is transparent.  However, they didn't execute the outward-rounded steps; they described them.  Still, it's more rigorous than previous attempts.  I'll mark it as plausible but not yet a machine-checked certificate.

**General**  
- All agents rely on imported global modules (central contour, energy, small‑exponent) that have not been reproved in this round.  This is acceptable as a proof skeleton, but the final theorem must state their exact hypotheses and ensure they mesh with the cap boundary.  This dependency remains a gap.

## Suggested synthesis

The three agents’ contributions are largely complementary and can be assembled into a **single split‑tracks proof program**:

1. **Freeze the endpoint‑cap reduction** (A1’s lemmas 1–7) as the structural backbone.  This reduces the KKT conjecture to the finite‑$B$ first‑lobe amplitude theorem.

2. **Complete the $n=1$ theorem permanently** by appending A4’s Robbins‑based gamma enclosure or an interval‑arithmetic certificate for $\Gamma(16/5)$ and $E(6/5)<0.39$.  This will be the first fully closed low‑degree case.

3. **For $n=2$**, use A1’s corrected polynomial and critical equation, but **replace A4’s incorrect compactified coefficient** with the correct expression derived by A3.  Then execute true interval root isolation and value enclosures.

4. **For large $n$**, split the residual strip into two analytic regimes:

   - **Small‑$\alpha$ regime ($\alpha \le C\sqrt n$):** adopt A2’s rational‑Bessel relative‑amplitude method.  The sign‑definite Volterra bound is already rigorous **up to the point where the true critical point must be enclosed before the Bessel zero**.  The missing ingredients are (a) a rigorous finite bound for the phase shift or a direct enclosure of $z_1^*$ using the Riccati/Frobenius pathway, and (b) the gamma‑normalisation envelope, which A4’s Robbins method can supply if completed.  A2’s $M_Q\le 2.75$ should be formally verified by A3 and added to the lemma bank.

   - **Bulk regime ($\alpha \ge C\sqrt n$):** the weighted Langer/Airy track remains necessary.  A1’s conditional theorem provides the correct container; A3 should supply the missing Frobenius‑to‑Airy cut coefficient and an instantiated DGS/Olver weight with explicit variation bounds.

5. **The Laguerre‑face ($\theta=0$) obstruction** identified by A2 and A4 suggests that the monolithic global Langer approach fails for fixed $\alpha$.  Therefore the split must be **strict**: the rational‑Bessel track covers $\alpha \le C\sqrt n$, and the Langer track covers $\alpha \ge C\sqrt n$, with a possible finite overlap that can be resolved by interval verification.  A2’s scaling argument gives $C\approx 1.23$, but this must be refined.

6. **Gamma‑ratio envelope for arbitrary $n$** should be attacked using A4’s Robbins framework, split into regimes (fixed $\alpha$, $\alpha=C\sqrt n$, $\alpha=cn$).  A3 should produce explicit finite constants.

## Research strategy adjustments

The next round (Round 26) should be an **execution and certification round** with the following priorities:

- **A1:** Synthesise the $n=1$ theorem as a permanent artifact with the arithmetic appendix; then work with A3 to finalize the first‑lobe reduction lemma‑bank text, including all boundary cases.  
- **A2:** Refine the rational‑Bessel track by providing a rigorous bound on the true critical point $z_1^*$ (e.g., via a validated Riccati IVP for the phase shift) and replace the asymptotic phase‑shift argument with a finite constant; solidify the $M_Q\le 2.75$ lemma with an algebraic audit.  
- **A3:** Audit and correct A4’s compactified $n=2$ coefficients; finalize the Langer residual removable value CAS verification; audit A2’s $M_Q$ bound; attack the gamma‑ratio envelope via regime‑split Robbins/Binet bounds (at least for $\alpha\le C\sqrt n$).  
- **A4:** Execute the $n=2$ interval certificate using the **corrected** compactified cubic; execute the $n=1$ arithmetic appendix (Robbins bound for $E(6/5)$ or Arb enclosure); run high‑precision diagnostics for the rational‑Bessel Volterra integral and phase shift on hard grids.

The semi‑discrete $\beta\in\mathbb N_0$ subset should be tested numerically but should not drive a new proof route unless a **sign‑stable contiguous relation** is discovered.

The Krasikov‑style higher‑order Sonin envelopes should remain a small exploratory allocation; A3 could test a simple candidate on $n=1,2$ to see if it can bypass the turning‑point singularity.  But it must not delay the main execution tracks.

Do **not** introduce new broad architecture.  The architecture is now sufficient; the bottleneck is **constants, enclosures, and explicit bounds**.

## Verification assignments

The following concrete verification tasks must be assigned to specific agents in the next round:

| Task | Assignee | Description |
|------|----------|-------------|
| 1. $n=1$ arithmetic appendix | A4 / A3 | Produce outward‑rounded enclosures for $\Gamma(16/5)$ and $E(6/5)$, or a rational Binet‑Stirling certificate with explicit inequalities. |
| 2. Correct compactified $n=2$ cubic | A3 | Re‑derive the correct scaled cubic from $C(u)$ with $B=(\alpha+3)/\theta$, and provide coefficients $\tilde a_3,\tilde a_2,\tilde a_1,\tilde a_0$ in a manner suitable for Arb. |
| 3. $n=2$ interval root isolation | A4 | Implement the corrected cubic in Arb; isolate critical roots and evaluate the KKT margin on a covering of $\alpha\in[1/2,15/7], \theta\in[0,1]$. |
| 4. Rational‑Bessel $M_Q$ lemma audit | A3 | Verify the inequality $M_Q\le 2.75$ with exact rational arithmetic and confirm strictness; add to lemma bank. |
| 5. Enclosure of true critical point for $n=2,3,5$ | A4 / A2 | Use validated Riccati IVP or interval Newton to bound $z_1^*$ and ensure it lies before $j_{\alpha,1}$; feed into Volterra bound. |
| 6. Gamma‑ratio envelope for $\alpha\le C\sqrt n$ | A3 | Using Robbins/Binet bounds, derive a computable upper bound for $M_{n,\alpha,B}$ (or $1+\varepsilon_\Gamma$) that is rigorous for all $n\ge10$ and $\alpha\le C\sqrt n$. |
| 7. Frobenius‑to‑Airy cut coefficient | A1 / A3 | Formalize the connection $\mathfrak C_c$ from endpoint Frobenius data; attempt to bound it in the bulk regime $\alpha\ge C\sqrt n$ using DGS/Olver weights. |
| 8. Langer residual CAS cancellation | A3 | Provide a CAS log verifying the removable value $\Psi_B(0)$ and the cancellation of singular terms; this is a prerequisite for any published Langer theorem. |

## Score by agent

I assign scores based on mathematical depth, correctness, clarity, and contribution to closing the proof.

| Agent reviewed | Score (0-10) | Main reason | Must verify next |
|---|---|---|---|
| A1 | 9 | Best theorem package; almost closes $n=1$; provides clear reduction and $n=2$ target. Minor pending arithmetic appendix. | $n=1$ arithmetic appendix; finalize boundary clauses. |
| A2 | 7 | Elegant Volterra bound and sign‑definite kernel; important simplification. But over‑labelling as [PROVED] on phase‑shift and scaling; missing rigorous $z_1^*$ enclosure. | Provide rigorous bound on $z_1^*$; audit $M_Q$ lemma. |
| A4 | 6 | Useful concrete lemmas (Robbins, Bessel max, compactified cubic). However, $n=2$ coefficient error; no executed intervals; some claims still heuristic. | Correct $n=2$ coefficients; execute $n=2$ interval certificate. |

(A3 was not among the Stage A outputs reviewed but is mentioned as active; their algebra audit from previous rounds is trusted. The scoring here only covers the three agents whose Stage A responses are under review.)

## Next‑round recommendation

Round 26 should be a **pure execution round** with the following allocation:

- **A1:** Convert the $n=1$ theorem into a **permanent proof artifact** with the arithmetic appendix; then collaborate with A3 to finalize the endpoint‑cap reduction as a lemma‑bank theorem. No new architecture.
- **A2:** Deliver a **rigorous enclosure of $z_1^*$** for a representative sample (e.g., $n=5, \alpha=2$, $\beta=0$) using either a validated Riccati IVP or an interval Newton method applied to the rational‑coordinate critical equation; provide explicit constants for the Volterra bound that are safe for $n\ge N_0$.  If the enclosure fails, report the obstruction honestly.
- **A3:** **Audit and correct** A4’s compactified $n=2$ cubic; finalize the $M_Q$ lemma; begin the regime‑split gamma‑ratio envelope using Binet/Robbins; provide the CAS cancellation log for the Langer residual.
- **A4:** **Execute** the $n=2$ interval certificate with the corrected cubic; produce outward‑rounded logs and failure boxes; also execute the $n=1$ arithmetic appendix (Robbins or Arb).  Do not supply pseudo‑code.

Exploratory allocation (at most 15%): A2 may test the Airy relative‑amplitude identity on a concrete box to see if it fails sign, and A3 may test a Krasikov‑style Sonin candidate for $n=1$.  Neither should displace the primary execution tasks.

## Confidence

- Confidence that the endpoint‑cap reduction is **correct** and should be frozen: **0.92**.
- Confidence that the $n=1$ theorem is **proved** modulo the arithmetic appendix: **0.90**.
- Confidence that A2’s Volterra bound up to $j_{\alpha,1}'$ is **rigorous**: **0.85**; but confidence that it covers the true critical point without additional phase‑shift control: **0.55**.
- Confidence that A4’s Robbins‑based gamma bound can be made rigorous with modest effort: **0.80**.
- Confidence that A4’s compactified $n=2$ cubic as written is **incorrect** and must be fixed: **0.85**.
- Overall confidence that the split‑tracks proof program (rational‑Bessel + Langer + low‑degree) can eventually close the semi‑discrete KKT conjecture: **0.68**.
- Confidence that Round 25 proves the full real‑parameter KKT conjecture: **0.15**.
