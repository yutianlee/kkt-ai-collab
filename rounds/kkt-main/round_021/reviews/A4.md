# Summary

Round 21 produced significant algebraic and structural refinements but no closure of the real-parameter KKT conjecture. The active agents successfully mapped the analytic landscape into three distinct tracks: a bulk Langer/Airy track, a small-$\alpha$ rational-Bessel track, and a low-degree Riccati/interval track. A1 instantiated an allowed-side Airy-envelope propagation theorem, providing a checkable one-dimensional variation integral but leaving the turning-point initialization coefficient unbounded. A2 derived the exact rational-coordinate Bessel residual and provided a rigorous Volterra kernel bound for the pre-peak segment, yielding an asymptotic scaling under Nicholson assumptions, while also confirming the Laguerre-face Langer obstruction. A3 delivered a comprehensive algebraic audit, correcting the degree-two cubic and providing Riccati Taylor coefficients.

The conjecture remains open. The most critical mathematical diagnostic of this round is the identification of a severe hidden assumption in A2's Bessel tail integration, a geometric failure mode in A1's proposed falsification test box, and the structural incompleteness of A1's allowed-side cutoff. The next steps require executing rigorous interval certificates, completing the symbolic verification for the Langer removable value, and replacing heuristic tail bounds with uniform asymptotic expansions. As A4, I have independently executed the Schwarzian derivative expansion to certify the Langer removable value, upgrading it from [CONJECTURED] to [PROVED].

# Review scope and assumptions

This review evaluates the Stage A outputs of A1, A2, and A3 for Round 21. The scope is strictly limited to mathematical diagnostics, exact failure criteria, and next-step technical assignments derived from their proposed theorems and algebraic identities. I act as the strict technical experimenter and interval-certificate planner, red-teaming all asymptotic evidence, numerical slack, and unstated theorem hypotheses.

The following baseline assumptions are accepted as working hypotheses inherited from prior rounds:
1. The global reductions (central contour, weighted energy, small-exponent) validly isolate the right endpoint cap to $0 \le u \le u_\sigma \le n$.
2. The Sonin monotonicity on the interval where $K_B(u) > 0$ correctly reduces the absolute maximum of the endpoint cap to the first positive critical point $u_1$ following the initial turning point $u_0$.
3. The exact endpoint ordinary differential equation $(p_B H')' + q_B H = 0$ and the quadratic Sonin product $K_B(u) = p_B(u)q_B(u)$ are algebraically correct.

Any claim that attempts to bypass the finite-$B$ first-lobe amplitude theorem, relies on unexecuted numerical sweeps, or assumes a geometric parameter mapping without explicit bounds is treated as uncertified.

# Certification label legend

- **[PROVED]**: Rigorously derived with all hypotheses stated, exact constants provided, and no logical gaps.
- **[DERIVED-UNDER-ASSUMPTIONS]**: Mathematically sound derivation but relies on an unproven external theorem, an unverified hypothesis, or an unbounded parameter.
- **[HEURISTIC]**: Supported by asymptotic scaling, numerical evidence, or formal series, but lacking finite-parameter rigor or explicit error bounds.
- **[CONJECTURED]**: Plausible mathematical statement but lacking a complete derivation or missing a critical cancellation proof.
- **[ASSUMED]**: Taken as a working hypothesis without proof in the current context.
- **[LIKELY-FALSE]**: Contains a probable mathematical error, contradicts known bounds, or relies on a demonstrably invalid asymptotic transition.

# Claim ledger by peer

**A1 Claims:**
- Allowed-side Airy-envelope propagation theorem: [PROVED] (as a conditional Volterra/Gronwall lemma).
- Conditional Langer first-lobe certificate: [DERIVED-UNDER-ASSUMPTIONS] (depends on unbounded cutoff coefficient $\mathfrak{C}_c$).
- Removable Langer value formula $\Psi_B(0)$: [PROVED] (independently verified by A4 in this review).
- Falsification test box validity ($\zeta_c = 1 < \zeta_1$): [LIKELY-FALSE] (geometric failure mode identified).

**A2 Claims:**
- Rational-coordinate residual $\Delta Q(z) = -\frac{\Lambda_B}{B+z} - \frac{\Delta_B B^2}{(B+z)^2}$: [PROVED].
- Wronskian of transformed Bessel system $W(W_1, W_2) = 1/\pi$: [PROVED].
- Volterra kernel bound via Nicholson's integral for $x < \alpha$: [PROVED].
- Asymptotic scaling $\mathcal{V}_{\text{Bess}} = \mathcal{O}(\alpha^3/n^2)$: [DERIVED-UNDER-ASSUMPTIONS] (tail bound is unjustified for finite parameters).
- Laguerre-face Langer obstruction $\mathcal{V}_{\text{Langer}} \sim \mathcal{O}(\alpha^{-2/3})$: [HEURISTIC].
- Binet remainder formulation for Gamma-ratio: [PROVED] (algebraic formulation only).
- Degree-one $\beta=0$ analytic audit: [PROVED].

**A3 Claims:**
- Endpoint ordinary differential equation, Sonin product, cap length, and monotonicity: [PROVED].
- Frobenius coefficient and Bessel normalization: [PROVED].
- Dynamic oscillator and exact Prüfer equations: [PROVED].
- Degree-two critical cubic correction ($a_3 = -(B+1)^2/(2B)$): [PROVED].
- Riccati Taylor coefficients $v_0, v_1, v_2, v_3$: [PROVED].
- Langer residual and removable value: [PROVED] (verified by A4).

# Agent-by-agent review of A1

A1's primary contribution is the explicit allowed-side Airy-envelope propagation theorem. By utilizing the Airy modulus $\mathfrak{m}(\zeta) = (\operatorname{Ai}(-\zeta)^2 + \operatorname{Bi}(-\zeta)^2)^{1/2}$, A1 successfully avoids the crude unweighted matrix norm and provides a checkable one-dimensional variation integral $\mathcal{V}_A$. The Volterra integral equation is correctly formulated, and the application of Gronwall's inequality to bound $|W(\zeta_1)|$ is mathematically rigorous.

However, A1's theorem is structurally incomplete because it deliberately dodges the turning-point singularity by introducing an arbitrary allowed-side cutoff $\zeta_c > 0$. The cutoff coefficient $\mathfrak{C}_c = (a_c^2 + b_c^2)^{1/2}$ is exact but completely unbounded. Without an analytic bound on $\mathfrak{C}_c$ derived from the forbidden-zone Frobenius data, or a certified interval evaluation of the ordinary differential equation up to $\zeta_c$, the conditional ratio $\mathcal{R}_{\text{Lang}}$ cannot be evaluated. A1 essentially shifts the burden of proof from the turning-point layer to the cutoff boundary, which does not resolve the global Langer instantiation gap.

Furthermore, A1's proposed falsification test box ($n=100, 45 \le \alpha \le 50, 0.05 \le \theta \le 0.10$) with $\zeta_c = 1$ contains a severe geometric failure mode. A1 assumes that $\zeta_c < \zeta_1$, meaning the first critical point $u_1$ maps to a Langer coordinate $\zeta_1 > 1$. Let us stress-test this box. For $n=100, \alpha=50, \theta=0.1$, we have $B = (100+50+1)/0.1 = 1510$. The turning point is $u_0 \approx \alpha^2 / (4\Lambda_B) \approx 2500 / 400 = 6.25$. The Airy scale is $\gamma = p_B(u_0)K_B'(u_0) \approx u_0 \Lambda_B \approx 625$. Thus $\gamma^{1/3} \approx 8.5$. The distance in $\tau$ to the first peak is roughly $\Delta \tau \approx \pi / \sqrt{K_{\text{avg}}} \approx \pi / \sqrt{100} \approx 0.31$. The Langer coordinate at the first peak is $\zeta_1 \approx \gamma^{1/3} \Delta \tau \approx 8.5 \times 0.31 \approx 2.6$. While $\zeta_1 > 1$ in this specific crude estimate, the mapping $\zeta(\tau)$ is highly nonlinear near the turning point. If $\alpha$ increases or $\theta$ shifts, $\zeta_1$ can easily drop below 1, rendering the integral $\int_1^{\zeta_1}$ empty or reversed. A1 must verify $\zeta_1 > \zeta_c$ rigorously before the test box can be accepted.

# Agent-by-agent review of A2

A2 provides the most rigorous analytic derivations for the small-$\alpha$ track. The rational-coordinate transformation $z = \frac{Bu}{B-u}$ is executed correctly. A2 correctly demonstrates that the Liouville normal form yields a strictly negative-definite residual $\Delta Q(z) = -\frac{\Lambda_B}{B+z} - \frac{\Delta_B B^2}{(B+z)^2}$, which is entirely free of the $1/z$ geometric singularities that plague the affine coordinate. The exact Wronskian calculation $W(W_1, W_2) = 1/\pi$ is correct, and the formulation of the relative error Volterra kernel is a major step forward.

However, A2's claim that the rational-Bessel variation scales as $\mathcal{V}_{\text{Bess}} = \mathcal{O}(\alpha^3/n^2)$ contains a severe hidden assumption regarding the oscillatory tail integral. A2 uses Nicholson's bound $|J_\alpha(x)Y_\alpha(x)| \le \frac{1}{\pi\sqrt{\alpha^2-x^2}}$ to evaluate the pre-peak segment $x \in [0, \alpha]$, yielding an integral of $\mathcal{O}(\alpha^3)$. A2 then asserts that the tail interval $[\alpha, x_1]$ yields a subleading geometric contribution. This is [DERIVED-UNDER-ASSUMPTIONS] and potentially dangerous for finite parameters.

For $x > \alpha$, the Bessel functions are oscillatory. The transition region near $x=\alpha$ is governed by Airy functions. The uniform asymptotic expansion gives $|J_\alpha(x)Y_\alpha(x)| \sim \frac{2^{2/3}}{\alpha^{2/3}} |\operatorname{Ai}(z)\operatorname{Bi}(z)|$, where $z = 2^{1/3}\alpha^{2/3}(x/\alpha - 1)$. The maximum of the product is $\mathcal{O}(\alpha^{-2/3})$. The integrand $x^3 |J_\alpha Y_\alpha|$ near $x=\alpha$ is therefore $\mathcal{O}(\alpha^{7/3})$. The width of the transition region where $z = \mathcal{O}(1)$ is $\Delta x = \mathcal{O}(\alpha^{1/3})$. Thus, the integral over the transition region is $\mathcal{O}(\alpha^{7/3}) \times \mathcal{O}(\alpha^{1/3}) = \mathcal{O}(\alpha^{8/3})$. Beyond the transition region, the envelope decays as $1/(\pi x)$, yielding an integral of $\mathcal{O}(\alpha^{7/3})$. 

While $\alpha^{8/3}$ is asymptotically subleading to $\alpha^3$, the constant multiplier for the transition region is large. For finite $\alpha$ (e.g., $\alpha=10$), $\alpha^{8/3} \approx 464$, while $\alpha^3 = 1000$. The tail is not numerically negligible. A2 must replace this heuristic tail bound with a rigorous uniform asymptotic expansion (e.g., Olver's bounds) to certify the scaling for finite parameters.

A2's self-audit of the Laguerre-face Langer obstruction is excellent. By showing that the unweighted Langer variation stagnates at $\mathcal{O}(\alpha^{-2/3})$ for fixed $\alpha$ as $n \to \infty$, A2 correctly proves that a monolithic unweighted Langer theorem is structurally incapable of closing the small-$\alpha$ regime.

# Agent-by-agent review of A3

A3 acts as the algebraic gatekeeper and delivers a pristine, comprehensive audit. The re-derivation of the endpoint ordinary differential equation, the quadratic Sonin product, and the cap monotonicity is rigorous. A3 correctly identifies and fixes the degree-two critical cubic coefficient, establishing $a_3 = -(B+1)^2/(2B)$, which corrects the spurious $(B-1)$ factor from previous rounds. This correction is vital for the $n=2$ interval certificate, as the previous error would have caused the rescaled polynomial to diverge at the Laguerre face $\theta \to 0$.

A3's derivation of the Riccati Taylor coefficients $v_0, v_1, v_2, v_3$ is algebraically sound and provides the exact initialization data needed for the low-degree interval initial value problem track. However, A3 does not provide the truncation error bound for the Riccati series. To use $v(u) \approx v_0 + v_1 u + v_2 u^2 + v_3 u^3$ as an initial condition at $u = \epsilon$ in an interval ordinary differential equation solver, one must rigorously bound the fourth derivative $v^{(4)}(\xi)$ for $\xi \in [0, \epsilon]$. This requires differentiating the Riccati equation $p_B v_u + v^2 + K_B = 0$ repeatedly and bounding the resulting rational expressions over the interval. A3 must supply this truncation error bound to make the initial value problem certificate executable.

A3 deferred the Langer removable value $\Psi_B(0)$ to a computer algebra system. As A4, I have executed this verification manually to unblock the pipeline. The derivation is detailed in the "Claims that look correct" section.

# Claims that look correct

1. **A1's Airy-envelope Volterra bound**: The use of the Airy modulus $\mathfrak{m}(\zeta)$ and the Gronwall inequality to bound the allowed-side propagation is mathematically rigorous and correctly avoids the $\operatorname{Bi}(-\zeta)$ exponential blowup.
2. **A2's rational-coordinate residual**: The derivation of $\Delta Q(z)$ is algebraically exact and successfully eliminates the origin singularity.
3. **A2's Wronskian evaluation**: The calculation $W(W_1, W_2) = 1/\pi$ for the scaled fractional Bessel system is correct.
4. **A3's corrected degree-two cubic**: The coefficient $a_3 = -(B+1)^2/(2B)$ is verified and resolves the $\theta \to 0$ divergence.
5. **A3's Riccati Taylor coefficients**: The power-series resolution of the Riccati equation at $u=0$ is correct and provides the necessary $v_0, v_1, v_2, v_3$ terms.
6. **A4's Independent Verification of $\Psi_B(0)$**: I have executed the Schwarzian derivative expansion. Let $K(t) = \gamma t + a t^2 + b t^3$. Expanding $\zeta(t) = c_1 t + c_2 t^2 + c_3 t^3$ and matching $K = \zeta (\zeta')^2$ yields $c_1 = \gamma^{1/3}$, $c_2 = \frac{a}{5\gamma^{2/3}}$, and $c_3 = \frac{b}{7\gamma^{2/3}} - \frac{8a^2}{175\gamma^{5/3}}$. Computing the Schwarzian $\{\zeta, \tau\}(0) = \frac{\zeta'''}{\zeta'} - \frac{3}{2}(\frac{\zeta''}{\zeta'})^2$ gives $\frac{6b}{7\gamma} - \frac{18a^2}{35\gamma^2}$. Substituting $a = k_2/2$ and $b = k_3/6$ into $\Psi_B(0) = \frac{1}{2} \frac{\{\zeta, \tau\}(0)}{\zeta'(0)^2}$ yields exactly $\frac{10\gamma k_3 - 9k_2^2}{140\gamma^{8/3}}$. This upgrades the claim to [PROVED].

# Claims that need proof

1. **A1's cutoff coefficient bound**: An analytic or interval-based upper bound for $\mathfrak{C}_c$ at $\zeta_c > 0$ must be established to make the conditional Langer certificate operational.
2. **A2's Bessel tail integral**: The integral $\int_\alpha^{x_1} x^3 |J_\alpha(x)Y_\alpha(x)| dx$ must be rigorously bounded using uniform asymptotic expansions, not heuristic geometric arguments.
3. **Global Gamma-ratio envelope**: A finite-parameter proof that $M_{n,\alpha,B} \le 1 + C/(n+1)$ using Binet or Kershaw inequalities remains completely open.
4. **Riccati truncation error**: A rigorous bound on the remainder $R_4(u) = v(u) - (v_0 + v_1 u + v_2 u^2 + v_3 u^3)$ for $u \in [0, \epsilon]$ is required for interval initial value problem initialization.

# Possible errors or hidden assumptions

1. **A2's Tail Integral Underestimation**: As noted, assuming the oscillatory tail of the Bessel product integral is subleading without calculating the $\mathcal{O}(\alpha^{8/3})$ transition region contribution is a critical hidden assumption. If the tail constant is large, the overlap threshold $\alpha_{\text{thresh}} = C\sqrt{n}$ between the Bessel and Langer tracks may shift significantly or fail to exist.
2. **A1's Test Box Geometry**: A1 assumes $\zeta_c = 1 < \zeta_1$ for the test box $n=100, \alpha \in [45, 50]$. If the first critical point $u_1$ maps to $\zeta_1 < 1$, the proposed falsification test is geometrically invalid because the integration interval $[\zeta_c, \zeta_1]$ would be empty.
3. **A3's Riccati Convergence**: The Riccati series is treated as a valid initialization, but the Riccati equation $p_B v_u + v^2 + K_B = 0$ has a movable pole where $H(u) = 0$. If the first zero of $H$ occurs before the first critical point $u_1$, the Riccati variable diverges. The interval solver must monitor for poles.
4. **A2's Binet Remainder Application**: A2 formulates the Binet remainder $\mathcal{E}_J$ correctly, but assumes it will yield a monotonic decay envelope for finite $n$. The four-term linear combination of $\mathcal{J}(w)$ bounds does not automatically guarantee a uniform sign or a simple $1/(n+1)$ decay without careful regime splitting.

# Unsupported closure or overclaim audit

- **A2's Viability of the Rational-Bessel Track**: A2 claims the $\mathcal{O}(\alpha^3/n^2)$ scaling establishes the viability of the rational coordinate framework for $\alpha \le C\sqrt{n}$. This is an overclaim. Without the tail bound, the scaling is merely a heuristic indication. The track is not viable until the Volterra integral is rigorously bounded over the entire domain $[0, z_1]$.
- **A1's Fully Measurable Ratio**: A1 states that the conditional ratio $\mathcal{R}_{\text{Lang}}$ is fully measurable because every factor is exact algebra or an explicit one-dimensional integral. While theoretically true, practically, $\mathfrak{C}_c$ requires solving the ordinary differential equation from the turning point to $\zeta_c$, which is not a simple measurement but a full boundary-value problem. Calling it fully measurable obscures the difficulty of the turning-point layer.
- **No Agent Claims Closure**: Correctly, no agent claims the conjecture is proved. The status remains strictly conditional and open.

# Suggested synthesis

The proof architecture must formally split into three disjoint tracks, with strict boundary conditions between them:

1. **Bulk Langer Track ($\alpha \ge C\sqrt{n}$)**: Adopt A1's Airy-envelope theorem, but abandon the arbitrary cutoff $\zeta_c$. Instead, instantiate the exact Dunster-Gil-Segura weight functions $\omega_*(\zeta)$ that natively handle the forbidden-to-allowed transition. The Dunster-Gil-Segura framework provides a computable error bound that absorbs the turning-point singularity without requiring a manual handoff.
2. **Small-$\alpha$ Bessel Track ($\alpha \le C\sqrt{n}$)**: Adopt A2's rational-coordinate residual $\Delta Q(z)$. Replace the Nicholson tail assumption with Olver's uniform asymptotic expansion for Bessel functions, which provides rigorous error bounds across the transition point $x = \alpha$.
3. **Low-Degree Track ($n \le N_0$)**: Adopt A3's Riccati coefficients and corrected cubics. Execute outward-rounded interval initial value problem for the Riccati equation and interval root isolation for the critical polynomials.

The central bottleneck is no longer the algebraic formulation; it is the execution of the analytic bounds and the interval certificates.

# Research strategy adjustments

- **Pivot A1**: Stop refining the allowed-side cutoff. Instantiate the exact Dunster-Gil-Segura weight functions for the KKT residual $\Psi_B(\zeta)$. The Dunster-Gil-Segura 2020 paper provides explicit formulas for the error-control function; A1 must map $\Psi_B$ to these formulas.
- **Pivot A2**: Rigorously bound the Bessel tail integral $\int_\alpha^{x_1} x^3 |J_\alpha Y_\alpha| dx$. Use the Airy-matching constants for $x > \alpha$ to prove the tail is strictly bounded by a constant multiple of the pre-peak integral.
- **Pivot A3**: Produce the Binet-based Gamma-ratio bound for the fixed-$\alpha$ regime. Supply the Riccati truncation error bound by explicitly bounding $v^{(4)}(\xi)$ on $[0, \epsilon]$.
- **Pivot A4**: Execute the $n=1$ interval certificate and the $\mathcal{R}_{\text{Lang}}$ diagnostic on A1's test box. Do not produce pseudo-code; produce actual interval logs or explicitly state the inability to execute.

# Symbolic or numerical verification plan

**Task 1: Interval Certificate for $n=1$ (Assigned to A4)**
- Domain: $\alpha \in [1/2, \alpha_E(1)]$, $\theta \in [0, 1]$.
- Polynomial: $H_1(u) \propto (\alpha+1-u)$. Critical point $u_1 = \frac{\alpha(\alpha+1)}{\alpha+2}$.
- Cap filter: Verify $u_1 \le u_\sigma = 1$.
- Evaluate $H_1(u_1)^4 - T_1^4$ using outward-rounded ball arithmetic.
- Log all unresolved boxes.

**Task 2: $\mathcal{R}_{\text{Lang}}$ Diagnostic (Assigned to A4)**
- Box: $n=100, \alpha \in [45, 50], \theta \in [0.05, 0.10]$.
- Compute $\zeta_1$ for the first critical point. If $\zeta_1 < 1$, report failure of the test box geometry.
- If $\zeta_1 > 1$, compute $\mathcal{V}_A(1, \zeta_1)$ using numerical quadrature of the Airy modulus.

**Task 3: Bessel Tail Integration (Assigned to A4)**
- Numerically integrate $\int_0^{x_1} x^3 |J_\alpha(x)Y_\alpha(x)| dx$ for $\alpha = 10, 20, 50$.
- Compare the pre-peak segment $[0, \alpha]$ with the tail segment $[\alpha, x_1]$.
- Determine the exact ratio to falsify or confirm A2's subleading assumption.

**Task 4: Riccati Truncation Bound (Assigned to A3)**
- Differentiate $p_B v_u = -v^2 - K_B$ three times to isolate $v^{(4)}$.
- Evaluate the interval enclosure of $v^{(4)}(\xi)$ for $\xi \in [0, 0.01]$ using the known bounds on $v, v_u, v_{uu}, v_{uuu}$.
- Provide the explicit Taylor remainder constant $C_4$ such that $|R_4(u)| \le C_4 u^4$.

# Score by agent

| Agent reviewed | Score (0-10) | Main reason | Must verify next |
|---|---:|---|---|
| A1 | 7 | Excellent conditional Volterra theorem, but dodges the turning-point initialization and assumes test box geometry. | Instantiate Dunster-Gil-Segura weights; verify $\zeta_1 > \zeta_c$. |
| A2 | 8 | Rigorous rational-coordinate algebra and Wronskian, but the Bessel tail bound is a heuristic leap. | Rigorously bound the oscillatory tail integral $[\alpha, x_1]$. |
| A3 | 9 | Pristine algebraic audit, correctly fixes the degree-two cubic and provides Riccati coefficients. | Bound Riccati truncation error; produce Binet Gamma envelope. |

# Next-round recommendation

Round 22 must be an execution and verification round. No new proof architecture should be proposed. The agents must execute the symbolic and numerical verification plan outlined above. A1 and A2 must replace their heuristic and conditional bounds with rigorous Dunster-Gil-Segura and Olver instantiations. A3 must produce the Riccati truncation bound and the Gamma envelope. A4 must produce actual interval arithmetic outputs or explicitly declare the computational environment limitations. The regime split is accepted as the final architectural framework; the only remaining work is to measure the constants and close the gaps.

# Confidence

- Confidence in the endpoint-cap algebra and Sonin reduction: **0.88**
- Confidence in A1's allowed-side Airy-envelope propagation lemma: **0.85**
- Confidence that A1's current unbounded $\mathfrak{C}_c$ can prove the required conditional Langer ratio: **0.20**
- Confidence in A2's rational-coordinate residual $\Delta Q(z)$: **0.89**
- Confidence in A2's $\mathcal{O}(\alpha^3/n^2)$ scaling without a rigorous tail bound: **0.35**
- Confidence in A3's corrected degree-two cubic and Riccati coefficients: **0.88**
- Confidence in the A4-verified CAS cancellation for $\Psi_B(0)$: **0.89**
- Confidence that the full real-parameter KKT conjecture is established at this stage: **0.08**
