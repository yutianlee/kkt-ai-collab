# Round 24 Strategy Directive

Use the Round 23 judge synthesis as the controlling direction for Round 24. This note overrides the older Round 23 strategy note for current-round priorities.

Round 24 should focus on three deliverables:

1. **Final lemma-bank text.** Freeze the endpoint-cap algebra and add the $n=1$ residual endpoint theorem after a final normalization check.
2. **Corrected $n=2$ certificate.** Use the corrected degree-two cap polynomial, compactified variables, and interval/root-isolation checks. Do not use A4's rejected Round 23 low-degree formulas as written.
3. **One theorem-level constant inequality.** Produce either a rational-Bessel Volterra lemma with explicit constants and hypotheses, or a Langer/Airy constant inequality with explicit finite-parameter error control.

Agent allocation:

- A1: synthesize the lemma-bank update and state exactly what is now proved after Round 23.
- A2: repair the rational-coordinate Volterra track by giving explicit constants or a bounded failure.
- A3: perform the final normalization/algebra audit, especially for the $n=1$ theorem and corrected $n=2$ formulas.
- A4: build a corrected computational certificate plan/prototype for $n=2$ with actual interval-style logs where possible.

Do not spend Round 24 on broad proof architecture unless it produces one of the three deliverables above.
